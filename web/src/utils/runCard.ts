/**
 * 一张**运行**卡片在任务中心那一屏里的身份。
 *
 * 同一条运行会同时出现在**实况区**与展开着的**历次运行**里，因此「哪条运行」不足以指认一张卡片：
 * 按运行标识记「事件流开在哪」的话，两张卡片下面各画一份，用户会以为那是两条运行各自的失败。
 */

/** runCardId 拼出一张运行卡片的身份：它画在哪一区、画的是哪一条运行。 */
export function runCardId(origin: string, runID: number) {
  return `${origin}:${runID}`;
}
