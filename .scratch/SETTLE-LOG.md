# 结算记录

`/settle` 每清空一段挂账就在这里留一行。**一次读数说明不了什么，一串才是这个仓库自己的尺子。**

## 量表：每票条目数

一个 effort 记下的挂账条目，除以它握着的票数。对着 **1** 读——1 不是标定出来的，是结构性的：
在 1 上，那批票平均每张都撞上了一件它没预料到的事。

- **远低于 1** — 票是照着已经摸清的地面写的
- **1 上下** — 寻常的实现工作
- **远高于 1** — 票写在地形还没摸清的时候，缺掉的那份规格落进了挂账清单

高读数是关于 `/to-tickets` 的信号，不是关于挂账文件的：那批票该先走一趟 `/wayfinder`。

## 2026-09-08 · 第一轮

范围 `task-run-model` D1–D123 与 `task-limits-report` D1–D5，划定时 open 105 条，清空。

| effort | 票数 | 条目 | 每票条目数 |
|---|---|---|---|
| task-run-model | 19 | 123 | **6.5** |
| task-limits-report | 1 | 5 | **5.0** |
| task-run-handle（前几轮已清） | 6 | 10 | **1.7** |

**跨度 1.7 → 6.5（3.9×）。** 同为任务子域的三批票，差的不是题材而是票的成色：
`task-run-handle` 是一次范围清楚的接口收窄，`task-run-model` 是照着一份规格重写整个任务/运行模型，
而那份规格的表形状、状态机与关键决定平均每张票漏掉六件半事——**它想要的是先走 `/wayfinder`**。

三批更早的 effort（`diskwork` 8 票、`proposal-module` 6 票、`task-engine-run` 11 票）根本没有
挂账文件，那是**没有量具**，不是读数为 0，不进跨度。

本轮分布：记录 82 · 岔口 10（并成 6 个决定）· 工作 13（并成 10 件）。
记录占 78%——挂账被慷慨地记着，这是该失败的方向。
