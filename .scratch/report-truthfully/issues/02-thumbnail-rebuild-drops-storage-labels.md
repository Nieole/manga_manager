# 02 — 缩略图重建撤掉存储画像标签

**What to build:** 缩略图重建的运行详情里那个存储档位是假的。它发起时按**全局默认策略**（空路径）
解析一次写进声明，而它**逐库**扫；每完成一个库，逐库报文又按键覆盖一次。所以那一格最终显示的是
**最后一个跑完的库**的档位——既不是全局默认，也不是任何有意义的答案。配了按库策略的用户会照着
一个错的档位去调设置。

跨库的运行本来就没有单一的存储画像可报。这不是新判断：撤掉它的并发徽章时论证的正是这一条
（「那个数取自全局默认策略而它逐库扫」），徽章撤了、标签是留下的尾巴。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 缩略图重建的发起声明里撤掉存储画像、卷键、封面并发三项，**保留执行模式**
- [x] 逐库报文里那四个描述性值一并撤掉
- [x] 在任务引擎 seam 上断言：缩略图重建的运行上不带这几项，执行模式仍在
- [x] 在同一处断言：它的 IO **指标**（等待、暂停、耗时）不受影响，仍然照报
- [x] 前端用例守：这条运行的「存储 IO」那一条整条不显示——跨库的运行没有单一画像可报
      （后端撤干净之后这一条仍会画出五格数，因为票 01 把 `runIOParams` 的数值那一半改成了
      指标优先；整条改门在描述性那一半上才做得到，见挂账 **D11**）
- [x] 把「它逐库扫，因此没有单一的存储画像」写进该启动点的符号 doc，防止下次有人照着别的任务把标签加回来
- [x] 不改任何契约结构体、不重生 `generated.ts`
- [x] `CHANGELOG.md` 记一条
- [x] 后端与前端门禁全绿
