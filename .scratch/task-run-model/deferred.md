# 挂账清单 · task-run-model

跑票过程中冒出的非阻塞选择。流程不为它们停；由用户挑时机逐条处理。

## D1 · 票 05 · 作用域 id 用 `int64` 零值表示「没有」，还是 `*int64`

- **问：** 身份四要素里的作用域 id 今天在 `api.TaskStatus` 上是 `*int64`（JSON 侧要 omitempty）。
  领域包改成了 `int64`，0 表示系统级没有作用域对象。理由是身份的唯一约束要比较这一列，
  而 SQL 里 NULL 不等于 NULL——留 NULL 的话同一个系统级身份会被建出任意多条。
- **选项：** A `int64` + 0 表示没有（推荐，唯一约束天然成立，`Identity.Validate` 顺带把
  「系统级不得带 id」与「库级必须带 id」两条判掉）｜ B `*int64`，由 `taskstore` 在写入时 coalesce 成 0，
  代价是那条 coalesce 漏掉一处不会有编译错误
- **不处理会怎样：** 已按 A 落地。票 06 建表时 `scope_id` 应为 NOT NULL DEFAULT 0，
  接线时 api 的 DTO 要在边界上做一次 0 ↔ nil 的转换。
- **状态：** open

## D2 · 票 05 · 运行的展示标签落在哪张表

- **问：** 规格的表形状列了六张表（runs / run_events / run_samples / run_metrics / run_limits / run_args），
  没有标签的去处；而 `RunSpec.Labels` 与 `taskrun.Frame.Labels`（刮削源名等）今天确实存在，
  它们既不是重启入参也不是指标。领域包为此在端口上开了 `MergeRunLabels`。
- **选项：** A 单开一张 `run_labels`（推荐，与「五类语义各回各家」一致，读回时不必按前缀分拣）｜
  B 并进 `run_args`，靠键前缀区分，代价是那张表重新混进两类语义——这正是 `params` 堆的成因｜
  C 判定标签属于展示态、升为 `runs` 的定长真列，代价是加一个标签要改 schema
- **不处理会怎样：** 票 06 已按 A 落地：`run_labels` 单开一张。改成 B 只动适配器一侧，
  领域端口不动；但两类语义会重新同居一个命名空间。
- **状态：** open

## D3 · 票 05 · 落盘是同步写端口，还是承接今天那条异步落盘 goroutine

- **问：** 今天的引擎有一条 500ms 节流的落盘 goroutine（待写集合按键合并、终态唤醒立即刷、
  停机前再刷一次），它防的是「在临界区里同步写 SQLite 阻塞任务 API」。领域包对着的是一个端口，
  纯内存下这套机械没有意义，因此本票写成了同步调用。票 07 的验收里点名要重新论证这条。
- **选项：** A 领域同步写，节流与批量交给 `taskstore` 适配器（推荐，领域层因此没有「待写集合」这类
  与规则无关的状态，票 07 只需在适配器一侧决定怎么攒批）｜ B 把异步落盘原样搬进领域包，
  代价是端口多一条「刷盘」语义，而纯内存用例要为它另写一套等待
- **不处理会怎样：** 已按 A 落地。票 07 接线时若发现同步写真的压着任务 API，
  要在适配器里加节流，而不是把它加回领域层。
- **状态：** open

## D4 · 票 05 · 控制能力（可暂停 / 可取消 / 可恢复）落不落列

- **问：** 今天这三个布尔值是 `tasks` 表的真列。规格的新表形状里没有它们，而它们的事实来源是
  「这条运行的**暂停闸门**此刻还在不在」——闸门活在进程里。领域包因此把它们做成了
  `Capabilities`，由引擎按运行的活性派生，随快照一起投递。
- **选项：** A 派生、不落列（推荐，落成列的话重启后那几个布尔值会集体说谎：库里写着可暂停，
  而闸门早已随进程消失）｜ B 落成 `runs` 的列，读回时再按活性覆盖一遍，代价是同一件事有两个来源
- **不处理会怎样：** 已按 A 落地。票 07 接线时 api 的 DTO 要从 `Snapshot.Capabilities` 取这三个值，
  而不是从运行行上读。
- **状态：** open

## D5 · 票 05 · **排队中**运行的开始时刻留零值

- **问：** 排队中的运行还没开跑，`started_at` 写什么。领域包留零值，等它真的进入运行中才写——
  写入队时刻的话，排了一小时队的运行会被算成跑了一小时，速率与 ETA 一路失真。
- **选项：** A 留零值，放行时补写（推荐，速率的分母因此只包含真的在跑的那段）｜
  B 写入队时刻，另加一列记真正的开跑时刻，代价是多一列且两列的语义要靠命名区分
- **不处理会怎样：** 已按 A 落地。票 06 建表时 `runs.started_at` 必须可空（或接受零值），
  不能加 NOT NULL 且无默认。
- **收口（票 11）：** 领域侧仍按 A 留零值；对外契约那一格改成 `*time.Time` + omitempty，
  翻译只在 `runStatusFrom` 一处做（见 D54）。
- **状态：** done

## D6 · 票 05 · `taskrun` 的包 doc 仍把句柄的归属写作 api 的任务引擎

- **问：** `internal/taskrun` 的 package doc 写着「句柄由 api 的任务引擎构造并交给任务体」，
  而票 07 接线之后构造它的是 `internal/task` 的引擎。本票隔离在 worktree 里、不改现有文件，
  因此没有动它。词汇上「任务句柄」也该随规格改名为**运行句柄**。
- **选项：** A 留给票 09 的符号对齐一并改（推荐，那一票本来就要改名与重生成契约）｜
  B 票 07 接线时顺手改，代价是改名散在两票里
- **不处理会怎样：** 过渡期内那段包 doc 指着一个即将不成立的归属。
- **状态：** done（票 09 按 A：包改名为 `internal/runhandle`，归属改写作 `internal/task` 的引擎，
  注释里的「任务句柄」全仓改口为**运行句柄**）

## D7 · 票 05 · 落盘端口只有写入面，**运行事件**的读取与计数还没有去处

- **问：** 票 05 只要求端口容得下「将来的**写入**」，因此端口上只有 `AppendRunEvents` 与
  `AppendRunSamples`。票 16 的「条目失败每次运行最多留 500 条，超出只累加计数并落一条
  『还有 N 条未列出』」要先问一句「这条运行已经存了几条 item 事件」，那是读，端口今天答不出。
- **选项：** A 等票 16 落地时按它真正要的形状加读取方法（推荐，读取面的分页与筛选口径由详情页
  决定，现在猜出来的签名多半要改）｜ B 本票就加一个 `CountRunEvents(runID, kind)`，
  代价是端口多一个没有实现也没有调用方的方法
- **不处理会怎样：** 票 16 要往端口上加方法。票 05 的验收没有欠账——它只要求容得下写入。
- **状态：** open

## D8 · 票 05 · `AGENTS.md` 的 `internal/*` 包清单漏掉 `internal/task`

- **问：** `AGENTS.md` 的 Project Structure 一节逐个点名了 `internal/*` 下的包，新建的
  `internal/task` 不在其中；票 06 的 `internal/taskstore` 落地后同样会缺。这是共享文件，
  本票按隔离纪律没有动它。
- **选项：** A 等两个新包都落地后一次性补进去（推荐，一次改动补齐两个名字）｜
  B 每建一个包就改一次，代价是共享文件在批次里被反复碰
- **不处理会怎样：** 清单少两个包名，新来的 agent 得自己 `ls internal/` 才知道它们存在。
- **状态：** done（票 07 按 A 补上，两个包都落地了，同一句话里一起加）

## D9 · 票 06 · 身份表在过渡期不能叫 `tasks`

- **问：** 规格的表形状把身份表写作 `tasks`，而旧引擎那张同名表此刻仍在被读写，
  票 07 的验收还要求它「并存但不再被写入，也未被删除」。同一个库里两张同名表不可能并存，
  改名旧表会当场打断正在跑的旧引擎。本票因此把身份表建作 `task_identities`。
- **选项：** A 票 08 丢弃旧表时顺手 `ALTER TABLE task_identities RENAME TO tasks`（推荐，
  最终名字与规格、ADR 0004 与领域类型 `task.Task` 对齐；SQLite 会一并改写 `runs` 上那条外键引用）｜
  B 永久留作 `task_identities`，代价是库里的表名与规格的表形状对不上
- **不处理会怎样：** 票 08 不做这一步就等于选了 B。表形状、四列唯一约束与两条部分唯一索引
  在两种选择下完全一样，改的只是一个名字。
- **状态：** done（票 09 按 A，见 D34）

## D10 · 票 06 · i18n 占位参数落成 `runs` 的一列，还是自己一张侧表

- **问：** 规格写「i18n 占位参数随 `message_code` 落在 `runs` 上」，而 `Run.MessageParams` 是一张
  `map[string]string`，表形状里没有给它列。本票落成 `runs.message_params` 一列，内容是 JSON 对象。
- **选项：** A 落成 `runs` 的一列（推荐，与 `message_code` 同行整份改写，进度帧的热路径上不多两条语句；
  它不是 ADR 0004 否掉的那个堆——那个堆挤着五类语义且指标不可聚合，这一列只装一类且从不参与查询）｜
  B 单开 `run_message_params(run_id, key, value)`，与另外四张侧表同形，代价是每次文案变化都要
  先删后插，而那是逐条目进度在走的路
- **不处理会怎样：** 已按 A 落地。改成 B 只动 `taskstore` 一侧，领域端口不变。
- **状态：** open

## D11 · 票 06 · 时间列存 epoch 毫秒而不是 `DATETIME` 文本

- **问：** 新表的每一个时刻都存整数毫秒，与仓里既有表的 `DATETIME` 文本列不同。
  理由是保留裁剪的 DELETE 与聚合查询都要拿时间列做比较，而本仓已经吃过「两个写入方写出两种文本
  格式、SQLite 比的却是文本」的亏。
- **选项：** A 存整数毫秒（推荐，比较与聚合没有歧义，也不必再为时区与格式各写一个规范化函数）｜
  B 与既有表一致存文本，由 `taskstore` 保证只有一个写入方、只用一种格式，代价是那条保证没有任何
  机制守着
- **不处理会怎样：** 已按 A 落地。代价是直接用 sqlite CLI 翻这几张表时要自己换算时刻。
- **状态：** open

## D12 · 票 06 · 新表不推 `currentSchemaVersion`

- **问：** 建表语句是幂等的 `CREATE TABLE IF NOT EXISTS`，不带任何回填，因此本票没有把
  `internal/database` 的 `currentSchemaVersion` 加一——那个版本号门控的是随库规模线性增长的全量重算
  （系列首字母、统计、标签计数、来源、FTS 重建），为几张空表推一次会让每个存量库白算一遍。
- **选项：** A 不推，靠幂等 DDL 每次启动重放（推荐，与 KOReader 账号、书签用户隔离那两次的做法一致，
  也与那个常量自己的注释一致：「新增需要全量回填的 schema 变更时才 +1」）｜
  B 推一版当作 schema 变更的记号，代价是全体存量库在升级后的首启多跑一轮全量回填
- **不处理会怎样：** 已按 A 落地。票 08 丢弃旧表时若要推版本，那是它自己的决定，与本票无关。
- **状态：** open

## D13 · 票 06 · 侧表只有写入面，入参与上限的读回还没有去处

- **问：** 端口对四张键值侧表只声明了写入（`MergeRunArgs` / `MergeRunLabels` / `SetRunMetrics` /
  `AddRunMetrics` / `SaveRunLimits`），没有任何读回。而**重启函数**要读回原始入参、运行详情页要读回
  标签与当时生效的上限。D7 记的是**运行事件**那一半，这是键值侧表这一半。
- **选项：** A 等票 07（重启入参）与票 16/17（详情页）各自按真正要的形状往端口上加（推荐，
  读取面的形状由消费方决定）｜ B 本票就把四个读取方法加上，代价是端口多出一批没有调用方的方法
- **不处理会怎样：** 已按 A 落地：`taskstore` 今天只实现端口声明的写入面，聚合查询另有
  `AverageRunDuration` 与 `SumRunMetrics` 两个本包自己的方法。
- **状态：** open

## D14 · 票 06 · `AGENTS.md` 说 SQL 来源只有 `sql/query.sql` 与 `schema.sql`

- **问：** `AGENTS.md` 的 Project Structure 一节写着「SQL sources are in `sql/query.sql` and
  `internal/database/schema.sql`」，而任务与运行那几张表的 DDL 在 `internal/taskstore/schema.go` 的
  Go 字符串里（放进 `schema.sql` 会让 sqlc 生成一批与领域类型重名的模型）。这是共享文件，
  本票按隔离纪律没有动它。D8 记的是包清单缺项，这是 SQL 来源那一句。
- **选项：** A 与 D8 一并补进 `AGENTS.md`（推荐，同一节、同一次改动）｜
  B 把 DDL 挪进 `schema_handwritten.sql`，代价是表结构与它的迁移分居两个包
- **不处理会怎样：** 照那句话去找表的人找不到 `runs`，得先 `grep CREATE TABLE`。
- **状态：** done（票 07 按 A 补上，与 D8 同一次改动）

## D15 · 票 06 · `internal/database` 反过来依赖 `internal/taskstore`

- **问：** 新表的建表语句由 `database.Migrate` 调 `taskstore.Migrate` 落地，于是 `database` 依赖了
  `taskstore`，而仓内其余方向都是别人依赖 `database`。挂在这里是因为 `Migrate(dbPath)` 是全仓唯一的
  迁移入口，挂别处就得靠每个调用方记得再调一次，而 `internal/database` 自己的用例也拿不到新表。
- **选项：** A 留在 `database.Migrate`（推荐，迁移只有一个入口，忘不掉）｜
  B 由装配期（`cmd/server`）依次调两个 Migrate，`database` 因此不认识 `taskstore`，
  代价是新增入口时要记得补第二条调用，且 `internal/database` 的用例里没有新表
- **不处理会怎样：** 已按 A 落地。票 07 接线时若把 `taskstore` 的构造挪进装配期，可以顺便重看这条。
- **状态：** open

## D16 · 票 04 · **变体**落盘走任务参数还是给老表加一列

- **问：** 身份四要素里类型、作用域、作用域 id 三项都是 `tasks` 表的真列，**变体**没有。本票让它随
  任务参数（`params` TEXT）落盘再读回，理由是这张表随票 08 整表丢弃，而加一列要动 `schema.sql`、
  写一条迁移、重跑 `sqlc generate`，两票之后又全删掉。
- **选项：** A 维持随任务参数走（推荐，因为这张表两票之后就没了，而新表本来就有 variant 列）｜
  B 现在就加 `variant` 列并写迁移，让四要素在老表上也齐整
- **不处理会怎样：** 已按 A 落地。升级前落下的刮削与书哈希重建历史行读不回变体，
  `taskStatusFromRecordLive` 会按当前注册表把它们算成不可重试——界面上那个重试按钮消失（而不是
  点下去报错），重新发起一次即可，新起的任务不受影响。
- **状态：** open

## D17 · 票 04 · `TaskStatus.variant` 要不要现在就进前端契约

- **问：** 身份四要素的另外三项都在 JSON 里，本票把变体也放了进去并重生了 `web/src/api/generated.ts`。
  但前端今天没有任何消费方，而票 16 还要把整份契约重生一遍。
- **选项：** A 维持进 JSON（推荐，因为四要素分两处走的话，任何按快照判身份的地方都要再从别处补
  一项；测试也才能像断言另外三项一样断言它——规格的测试口径正是「只断言投递出去的载荷」）｜
  B 标 `json:"-"`，等票 16 连同契约重生一起放出去，代价是身份四要素在快照上缺一项
- **不处理会怎样：** 契约里多一个可选字段先于用途存在；从库里读回的历史任务，参数面板里会多出
  一行 `variant`（`phase`、`can_pause`、`metric.*` 今天已经是这样）。
- **状态：** open

## D18 · 票 04 · 播种脚手架的身份是必填还是留一个默认

- **问：** `taskSeed` 把 `Type` 换成了必填的 `Identity`，83 个播种点跟着改。另一条路是保留 `Type`、
  身份留一个「默认系统级」的兜底，只有关心作用域的用例才填。
- **选项：** A 必填（推荐，因为脚手架的 doc 本就写着「播种与生产同源」，而兜底会让按作用域筛选的
  用例静默换轨——它们不会变红，只会不再守着原来那条）｜ B 默认系统级，churn 小得多
- **不处理会怎样：** 已按 A 落地；代价是本票 diff 里测试改动占了大头。
- **状态：** open

## D19 · 票 02 · 任务体路径上的日志调用形状

- **问：** 票据的边界写着「不要为此改任何一处 `slog.Info/Warn/Error` 的调用形状」，但 Go 的 slog
  包级函数把 `context.Background()` 传给 handler——ctx 里的任务键因此永远到不了 `Handle`。要让
  handler 读得到，调用点必须改成 `slog.InfoContext(ctx, …)` 一族。同一条边界的另一句
  （「scanner 的日志在跑在任务 ctx 上时同样应当带上，守护/watcher/首扫那几条路径仍不带」）
  反过来要求调用点认 ctx：同一段代码带不带只由跑在谁的 ctx 上决定，只有带 ctx 的调用做得到。
- **选项：** A 三类任务走到的日志改成带 ctx 的形式（已落地）｜ B 全仓一次改齐，让「写日志用带 ctx
  的调用」成为无例外的仓内惯例｜ C 放弃 ctx 通道，改用 goroutine 局部存储读当前任务键——Go 没有
  官方支持，只能靠解析 `runtime.Stack` 或 `//go:linkname`，而且扫描器的 worker 是子 goroutine、
  继承不到
- **已按 A 改到的地方：** `internal/scanner`（扫描三阶段、封面 worker、软链遍历、收尾指标行）、
  `internal/metadata`（各 Provider 的搜索与上游失败）、`internal/images`（转码告警）、
  `internal/diskwork`（等令牌等太久那条）与 `internal/api` 的任务体。**其中两处不只服务任务**：
  `images.ProcessImage` / `ProcessImageDetailed` 是导出签名，前台阅读取页也从那儿走（那条路径
  永远取不到任务键，只是跟着改了形参）；`metadata` 各 Provider 的 `SearchMetadata` 同时服务
  交互式搜索端点。
- **不处理会怎样：** 已按 A 落地，代价是仓里两种调用形状并存：任务走到的认 ctx、请求处理与后台
  常驻那些不认。新写的日志落在哪一侧没有编译期约束，写错的后果是那一行在「查看日志」里过滤不到
  ——不会变红，只会少一行。
- **状态：** open

## D20 · 票 02 · 还有哪些日志没盖到，以及惯例该写在哪

- **问：** 按 D19 的 A 改完之后，仍有几处走在任务里却不带任务键的日志：`internal/database`
  （系列统计刷新的告警）、`internal/parser`（RAR 前滚跳过、ComicInfo 非 UTF-8 编码声明）与
  `internal/koreader`（书籍指纹重建、进度对账那几个任务体）。第一个本轮整包归并行 agent
  （票 06），碰不得；第二个要把 ctx 穿进 `Archiver` 接口与 `ParseComicInfo`，是另一场重构；
  第三个不在本票点名的三类任务里。另外 D19 定下的调用形状惯例今天没有归属，它该进 `AGENTS.md`
  的「Coding Style」，而本轮两条线都被禁止动那个文件。
- **选项：** A 都先欠着，惯例也先欠着（已落地）｜ B 票 06 合流后补 `internal/database`，
  `internal/koreader` 顺手改（都是现成的 ctx 形参），`internal/parser` 单开一票｜
  C 只补 `AGENTS.md` 的惯例条目，代码欠着
- **不处理会怎样：** 那几行在「查看日志」按任务键过滤时看不到——按钮有内容了，但漏掉的正是
  「系列统计没刷上」「归档里有一条读不出来」这类排障时最想看的行。惯例没有归属则靠这份挂账与
  `internal/logger` 的符号 doc 传递。
- **状态：** open

## D21 · 票 07 · 槽位与**排队中**本票不生效

- **问：** 领域引擎自带运行槽位（默认 2），超出的运行落进**排队中**。照默认接线的话，本票当场就会
  让第三条后台任务排队，并把前端不认识的 `queued` 状态推出去——而队列、**合并**、槽位上限与它们的
  界面是票 11 的整体。
- **选项：** A 本票把槽位设成实质无上限（`taskSlotsUnlimited`），保持今天「17 类可同时全开」的行为，
  票 11 回来放开（推荐，本票的边界写着「用户可观察的唯一变化是重试不再抹掉上一次」）｜
  B 现在就让默认 2 生效，代价是把票 11 的一半提前，且前端没有对应展示
- **不处理会怎样：** 已按 A 落地。**票 11 必须回来改这个常数**，否则并发上限那一票只剩界面、没有闸门。
- **收口（票 11）：** `taskSlotsUnlimited` 删除。上限改从运行时配置读（`tasks.max_concurrent_runs`，
  默认 2，设置页可改 1–16），领域引擎每次准入与放行现读一遍。
  `TestThirdRunStaysQueuedAtTheSlotLimit` 守着「第三条真的排队」，
  `TestRaisingTheSlotLimitAdmitsTheNextRun` 守着「改配置对新的放行生效」。
- **状态：** done

## D22 · 票 07 · 准入粒度从**任务键**改成**身份**，外部库那两类因此变了行为

- **问：** 旧闸门判的是任务键，而 `scan_external_library` / `transfer_external_library` 的键带着会话 id，
  于是同一个库的两个会话今天能并发。新准入按身份四要素判（关键决定 3「准入只剩一处」），第二个会话
  当场拿到 409。这是本票承诺之外的第二个用户可观察变化。
- **选项：** A 接受（用户已拍板：今天那个并发是键里恰好带了 session id 的副产品、不是设计，而同一个库
  同时跑两个传输本来就在争抢磁盘）｜ B 保留今天的并发，代价是再开一道按键的闸门，与规格直接冲突
- **不处理会怎样：** 已按 A 落地并写进 `CHANGELOG.md`。
- **状态：** open

## D23 · 票 07 · 进度落盘从异步节流改成同步直写

- **问：** 旧引擎有一条 500ms 节流的落盘 goroutine，它的符号 doc 点名了理由：**在临界区里同步写
  SQLite 会阻塞任务 API 与系列详情页，最长一个 busy_timeout**（扫描器的批量事务期间尤甚）。
  D3 已定「领域同步写，节流与批量交给 taskstore 适配器」，本票因此每一帧上报都是一次同步 UPDATE。
  写入频次由上报侧兜着（扫描器自己 250ms 节流一次），但这条风险是被量过的，不是假想。
- **选项：** A 先同步直写，压着了再在 `taskstore` 一侧加节流（推荐，把「读回要看得见刚写的」这件事
  留到有实测数据时再设计——写回缓冲要同时服务 `LoadRun`，那是一个要单独立票的机制）｜
  B 本票就在适配器里做写回缓冲，代价是准入下沉与写回缓冲两个都不好测的机制挤进同一次提交
- **不处理会怎样：** 已按 A 落地。表现是负载高时任务 API 变慢，而不是变错。
- **读取面同理**：投递一帧要把侧数据现取一遍（三张键值表加上限表，四句），而这也发生在**临界区内**。
  逐条目进度那一路由投递水位兜着（每 200ms 一帧），控制动作与终态那一路不受水位约束但本来稀疏。
  它与写入面是同一笔账、同一个去处（适配器侧的攒批与缓存），不在领域里存一份镜像——那会把
  「指标在落盘侧累加」这条唯一事实来源重新变成两份。
- **状态：** open

## D24 · 票 07 · `pause_reason` 从此恒为空

- **问：** 它今天的唯一来源是引擎在 `pause` 里写死的 `"manual_pause"`（也是它唯一的取值），
  经**任务参数**落盘、读回时解码。新模型的运行行上没有这一列，领域包也没有这个概念——
  **暂停闸门**统一之后「为什么暂停」只有一个答案。字段仍在 JSON 里，值恒为空串。
- **选项：** A 先留着空字段（已落地，前端 `pause_reason` 那处渲染因此不出现）｜
  B 本票就把它从契约里摘掉，代价是契约在票 09/16 还要再重生一遍｜
  C 在领域的运行行上补一列，把「谁按下的暂停」做成真语义——那要等票 11/12 有了自动发起方才有第二个取值
- **不处理会怎样：** 票 03 按 C 收口：全部暂停做成逐个按下之后，「谁把它按下的」第一次有了
  第二个取值，字段因此重新有话可说。`pause_reason` 落成 `runs` 的真列，取值是封闭枚举
  `manual` / `pause_all`，只在**已暂停**期间非空（离开暂停的每条出口随 `PausedAt` 一起清掉）。
  前端把它渲染在运行详情里，原先那条按**任务参数**渲染的死路一并撤掉。
- **状态：** done（票 03 按 C）

## D25 · 票 07 · 作用域显示名落成运行行上的**过渡期**列

- **问：** `TaskSpec.ScopeName` 多数取值是资料库名（可由作用域 id 解析回来），但刮削全库那条写的是
  「全库」——一个挂在系统作用域上的字面量，解析不出来。规格的表形状没有给它去处。
- **选项：** A 与**任务键**一样落成 `runs.scope_name` 一列并带在 `task.Run` 上，标明是过渡期字段
  （已落地，类型安全，不必靠保留键约定）｜ B 塞进 `run_labels` 的一个保留键，读回时提升并滤掉，
  代价是接反了不会有编译错误，而展示标签会多出一项｜ C 渲染时按作用域解析，「全库」另找去处
- **不处理会怎样：** 已按 A 落地。
- **状态：** done（票 10 按 A 定案：显示名留在**运行**上，任务清单从这个任务最近一次运行取它
  （`TaskSummary.ScopeName`）。理由是「全库」那个字面量仍解析不出来，而身份行只放长期属性——
  一个随每次发起可能变化的展示串不属于那里。代价是保留裁剪把一个任务的运行清光之后，
  那一行回落到「作用域 + id」；`runs.scope_name` 这一列因此与 `task_key` 一起留到票 16）

## D26 · 票 07 · 累计指标不再镜像成一份字符串塞进任务参数

- **问：** 旧引擎的「加指标」那条通道会把累加值同时写进 `Metrics` 与 `Params`（一份 int64、一份字符串）。
  存储 IO 面板读的是后者。镜像随 params 堆一起没了，而上报侧仍有两条通道：跨库重建走**累加指标**，
  单库扫描把整份报文写成一批字符串走**重启入参**。
- **选项：** A 读侧先看指标、再退回任务参数（已落地，与前端 `taskMetric` 早就一致的口径）｜
  B 把扫描观察者那条改道成指标，代价是那批数字会从参数面板搬进指标面板——一次用户可见的搬家
- **不处理会怎样：** 已按 A 落地。**票 16（运行详情）该顺手把 B 做掉**：扫描报文里那二十来个数
  本来就是指标，留在重启入参里既不可聚合，也让「**重启函数**读回原始入参」那句话名不副实。
- **状态：** open

## D27 · 票 07 · 同一任务键的多条运行，会在 SSE 到达时把彼此挤出当前页

- **问：** 前端 `BackgroundTasks.tsx` 收到一帧 `task_progress` 时按 `key` 去重再插到队首
  （`prev.filter(item => item.key !== task.key)`）。重试之后同一个键有多条运行，新运行的每一帧
  都会把上一次那条从当前页里摘掉，直到下一次轮询（15s）把它拉回来。
- **选项：** A 不动（本票的边界是「前端不改动即可正常显示」，而票 10 要把任务中心整个改成两层，
  去重键随之变成运行标识）｜ B 本票顺手把去重键改成 `run_id`，代价是改前端，越出本票边界
- **不处理会怎样：** 已按 A 落地。
- **状态：** done（票 10 按 B：去重键改成 `run_id`。实况区只装仍会变化的运行——一帧带着**终态**
  到达就把那条摘出去；同一帧还顺着 `task_id` 更新任务清单那一行的「上次结果」与展开着的历次运行）

## D28 · 票 07 · 端口的读取面本票一次加了三个方法

- **问：** D13 定的是「等消费方按真正要的形状加」。本票作为第一个消费方，加了 `LoadTasks`（批量取身份）、
  `LoadRunSideData`（批量取四张侧表）与 `DeleteRuns`（清除），并给 `RunFilter` 补了类型、作用域、
  作用域 id、任务键与关键词五条谓词。三个都是批量或下推形状，避免一页运行发出几十轮查询。
- **选项：** A 就这个形状（已落地）｜ B 侧数据分成四个读取方法，代价是列表接口对一页运行发四倍查询｜
  C 让端口交出一个「装配好的视图」，代价是端口开始长成表的形状——ADR 0006 明确拒绝的那件事
- **不处理会怎样：** 已按 A 落地。票 16/17 的详情页要读**运行事件**与**采样**时，读取面还会再长一截；
  那时值得回头看一眼这几个方法的分组是否仍然成立。
- **状态：** open

## D29 · 票 07 · 本票顺手删掉了票 08 清单上的一部分

- **问：** `golangci-lint` 的 `unused` 是门禁的一部分，因此接线一旦让旧引擎的内存表、异步落盘、
  `params` 编解码与记录/状态双向转换失去调用方，它们必须**在同一次提交里删掉**，没法留给票 08。
  票 08 的清单里也确实写着「若票 07 已判定不再需要」。
- **选项：** A 顺手删掉（已落地，门禁没有别的选择）｜ B 加 `//nolint` 留到票 08，代价是留一批
  没有读者的代码，还要带一条豁免注释
- **不处理会怎样：** 已按 A 落地。**票 08 的剩余范围因此变小**：只剩 `DROP TABLE tasks`、
  `database` 那批任务方法（`UpsertTask` / `ListTasks` / `DeleteTasks` / `MarkInterruptedTasks` /
  `MaxTaskSequence` 与 `TaskRecord`）的删除，以及 `user_version` 要不要推。
- **状态：** open

## D30 · 票 07 · 三条用例随它们守的机制一起消失或搬家

- **问：** 接线拆掉了几处机制，压在它们上面的用例因此失去被测对象：
  `task_clear_persist_test.go`（守「清除不被在途落盘写回来」——落盘只剩一处，那个窗口不存在了）整份删除；
  `TestHistoricRecordWithoutVariantIsNotOfferedForRetry`（守「可重试不照抄落盘那一列」——那一列没了）删除；
  `TestStopAllRuntimesCancelsBeforeResuming`（守「先取消后放行」的顺序）搬进 `internal/task`，
  因为判据要包住**运行时句柄**上的取消函数，而那是领域引擎的内部；api 侧留一条守信号确实通到了任务体。
- **选项：** A 按上述处置（已落地）｜ B 保留空壳用例，代价是它们守的是一段不再存在的实现
- **不处理会怎样：** 已按 A 落地。三条不变量里前两条如今是结构性的（只有一个写入方、没有那一列），
  第三条换了地方仍然有人守。
- **状态：** open

## D31 · 票 08 · 丢弃旧表不推 `currentSchemaVersion`（D29 的处置）

- **问：** 票 08 的验收写着「`tasks` 表经迁移删除，`user_version` 递增」。而本仓的
  `internal/database.currentSchemaVersion` 不是「schema 版本」，它门控的是随库规模线性增长的
  全量回填（系列首字母、统计、标签计数、来源、FTS 重建）——常量自己的注释就写着
  「新增需要全量回填的 schema 变更时才 +1」。为一句 `DROP TABLE IF EXISTS` 推一版，
  每个存量库都会在升级后的首启白算一遍 FTS 重建。
- **选项：** A 不推，`DROP TABLE IF EXISTS tasks` 每次启动无条件重放（推荐，与 D12 同一条口径，
  也与那个常量自己的注释一致；语句幂等，存量库照样掉表）｜ B 按验收字面推到 4，
  代价是全体存量库在升级后的首启多跑一轮全量回填，而它换来的只是一个记号｜
  C 另立一个「结构变更代」的版本号与回填代分开，代价是本票为一句 DROP 引入一套新机制
- **不处理会怎样：** 已按 A 落地，验收那一条按「表删掉了、版本号不推」记的账。
  **A 是可逆的那一侧**：将来真有需要回填的变更时推一版即可；B 一旦发布就在用户库上跑过了。
  `TestMigrateDropsLegacyTasksTable` 拿两个起始版本各跑一遍，钉住的是**这一句不在门控之内**
  ——「常量本身没被推」是一次性的事实，没有用例守得住，只有 code review 拦得下。
- **状态：** open

## D32 · 票 08 · 健康报告的「最近一次任务键」改从运行表取

- **问：** `attachLastTaskKeys` 给每条健康问题挂一个「去看这个库/系列最近那次任务的日志」的按钮，
  它此前查的正是旧 `tasks` 表——不改就没法丢表。查询整条搬进 `taskstore.LastRunKeysForScopes`
  （按 `task_identities` 的作用域分组、取 `runs.sequence` 最大的那条的 `task_key`），
  表名因此仍归 taskstore 自己，而不是让 `internal/database` 手抄别的包的表名。
- **选项：** A 搬进 taskstore（已落地，表名不外泄；定序换成 `sequence`，比旧的秒精度文本时间列更稳）｜
  B 留在 `internal/database` 里改表名，代价是身份表将来改名（D9）时这条 SQL 只会在运行期炸｜
  C 判定这个按钮随旧表一起作废，代价是 Organize 页少一个今天在用的入口
- **不处理会怎样：** 已按 A 落地。副作用是 `runs.task_key` 这条**过渡期**列多了一个读者：
  **票 10/16 把控制端点改成按运行寻址、准备删掉那一列时，要连这里一起改成按运行标识跳转。**
- **状态：** open

## D33 · 票 08 · `TaskFilters` 随旧表方法一起搬出 `internal/database`

- **问：** 删掉 `UpsertTask` / `ListTasks` / `DeleteTasks` / `MaxTaskSequence` 与 `TaskRecord`
  之后，`internal/database/tasks.go` 只剩一个 `TaskFilters`——而它早已不是数据访问层的东西，
  是六个任务端点共用的查询串形状，唯一的去处是 `runFilterFrom`。
- **选项：** A 搬进 `internal/api` 作 `taskFilters`，`tasks.go` 整份删除（已落地，调用点全在
  `internal/api` 内，全是机械替换）｜ B 留在 `internal/database`，代价是那个包里留一个自己一行
  代码都不用的 HTTP DTO，而文件头会开始说谎
- **不处理会怎样：** 已按 A 落地。它与票 09 的 `TaskStatus` → `RunStatus` 那批改名不冲突——
  换的是所在的包，不是概念名；票 09 若要连它一起改名，改的是同一个符号的一处声明。
- **状态：** open

## D34 · 票 08 · 身份表改名（D9）本票没有做

- **问：** D9 的推荐做法是「票 08 丢弃旧表时顺手 `ALTER TABLE task_identities RENAME TO tasks`」。
  本票没有做：改名要在存量库上先认出那张表再 RENAME，而认错的后果是 `runs` 的外键指向一张
  空表——这属于「一旦发出去就在用户库上跑过了」的那一类，而本票的边界是零行为变化。
- **选项：** A 留到一张自己的票，带着形状探测与升级路径用例（推荐，窗口还开着：这几张表尚未随版本
  发布，改名此刻仍然免费）｜ B 就此定为永久叫 `task_identities`，代价是库里的表名与规格、
  ADR 0004 与领域类型 `task.Task` 都对不上｜ C 本票顺手改，代价见上
- **不处理会怎样：** 表形状、四列唯一约束与两条部分唯一索引在两种选择下完全一样，
  差的只是一个名字。`taskstore/schema.go` 的 `tableTasks` 附近记着「不得只改这个常量」的陷阱。
  **发布之后再改名就要多带一段存量库的探测**，越晚越贵。
- **状态：** done（票 09 按 A：`renameLegacyIdentityTable` 走 ALTER TABLE RENAME，
  丢弃旧表那一句改成认形状（旧表有 `key` 列、身份表没有），
  `TestMigrateRenamesIdentityTableToTasks` 两个起始 `user_version` 各连跑两次守住它）

## D35 · 票 09 · i18n 键 `logs.taskStatus.*` 没有跟着改名

- **问：** 前端那八条状态文案的键是 `logs.taskStatus.all` / `.running` / …，`TaskCenter` 还按
  `logs.taskStatus.${task.status}` 拼着取。本票改的是代码符号，没有动这批键。
- **选项：** A 留作 `logs.taskStatus.*`（已落地，本票的判据是「它是不是那四组符号的一部分」，
  翻译键不是）｜ B 一并改成 `logs.runStatus.*`，代价是两份 locale 各改八行、拼串处改一处，
  且键名与外部翻译流程的对应关系断一次
- **不处理会怎样：** 键名里留着**任务状态**这个说法，而它描述的是一次**运行**的状态。纯命名，
  不影响任何渲染结果。
- **状态：** open

## D36 · 票 09 · 前端内部那条自定义事件仍叫 `manga-manager:task-progress`

- **问：** SSE 事件名已改成 `run_snapshot`，但 `Layout` 收到之后再 dispatch 的浏览器
  `CustomEvent` 仍叫 `manga-manager:task-progress`（另有 `-override` 一条），
  `BackgroundTasks` 与 `useExternalLibrary` 按这个名字监听。
- **选项：** A 只改 SSE 那一层（已落地，票据点名的是 SSE 事件名，浏览器事件名是前端内部的第二跳）｜
  B 两层一起改成 `manga-manager:run-snapshot`，代价是本票之外多碰三个文件，而它们与后端契约无关
- **不处理会怎样：** 同一份载荷在两跳里叫两个名字，读代码的人要多跳一次才知道它们是一件事。
- **状态：** open

## D37 · 票 09 · 任务体形参在 api 侧仍叫 `tp`

- **问：** 17 个启动点的任务体签名是 `func(ctx context.Context, tp *runhandle.Handle)`，
  `tp` 是旧名 `TaskProgress` 的缩写；`internal/task` 那侧的同一个形参已经叫 `handle`。
- **选项：** A 留着（已落地，`tp` 不属于票据点名的那四组符号，改它是另一次机械改动）｜
  B 一并改成 `handle`，与领域侧一致，代价是本票的改动面再宽出七十来处纯局部改名
- **不处理会怎样：** 同一个东西在两个包里的形参名不一样，新来的人要认两次。
- **状态：** open

## D38 · 票 09 · `api.RunStatus` 与 `task.RunStatus` 同名不同物

- **问：** 改名之后 `internal/api` 里同时看得见两个 `RunStatus`：本包那个是交给前端的**运行**快照
  结构体，`task.RunStatus` 是运行状态那个字符串枚举。`task_model.go` 一个函数里两者同时出现。
- **选项：** A 照规格的关键决定 16 落成 `RunStatus`（已落地，票据与规格都逐字这么写）｜
  B 叫 `RunSnapshot`，与 SSE 的新事件名 `run_snapshot` 对齐、也与 `task.Snapshot` 呼应，
  代价是偏离规格明文，且前端契约类型名要再改一次
- **不处理会怎样：** 包限定符是唯一的区分手段，读 `internal/api` 时要留意 `RunStatus` 与
  `task.RunStatus` 不是一回事。编译器分得清，人要多看一眼。
- **状态：** open

## D39 · 票 09 · api 侧其余 `Task*` 前缀的类型没有动

- **问：** `TaskResult`、`TaskLimits`、`TaskIdentity`、`TaskVariant` 都留着原名，
  `latestTaskByTypes`、`enrichTaskProgress`、`taskPausedSoFar` 这几个函数名同样。后两个类型对应
  **任务**与**变体**两个词条，名副其实；其余描述的其实是一次运行的结果、它生效的并发上限与它的进度。
- **选项：** A 只改票据点名的四组（已落地——判据是「名字里有没有那四个符号」，`listRunStatuses`
  与 `runStatusFrom` 因此改了，这几个没有）｜ B 把它们一并改成 `Run*`，
  代价是 `TaskLimits` 是 tsgen 目标，前端契约与消费方要再重生一次
- **不处理会怎样：** 任务子域里还留着一批按旧概念取的类型名与函数名，与刚改好的邻居自相矛盾。
- **状态：** open

## D40 · 票 09 · 身份表改名之后，回滚到票 08 那版会丢掉全部任务与运行

- **问：** 票 08 那版的 `internal/database` 里丢弃旧表是一句无条件的 `DROP TABLE IF EXISTS tasks`。
  本票改名之后身份表就叫 `tasks`，因此在升过级的库上退回旧二进制，首次启动会把身份表连同它级联的
  全部**运行**、事件、采样一起删掉——退回去的代码不认形状，只认名字。
- **选项：** A 记在案，靠「这几张表尚未随任何版本发布」把窗口关掉（已落地；受影响的只有开发机上
  升过级又退回去的库，而那些库里的任务清单本来就会被第一个守护 tick 重新长出来）｜
  B 改名推一次 `user_version` 或另立一个探针，让退回去的旧代码有办法认出来——办不到，
  旧代码里没有任何一处读它｜ C 不改名，代价见 D9
- **不处理会怎样：** 跨这次改名的回滚会静默丢掉任务清单。规格「迁移与提交顺序」那条
  「回滚只是把装配换回去」在这一步不成立，**发布前必须确认这一票与票 08 之间不做版本回退**。
- **状态：** open

## D41 · 票 03 · 「全部恢复」认状态不认**暂停原因**

- **问：** 全部暂停给每条被按下的运行记下了暂停原因（`pause_all` 对 `manual`），因此「全部恢复」
  本可以只放行自己按下的那些。本票让它认状态：所有**已暂停**的运行一律放行。
- **选项：** A 认状态（已落地，判据只留一处；按下「全部恢复」之后界面上不会剩着几条已暂停，
  而按钮已经翻回「全部暂停」）｜ B 只放行暂停原因为 `pause_all` 的那些，代价是按钮的取向
  （由「有没有运行被暂停」决定）与它实际会动的那批对不上，得再引入第三种显示状态
- **不处理会怎样：** 已按 A 落地。一条用户单独暂停的运行会被全部恢复一起放行——
  这是本票写进 `ResumeAll` 符号 doc 的明示行为，不是遗漏。
- **状态：** open

## D42 · 票 03 · `storageio` 的后台暂停从此没有生产调用方

- **问：** 两个用户端点移除之后，`PauseBackground` / `ResumeBackground` 在生产代码里一处调用都没有，
  只剩测试在用。诊断接口按卷那一栏的 `background_paused` 因此恒为 false，
  `settings.maintenance.pauseReason.manual_pause` 这条词条随之不可达。
- **选项：** A 原样保留（已落地——规格「Out of Scope」写明不动 `storageio` 自身，而票 11/12 的
  自动发起方、以及将来的「阅读时降载」策略都可能重新用上它）｜ B 连同那两个方法与诊断里那一栏
  一起删掉，代价是把一个仍有用的内部机制拆了，将来要重新长出来｜
  C 只删词条与诊断那一栏，保留机制
- **不处理会怎样：** 已按 A 落地。诊断面板上留着一栏恒为「后台可运行」的徽章，
  以及一条永远不会被选中的词条。
- **状态：** open

## D43 · 票 03 · 「有没有运行被暂停」这个答案仍挂在存储 IO 诊断接口下

- **问：** 语义按票据改了（`paused` 从「后台 IO 暂停开关」变成「有没有运行被暂停」），
  但它仍是 `GET /api/system/storage-io` 的一个字段——一个关于**运行**的事实挂在存储诊断的响应上。
  前端为了顶部那个按钮的取向，要拉一整份带库列表与策略解析的诊断。
- **选项：** A 原样留着（已落地，前端不必多开一条请求，改动面最小）｜
  B 挪进任务侧的实况汇总帧——规格关键决定 12 本来就要发一种轻量实况帧（槽位占用、活动数、
  排队数、全局暂停），这个字段是它的一格，等票 10 一起做｜ C 单开一个小端点，代价是多一条路
- **不处理会怎样：** 已按 A 落地。
- **状态：** done（票 10 按 B：新增 `GET /api/system/tasks/live`，实况帧带活动数、排队数、
  槽位上限、全局暂停与那批仍会变化的运行；`StorageIODiagnosticsResponse.Paused` 随之摘掉，
  任务中心不再为一个布尔值拉整份存储诊断）

## D44 · 票 03 · 全部暂停不看界面上的筛选条件

- **问：** 任务中心的列表带着状态 / 作用域 / 类型 / 关键词四条筛选，而「全部暂停」作用于**全部**
  运行中的运行，不受它们影响。
- **选项：** A 全局（已落地——「全部暂停」这个词说的就是全部，而且它要在几秒内让盘安静下来，
  按筛选分批按下与这个目的相反）｜ B 按当前筛选作用，代价是同一个按钮在不同筛选下动的东西不同，
  而清理任务那个按钮已经因为这一点配了「按当前筛选清理」这么长一个名字
- **不处理会怎样：** 已按 A 落地。想只暂停某一条的用户走那条运行自己的暂停键。
- **状态：** open

## D45 · 票 03 · 全部暂停只按得住**运行中**的运行，按不住**排队中**的

- **问：** `PauseAll` 筛的是 `StatusRunning`：排队中的运行没有开跑、没有闸门可按，也没有
  「排队且已暂停」这个状态。今天 `taskSlotsUnlimited` 关掉了槽位，队列里一条都不会有，
  因此不可达；票 11/12 打开槽位之后，按下全部暂停仍会有队列里的运行接着开跑，
  与「让盘安静下来」的目的相反。
- **选项：** A 只按运行中的（已落地，状态机里没有第五种状态可写）｜ B 给引擎加一个「全局暂停」
  的准入闸：暂停期间队列不放行，恢复时一并放开——那是槽位那一票的机械，本票没有队列可拦｜
  C 把排队中的一并取消，代价是「暂停」变成了「丢弃」，恢复之后它们不会回来
- **不处理会怎样：** 已按 A 落地，今天无差别。**票 11/12（槽位与队列）落地时必须按 B 收口**，
  否则全部暂停在有队列的机器上按不住盘。
- **收口（票 11）：** 按 B 落地。引擎多一个 `pausedAll` 闸门：全部暂停关上它，队列此后不放行、
  新发起的也先进排队；全部恢复打开它并一并放开。闸门经实况帧的 `paused_all` 发到界面上，
  「全部恢复」的可按性因此不再只看「有没有运行被暂停」（见 D57）。
- **状态：** done

## D46 · 票 01 · 中断运行那行「最后更新」显示的是重启时刻

- **问：** `MarkInterrupted` 把 `updated_at` 写成重启时刻（`finished_at` 取的是断掉前那次心跳）。
  任务中心每条运行下面那行相对时间读的是 `updated_at`，于是刚重启完，一条八小时前就没动静的
  中断运行写着「刚刚」。速率与耗时不受影响，它们取的是 `finished_at`。
- **选项：** A 原样留着（已落地——`updated_at` 在新模型里是「这一行最后一次被写的时刻」，
  那笔转写确实发生在重启时刻；其余三种终态下它与 `finished_at` 相等，只有中断不等）｜
  B 转中断时不改写 `updated_at`，让它继续等于心跳——代价是这一行拿到了新序号却声称没被写过｜
  C 前端在终态下改读 `finished_at`，`updated_at` 只服务活动态——那是票 10 重画任务中心的活
- **不处理会怎样：** 已按 A 落地。
- **状态：** done（票 10 按 C：前端 `runTimestamp` 在**终态**且有收尾时刻时改读 `finished_at`，
  `updated_at` 只服务仍会变化的运行。落盘侧一个字未动——`updated_at` 仍是「这一行最后一次被写的
  时刻」，改的只是界面读哪一个）

## D47 · 票 01 · 「心跳」是承重词，词汇表里没有它

- **问：** 「运行最后一次上报的时刻」这件事，代码注释、用例名与 CHANGELOG 里都叫它**心跳**，
  它是中断运行速率分母的那一端。`CONTEXT.md` 的后台任务一节没有这个词条。票 01 落地时把
  承重的几处注释改写成了「最后一次上报的时刻」这个白描，词只留在用例名与 CHANGELOG 里。
- **选项：** A 白描到底、不立词条（已落地，代价是同一件事在不同地方措辞不一）｜
  B 立一条词条——词汇表本批刚重写过，再加一条要连英文合集一起对齐｜
  C 把列改名成说得清自己是什么的名字，那是动 schema，不是本票的活
- **不处理会怎样：** 已按 A 落地。
- **状态：** done（票 10 维持 A：不立词条。任务中心重画之后，界面上唯一读这件事的地方
  （`runTimestamp`）已按 D46 改成在终态读收尾时刻，「心跳」因此只剩落盘侧一处内部说法，
  承重不到要占一条词条的程度；承重的几处仍写白描「最后一次上报的时刻」）

## D48 · 票 10 · **发起方**在每条运行上都显示，包括手动

- **问：** 今天全部运行的发起方都是**手动**（自动那三条路要到票 12 才建运行），于是这一格在每张
  运行卡片上都写着同一个词。票据的边界要求「展示位留出来但不得为了填满界面而伪造」。
- **选项：** A 照发（已落地，`manual` 是运行行上真实的取值，不是编出来的；规格的用户故事 2 要的
  正是「每条运行标出是谁叫来的」，票 12 一落地这一格就自己分化）｜ B 只在非手动时显示，
  代价是「手动」这个信息要靠一个徽章的缺席来表达，而缺席也可能是后端没发｜
  C 整格等票 12 再加，代价是展示位没有留出来
- **不处理会怎样：** 已按 A 落地。今天的表现是每张运行卡片多一个「手动」徽章。
- **状态：** open

## D49 · 票 10 · 同一排筛选器在清单与清除两条路上判的对象不同

- **问：** 任务清单的状态与关键词判在**每个任务最近一次运行**上（`task.TaskFilter.LastRun*`），
  而「按当前筛选清理」发的是 `DELETE /api/system/tasks`，那一路的状态判在**每条运行自己**身上。
  于是「上次成功、更早失败过」的任务不在失败清单里，它那几条失败运行却会被「按当前筛选清理」删掉。
- **选项：** A 保持两条口径，把差别写在界面上（已落地：筛选条上那句说明写明「筛的是任务清单，
  清理按钮删的是运行记录」）｜ B 让清除也按末次判，代价是「清理失败任务」清不掉历史里那些失败运行，
  而那正是这个按钮今天在做的事｜ C 清除改成只作用于清单上列出的那些任务，代价是要把一页 task_id
  发回后端，且用户看不见的历史运行反而删不掉
- **不处理会怎样：** 已按 A 落地。差别只在「先失败后成功」的任务上显现，而清除本来就是历史维护动作。
- **状态：** open

## D50 · 票 10 · 任务清单的两句取数落在领域端口上

- **问：** `ListTasks(TaskFilter)` 与 `LatestRuns(taskIDs)` 加在了 `task.Store` 上，而领域引擎
  一句都不用它们——它们只服务任务中心的取数。D28 记过同一件事：读取面按消费方要的形状加。
- **选项：** A 加在端口上（已落地，与 `LoadTasks`、`LoadRunSideData` 同一处；纯内存实现照样要
  守住同一份语义，因此定序与「没有上次」那条在两个底座上都有人守）｜ B 只做成 `taskstore.Store`
  自己的方法，由装配期把具体类型交给 api，代价是 api 从此持两个句柄，而领域侧的用例失去这两句的覆盖｜
  C 让端口交出一个装配好的「清单视图」，代价是端口开始长成表的形状——ADR 0006 明确拒绝的那件事
- **不处理会怎样：** 已按 A 落地。端口上因此有两个领域自己不调用的方法。
- **状态：** open

## D51 · 票 10 · 任务清单与历次运行都没有分页

- **问：** 清单一次取 50 行、历次运行一次取 20 条，两处都没有「加载更多」，也不报总条数。
  任务数受身份数封顶（类型 × 作用域对象），今天远不到 50；而历次运行受票 18 的保留裁剪封顶在 20。
- **选项：** A 先不做（已落地，两个上限都比现实的量级宽，且票 18 会把历史按 20 条裁掉，
  「取 20 条」正好等于「取全部」）｜ B 现在就加游标分页，代价是为一个还没出现的规模写一套翻页｜
  C 至少报一个总条数，代价是每次多一句 COUNT
- **不处理会怎样：** 已按 A 落地。库多到 50 个以上时清单会被截断且不提示。
- **状态：** open

## D52 · 票 10 · 顶部那五格计数撤掉，三条词条随之删除

- **问：** 原先顶部有活跃 / 暂停 / 失败 / 完成 / 中断五格，数的是**当前这一页**里的运行——
  带着用户的筛选与 50 条上限，从来不是全局答案。两层结构里实况区自带活动数与排队数（全局、真值），
  「失败了什么」由任务清单的上次结果与连败回答。
- **选项：** A 撤掉后三格，`settings.maintenance.failedTasks`、`logs.metric.completedTasks`、
  `logs.metric.interruptedTasks` 三条词条一并删掉（已落地）｜ B 留着但改成全局计数，
  代价是为三个数各发一句 COUNT，而它们答的问题任务清单已经答了｜ C 原样留着，代价是继续显示
  一组只对当前那一页成立的数
- **不处理会怎样：** 已按 A 落地。`logs.metric.*Hint` 那几条在本票之前就已无人引用，未一并清理。
- **状态：** open

## D53 · 票 10 · 槽位上限报 0 表示「没有上限可报」

- **问：** 槽位今天由 `taskSlotsUnlimited` 关着（D21）。实况帧要发一个分母，而把那个常数原样发出去
  会在界面上画出「2 / 2147483647」。
- **选项：** A 适配层把「上限等于无上限哨兵」翻成 0，前端见 0 就只画占用数（已落地，
  票 11 放开上限后这一格自己变成「槽位 n/N」，前后端都不必再改）｜ B 发那个哨兵值，
  由前端认它，代价是哨兵的取值成了跨进程契约｜ C 字段留空（omitempty），代价是 0 与「没发」
  在 JSON 里长得一样，而前端要为此多一层判断
- **不处理会怎样：** 已按 A 落地。0 在契约里既表示「没有上限」也表示「上限为零」，
  而后者不是一个合法的上限——`task.New` 把小于 1 的槽位数改写成默认值。
- **收口（票 11）：** 上限一放开这一格就自己变成了真数字——`slotLimit` 那层翻译随 D21 一起删掉，
  实况帧发的就是 `engine.Slots()`（恒 ≥ 1）。前端那条 `slots > 0` 的回落分支留着当兜底。
- **状态：** done

## D54 · 票 10 · **排队中**运行的开始时刻在契约里是 Go 的零值时刻

- **问：** `RunStatus.StartedAt` 是 `time.Time` 且不带 omitempty，而排队中的运行还没开跑
  （D5 定的：留零值，放行时补写）。它在 JSON 里因此是 `0001-01-01T00:00:00Z`，
  运行详情里那格「开始时间」会渲染成公元 1 年。今天不可达——槽位关着，队列里一条都没有。
- **选项：** A 记在案，留给票 11（推荐：那一票才让排队中的运行真的出现，改动与它的界面一起验才有判据）｜
  B 前端认那个字面量当作「没有」，代价是把一个魔法串写进渲染层｜
  C 后端改成 `*time.Time` + omitempty，代价是契约、`enrichTaskProgress` 与一批用例一起动，
  而本票没有任何一条可观察的行为需要它
- **不处理会怎样：** 已按 A 落地。**票 11 打开槽位时必须处理**，否则第一条排队运行的详情里
  写着它公元 1 年就开始了。
- **收口（票 11）：** 按 C 落地。`RunStatus.StartedAt` 改成 `*time.Time` + omitempty，
  `enrichTaskProgress` 在它为 nil 时不发速率与 ETA（分母不存在）。
  `TestQueuedRunOmitsStartedAt` 守着这三样一起缺席。
- **状态：** done

## D55 · 票 10 · 实况区的定序里没有「手动优先」

- **问：** 实况区按 `OrderLiveFirst` 取（序号降序），而序号在每一次可见变化时递增。票 12 之后，
  一条聒噪的守护扫描每报一帧就把自己顶到最前，安静的手动运行会一路下沉。
  验收那条「手动发起的运行在自动运行大量存在时仍然一眼可见」今天靠的是两层结构本身
  （自动运行折进它们各自的任务行）与**发起方**徽章，不是实况区的定序。
- **选项：** A 先不排（已落地：仍会变化的运行受准入索引限死在「每任务至多一活一排」，
  票 11 之后更受槽位上限压到个位数，一屏放得下，此刻加一条排序规则没有可观察的判据）｜
  B 实况区按发起方分组，手动的钉在最上，代价是同一区里出现两种定序，而「谁在最上面」
  不再等于「最近有动静的那个」｜ C 给实况区也加一排筛选，代价是它答的是全局问题（见 RunLive）
- **不处理会怎样：** 已按 A 落地。**票 12 让自动运行真的出现时要回头看一眼**：
  那时才有真实的量级判断这一条要不要排。
- **收口（票 12）：** 按 B 落地。判据出现了：守护扫描每小时一轮、监听扫描每次文件变动一轮，
  实况区里此后常驻着用户没点过的运行，而**在跑的那条每报一帧就取一个新序号**，
  用户刚按下的那次会一路下沉。定序因此分两组——手动一组钉在前，组内仍是序号降序
  （`task.OrderManualFirst`，前端 `applyRunToLive` 同形）。B 的代价照付：同一区里有两种定序，
  「谁在最上面」不再等于「最近有动静的那个」。只改实况区那一句取数，任务清单与历次运行不动。
- **状态：** done

## D56 · 票 10 · 实况汇总在前端也算了一遍

- **问：** 实况帧的活动数、排队数与全局暂停由后端 `taskEngine.live` 数出来，而 SSE 每到一帧，
  前端又按 `applyRunToLive` 重算一次——否则那三个数要等下一轮轮询（15s）才跟上，
  而「全部恢复」的可按性正由其中一个决定。同一个答案因此有两处算法。
- **选项：** A 前端重算，判据抽成一处并与后端同形（已落地：`applyRunToLive` 用
  `isActiveRunStatus` / `isQueuedRunStatus`，与 `taskIsActive` 同口径，另有用例守着）｜
  B 每帧回头拉一次实况接口，代价是进度帧的频次直接变成请求频次｜
  C 不重算、等轮询，代价是按下全部暂停之后「全部恢复」最长 15 秒按不下去
- **不处理会怎样：** 已按 A 落地。**票 19 发实况汇总帧时应当把这段拆掉**：帧里带着这三个数，
  前端就不必再算。在那之前，票 11 一旦让排队运行出现而它的变化没有被推送，这三个数会短暂偏差。
- **状态：** open

## D57 · 票 11 · 「全部暂停」期间**新发起**的运行也进排队

- **问：** D45 定的收口是「给引擎加一个全局暂停的**准入闸**」。闸门只拦住已经排上的那些，
  还是连此后新发起的一并拦下？只拦队列的话，用户按下全部暂停之后手动点一次扫描，盘照样转起来。
- **选项：** A 连新发起的一起拦（已落地：闸门关着时 `mustQueueLocked` 一律判排队，
  界面上那次发起表现为一张「排队中」的卡片，旁边就是亮着的「全部恢复」）｜
  B 只拦队列的放行，新发起照常开跑，代价是「全部暂停」这个名字对下一次点击不作数｜
  C 关闸期间直接拒绝新发起，代价是把刚被本票消灭的「冲突靠丢弃」又请回来
- **不处理会怎样：** 已按 A 落地。它是知情的：关闸期间点扫描不会立刻转盘，用户看到的是排队。
  闸门不落盘，重启即开——重启时活动运行本来就全部转**中断**，没有什么可以被它继续拦着。
- **状态：** open

## D58 · 票 11 · 控制端点改按**运行 id** 寻址，重试与清除仍按**任务键**

- **问：** 队列一开，同一个**任务键**此刻可以有两条仍会变化的运行（一条在跑、一条排队），
  而「这个键最近的那一次」由序号决定，序号每有一帧就换一次主人——用户按下排队那张卡片上的取消，
  动到的可能是正在跑的那条。关键决定 15 本来就写着「暂停 / 恢复 / 取消作用在**运行**上」，
  只是没有哪一张票认领它，而本票让它变成了当下就会出错的事。
- **选项：** A 三个端点改成 `POST /api/system/runs/{runID}/{pause,resume,cancel}`（已落地：
  卡片自己带着 `run_id`，没有第二种解释）｜ B 保留按键寻址，另加一个「要哪一条」的参数，
  代价是两种寻址并存｜ C 让按键寻址永远选活动那条，代价是排队中的运行在界面上取消不掉，
  而那是本票的一条验收
- **不处理会怎样：** 已按 A 落地。**重试**（作用在任务上）与**清除**（按筛选删运行记录）仍按任务键，
  `Run.Key` 那条过渡期字段因此还不能删——契约不再带任务键是它退场的前提。
  按键取「最近那一条」的其余调用点已经逐个看过：存储 IO 面板那条改成只看**开跑过**的运行
  （`task.StartedStatuses`），重试那条见 D61。
- **状态：** open

## D59 · 票 11 · **合并**丢掉的是后来那次的声明与任务体

- **问：** 并进已有的那条排队运行时，本次发起的 `RunSpec` 与任务体被丢弃，跑的仍是排在前面那份。
  两次发起参数不同的时候（例如强制重扫并进了一次普通扫描），用户按下的那次的参数不生效。
- **选项：** A 丢弃后来那份，只加计数（已落地：合并的语义就是「确保扫过」，
  而两份声明谁覆盖谁都要多一条规则）｜ B 用后来那份覆盖排队那条的声明，
  代价是「已经排上的那条会变成另一件事」，而用户看不到它变了｜ C 参数不同就不合并、各排一条，
  代价是「最多一条排队」那条索引挡着，且它正是本票要消灭的那串重复项
- **不处理会怎样：** 已按 A 落地。今天 17 个启动点里只有资料库扫描带 `force`，
  而强制重扫与普通扫描是同一个身份。票 12 让守护扫描也建运行之后要回头看一眼：
  自动发起与手动发起并成一条时，跑的是先排上的那份声明。
- **状态：** open

## D60 · 票 11 · 删库仍按三个**任务键**前缀取消，而不是按作用域筛

- **问：** `cancelLibraryScopedTasks` 拼 `scan_library_` / `scrape_library_` / `ai_grouping_library_`
  三个前缀去取消。本票把它从「取消最近那一条」改成了「取消这个键下每一条仍会变化的运行」
  （否则排在队里的那次扫描会在删库几分钟后自己开跑），但键前缀这份清单没动。
- **选项：** A 保留前缀清单（已落地：改动面最小，而这三类正是今天会在库上留下活动运行的）｜
  B 改成按**作用域**筛（`scope=library` + `scope_id`）取消全部仍会变化的运行——新模型里作用域是
  真列，这一句写得出来，代价是它会顺带取消清理、封面生成这些今天不在清单里的类型，
  行为变化没有用例覆盖｜ C 让落盘侧在删库时级联，代价是取消需要进程里的句柄，SQL 做不到
- **不处理会怎样：** 已按 A 落地。票 12、13 让守护扫描与封面生成也建运行之后，
  这份前缀清单会漏掉它们——那时按 B 改一次比现在补两个前缀更值。
- **收口（票 12）：** 仍按 A，清单补进 `cleanup_library_`。守护扫描与监听扫描用的就是
  `scan_library_` 这个键，本来就在清单里；新出现的只有监听器派生的清理。B 仍未做：
  它会顺带取消今天不在清单里的类型，而那没有用例覆盖。**票 13 的封面运行会再漏一次**，
  那时前缀清单是三条待补，按 B 改一次更值。
- **状态：** open

## D61 · 票 11 · 按**任务键**取「最近那一条」时，排队中的那条排在最前

- **问：** 序号在入队与每次**合并**时都会取一个新的，因此一个键上只要有排队运行，
  它就是「最近那一条」。重试端点按键取快照（`snapshotForRetry`），于是重试读到的是排队那条
  而不是在跑或刚失败的那条：它的重启入参与身份都相同，重启回来正好被合并进它自己（计数加一），
  今天没有可观察的差错，但「最近那一次」这个说法在有队列之后就不再等于「用户看着的那一次」。
- **选项：** A 不动（已落地：重试作用在**任务**上，读哪一条运行只是为了拿回入参与可重试性，
  而同一个身份的两条运行这两样完全相同）｜ B 重试改成按**任务 id** 寻址，读回身份而不是某条运行，
  代价是**重启函数**今天从运行快照上读入参，签名要跟着改｜ C 取「最近一条**开跑过**的运行」，
  代价是又多一处要记得排除排队中的地方，而它答的其实不是重试要的问题
- **不处理会怎样：** 已按 A 落地。运行详情页（票 16）如果也按键取「最近那一次」，
  要照着这条想一遍：那时用户是在看某一条具体的运行，取错就是显示了另一条。
- **状态：** open

## D62 · 票 12 · 建库首扫的**发起方**记「手动」而不是「串联」

- **问：** 票据把这一条留给实现时定：「串联」还是五种里的另一种。建库后的首扫是用户刚按下
  「添加资料库」的直接后果，而**串联**在词汇表里指「由另一件工作带起来的」——建库不是一次运行。
- **选项：** A 记手动（已落地：用户正等着看它扫到哪了，实况区的手动优先定序因此也把它排在前面）｜
  B 记串联，代价是「串联」从此有两种意思，其中一种没有上游运行可指｜
  C 新增第六种发起方，票据明确否掉
- **不处理会怎样：** 已按 A 落地。代价是它与用户在任务中心点「扫描」发起的那一次在徽章上分不出来。
- **状态：** open

## D63 · 票 12 · 闸门撤掉之后，同一个库上真的可能有两次扫描并发

- **问：** `ErrScanAlreadyRunning` 此前拦下的不只是「守护撞手动」，还包括**跨类型**的撞车：
  重建缩略图与重建索引的逐库强扫挂在系统作用域上，与 `scan_library_<id>` 是两个身份，
  任务那一处的准入不会拦它们。删掉闸门之后，这两趟强扫可以与一次库扫描在同一个库上同时跑。
- **选项：** A 让它们并发（已落地：闸门原本在这里是有害的——被跳过的库会被重建缩略图当作成功，
  而它已经删光了缩略图与封面路径，那一整库的书从此没有封面。ADR 0005 的取舍正是这一条）｜
  B 给逐库强扫也建**库级**运行，于是它与库扫描是同一个身份、自然排队，
  代价是「重建缩略图」会碎成一条系统运行加 N 条库运行，进度怎么合并要另想｜
  C 在扫描器里留一把「同一个库同时最多一次扫描」的锁，代价是准入重新有两个答案
- **不处理会怎样：** 已按 A 落地。两次扫描并发时入库走的都是 upsert，
  改名重连各自算各自的——没有用例覆盖这种并发，也没有已知的坏结果。
- **状态：** open

## D64 · 票 12 · 监听器那两条出口**等运行收尾**才返回

- **问：** 监听器按扫描的成败决定敢不敢接着清理（改名重连写在扫描末尾）。发起是异步的，
  因此 api 那两条出口要等到运行收尾才返回；运行排在队里的那段时间，监听器的 goroutine 一直挂着。
- **选项：** A 只等任务体（先落地过，随即换掉：**排队中被取消**的运行任务体一次都不会执行，
  等待方会一直挂到停机，而那个库的 `scansInFlight` 也一直不归零——它的幽灵记录从此再没人清）｜
  B 发起完就返回，把「清理必须跟在成功的扫描之后」改成由监听器下一轮的 rehome 守卫兜住，
  代价是那条守卫此刻看不见排队中的扫描（见 D65），中间会露出一个能删错行的窗口｜
  C 让引擎交出一个「这条运行收尾时通知我」的口子（`task.Engine.Await`），
  代价是领域引擎多一套等待者登记，而今天只有监听器用它
- **不处理会怎样：** 已按 C 落地。判据因此是**运行的状态**而不是「任务体返回了没有」，
  两者在排队中被取消时会分家。等的那段时间里 `scansInFlight` 保持非零，别的清理更保守地多等一轮，
  方向是安全的那一侧；`Stop` 取消 baseCtx 后立刻返回，停机不受影响。被**合并**掉的那次发起不等
  （它的任务体不会执行），交出 `scanner.ErrScanCoalesced`，监听器据此重新排期那轮清理。
- **状态：** open

## D67 · 票 12 · 资料库清理仍不可取消、不可暂停

- **问：** 票据正文写着「watcher 派生的扫描与清理…可暂停可取消」，而 `cleanup_library` 这个
  **任务类型**本身就不可取消（任务体刻意跑在 `context.Background()` 上，理由见
  `launchCleanupLibraryTask` 的 doc），也没有可暂停的可中断点——`CleanupLibrary` 认 ctx，
  但一次 `taskcontrol.Wait` 都不调。验收那条「与手动发起的没有区别」已经成立：
  同一个类型，手动发起的那条同样停不下来。
- **选项：** A 不动（已落地：控制能力是**任务类型**的属性，不是**发起方**的——按发起方分叉的话，
  同一个类型的两条运行会一条能取消、一条不能，而用户看不出为什么）｜
  B 让清理可取消：任务体改用运行 ctx、补一条取消文案码与两条词条，
  代价是停机时在飞的清理从「跑完」变成写进**已取消**（与其余可取消任务一致，但那是一次
  超出本票范围的行为改动）｜ C 顺带标 CanPause，代价是画出一个按下去什么也不会发生的暂停键——
  规格明确要求不可暂停的运行**在界面上明说**，而不是给一个假按钮
- **不处理会怎样：** 已按 A 落地。清理跑得很快（只做 stat），停不下来的窗口以秒计。
  真要做 B，它是**清理这个任务类型**的一次改动，两条发起路径一起变。
- **状态：** open

## D65 · 票 12 · 「这个库有没有扫描在跑」看不见**排队中**的扫描运行

- **问：** 监听器的 rehome 守卫问三句：这个库有没有排期中的扫描、本监听器有没有在飞的扫描、
  别处有没有扫描在跑。第三句问的是扫描器那笔记账，它只在扫描**真的进到函数体里**时为真——
  一条排在队里的守护扫描它看不见。
- **选项：** A 不动（已落地：改名一定会产生 Create(新名) 事件，监听器因此一定为该库排期一次扫描，
  前两句已经覆盖了「有改名待重连」这件事；排队中的别处扫描只是多扫一遍，不制造新的重连需求）｜
  B 让这一句改问任务引擎「这个库有没有仍会变化的扫描运行」，代价是监听器事件循环持着它那把
  全局锁去查一次 SQLite——那是热路径上唯一的全局锁｜ C 把这一句挪到锁外先算好再进锁，
  代价是它与判定之间又开了一个窗口
- **不处理会怎样：** 已按 A 落地。**票 13 让封面生成也建运行之后要再看一眼**：
  封面运行不写 books.path，与重连无关，因此这条判断不必跟着扩。
- **状态：** open

## D66 · 票 12 · 运行量的第四个来源：监听器派生的清理（给票 18 的量级备注）

- **问：** 规格按「5 个库、每小时一次守护扫描」估出每年 4.4 万条运行。本票之后实际来源有四类：
  守护扫描、监听扫描、监听清理、建库首扫。watch 模式的库每有一批文件删除/改名就多一条
  `cleanup_library_<id>` 运行，去抖窗口是 5 秒——批量整理书库时它比守护扫描密得多。
- **选项：** A 记在案，量级判断留给票 18（推荐：保留裁剪按「每任务留最近 20 次 + 终态 ≤90 天」算，
  清理任务与扫描任务是两个身份，各留 20 条，不必为它单开一条规则）｜
  B 现在就给监听清理定一个更短的保留，代价是保留规则按任务类型分叉，而票 18 还没写第一版｜
  C 让监听清理不建运行，代价是它动磁盘上的记录，正是 ADR 0005 要让它可见的那一类
- **不处理会怎样：** 已按 A 落地。票 18 估算容量时把「每库两个身份」算进去即可，
  没有哪一类自动运行的清理判据无处可依——它们都是普通的运行行，终态与序号一应俱全。
- **收口（票 18）：** 按 A，**监听清理不单开一条保留规则**，判断的依据有三条：
  一是配额判在**身份**上，清理与扫描是两个身份，一场批量整理再密也吃不掉那个库的扫描历史；
  二是一场整理里被裁掉的只是它自己最早的那几条，而同一场整理里最值得回看的恰恰是最近那几条；
  三是「上次成功是什么时候」与连败次数落在**任务行**上，不随运行被裁走——批量整理之后那一栏照样准。
  代价说清楚：两次清理之间（≤24h）一个 watch 库确实可能攒下上万条清理运行，
  下一次清理才把每个身份收回 20 条。真到了压不住的那天，该调的是**清理的节拍**（一个数字），
  不是让保留规则按任务类型分叉。
- **状态：** done

## D68 · 票 18 · 清理运行的**发起方**记「串联」而不是「定时」

- **问：** 清理由一条每天一次的后台节拍发起，读起来更像**定时**；而规格的关键决定 8 与票据正文
  都写作**串联**，本票按票据落地。两个词答的是同一个问题「这活是谁叫来的」，用户据此知道该去改哪个设置——
  而清理要改的设置（三个阈值）既不在某个库的扫描间隔里，也不由上一件事带出来。
- **选项：** A 维持**串联**（推荐，与规格、票据一致；它确实不是任何一个库的定时设置带来的）｜
  B 改成**定时**，代价是与规格的白纸黑字对不上，而「定时」在界面上会让人去资料库的扫描间隔里找它
- **不处理会怎样：** 已按 A 落地。改口只需改启动点那一个位置参数与一条用例断言。
- **状态：** open

## D69 · 票 18 · 清理运行不可暂停、不可取消，也不在重试注册表里

- **问：** 整个裁剪是一笔事务，中间没有可中断点，因此这条运行报的是不可暂停不可取消（同 D67 的资料库清理）。
  它也没注册**重启函数**，于是任务清单上没有重试按钮——改小阈值之后想立刻生效，只能等下一个节拍或重启。
- **选项：** A 维持（推荐，一笔事务的暂停键按下去只能是假的；等一天是这类维护活的常态）｜
  B 注册一条重启函数，让「立刻按新阈值清一次」成为一个按钮，代价是多一个只在改完设置那一刻有用的入口
- **不处理会怎样：** 已按 A 落地。阈值改动仍然生效，只是最长要等一个节拍。
- **状态：** open

## D70 · 票 18 · 清理在**启动时**先跑一次，而不是等满一天

- **问：** 节拍是 24 小时，但守护 goroutine 一起来就先清一次。理由是一台每晚关机的机器永远等不到
  那个 tick——而它恰恰是最需要清理的那一类：历史照常在长，清理一次都不会发生。
  代价是频繁重启的机器每次重启都留下一条清理运行。
- **选项：** A 启动兜底（推荐，与页缓存管家 startPageCacheJanitor 同形；多出来的那些清理运行
  自己也受同一套保留策略约束，堆不起来）｜ B 只等 tick，代价是短命进程从不清理｜
  C 把上次清理时刻落盘，按「距上次满 24h」判，代价是为一件每天一次的事新增一处持久化状态
- **不处理会怎样：** 已按 A 落地。调试期反复重启会在清理任务下留一串短运行，配额会把它们收掉。
- **状态：** open

## D71 · 票 18 · **采样**那一层不看运行死活，跑了 7 天以上的运行会丢掉早期曲线

- **问：** 采样裁剪是一句 `DELETE FROM run_samples WHERE at < ?`，不排除仍会变化的运行——
  运行本身受保护，它的早期采样点不受。一次跑满一周以上的大库扫描，详情页那条吞吐曲线因此从第 7 天开始。
- **选项：** A 维持（推荐，采样只用来看趋势，且「运行还在、曲线没了」正是规格给这一层写的话；
  排除活动运行意味着一条长跑的运行可以无上限地攒采样）｜ B 采样也排除仍会变化的运行，
  代价是最该被限住的那一条反而不受限
- **不处理会怎样：** 已按 A 落地。跑不满 7 天的运行（绝大多数）看不出区别。
- **状态：** open

## D72 · 票 18 · 清理运行要不要进票 15 的**续跑**白名单

- **问：** 票 18 写着「它是**可续跑**的」，而票 15 的白名单成文更早，里面没有它，续跑机制本身
  也还没落地。本票的守护节拍**启动就先清一次**（D70），因此重启后那条中断的清理实际上已经被接上，
  只是**发起方**记的是串联而不是恢复。
- **选项：** A 不进白名单，靠启动兜底那一次（推荐，同一身份的两次发起只会**合并**成一条，
  进白名单等于每次重启都发两次，其中一次必然被合并掉）｜ B 进白名单，发起方因此如实记作恢复，
  代价见 A｜ C 进白名单并去掉启动兜底，代价是全局续跑开关一关，清理就再也不在启动时跑了——
  而那个开关防的是「无人看着时自己动磁盘内容」，与删自己的历史行不是一回事
- **不处理会怎样：** 已按 A 落地：票 15 落地时不必为清理运行做任何事，重启后照样清。
  发起方那一格记的是串联，与 D68 是同一个口径问题。
- **状态：** done（票 15 复核：理由成立，清理运行两边都没进。`startRunHistoryJanitor` 在
  `markInterrupted` 之后起步、启动就先清一次，那条中断的清理因此照样被接上。）

## D73 · 票 18 · 「阈值≤0 即这一层不裁剪」这条纯判据定死在 SQL 谓词里

- **问：** 分层保留的判定按 ADR 0006 归 `internal/task`，好让它能纯内存测；而「零或负数的阈值
  一律不裁剪」这一条今天写在 `taskstore.PruneRuns` 的三个 `if … > 0` 上，用例只跑得起真 SQLite。
  领域侧只在 `RetentionPolicy` 的符号 doc 上声明了它。
- **选项：** A 维持（推荐：那三个 `if` 与它们守着的谓词贴在一起，隔一层反而更容易漏；
  而「选中了哪些行」本来就只有 SQL 答得出，判据与谓词同处一地不必两处对照）｜
  B 在领域侧加一个 `RetentionPolicy.Normalize()`，把非正阈值先归零再交给端口，代价是同一条规则
  有两个实现，且端口仍然不能指望调用方一定归一化过
- **不处理会怎样：** 已按 A 落地，`TestPruneWithNonPositivePolicyRemovesNothing` 在真库上守着它。
  api 那一侧另有 `retentionPolicyOf` 把越界值收在「不裁剪」这一侧，那是方向问题不是判据重复。
- **状态：** open

## D74 · 票 18 · 保留不提供「关掉」的出口，要留得久就把数字调大

- **问：** 三个阈值有没有一个「这一层永不清理」的表达。首版曾用 -1 当哨兵，code-review 判它是
  票据没要的语义（票 18 的立论恰是「不定保留策略，这套设计跑一年会自己把自己压垮」），
  而它当场带出一个真 bug：负得足够多的天数乘成 time.Duration 会绕回一个很小的**正**时长，
  下一次清理会把 24 小时前的终态运行全删。
- **选项：** A 不设哨兵，小于 1 一律归一化为默认值，要留得久就把数字调大——36500 天与 100 万条
  在实感上就是「永久」（推荐，与票据一致，也没有第二种解释）｜ B 重新引入一个哨兵值，
  代价是设置里多一个「关掉保护」的开关，而这套设计的前提正是它一直开着
- **不处理会怎样：** 已按 A 落地。端口那一侧仍认「零或负数不裁剪」（见 D73），只是设置到不了那里。
- **状态：** open

## D75 · 票 14 · **退避**曲线的起点是一个不可配的常数，三个可配数只决定形状

- **问：** 票据点名可配的是三个数（倍率、封顶、停发阈值），而一条指数曲线还需要一个**起点**。
  本票把它定成 `task.BackoffFirstInterval = 1h`，理由是「自动发起本身的自然节拍」——
  本仓资料库的默认扫描间隔正是 60 分钟（`config.DefaultScanInterval`），连败一次即等于跳过一个
  自然节拍。默认曲线因此是 2h、4h、8h、16h、24h（封顶咬住），第 6 次停发。
- **选项：** A 定成常数（推荐，票据只要三个数，多一个设置项就是多一个没人调的旋钮）｜
  B 起点也进设置，代价是四个数里有三个用户永远不会动｜ C 起点取这个库自己的扫描间隔，
  代价是**退避挂在任务上而扫描间隔挂在资料库上**——领域层拿不到它，而系统级任务根本没有间隔
- **不处理会怎样：** 已按 A 落地。要让退避更凶或更松，调倍率与封顶即可。
  已知代价：把扫描间隔设成 3 小时的库，第一档退避（2h）比它的自然节拍还短，那一档等于没退避——
  从第二次连败起（4h）才咬得住。最多多白转一遍盘。
- **状态：** open

## D76 · 票 14 · 重新**启用**一个任务不复位连败与**退避**

- **问：** 票据写死了两条复位路径——手动发起一次、或成功一次。人工启用不在其中，因此一个
  「连败 6 次之后被禁用」的任务重新启用后仍停在 fail_limit，界面上如实写着为什么。
- **选项：** A 不复位（推荐，与票据字面一致；连败是关于那个目标还坏不坏的事实，用户拨一下开关
  并不能让盘插回去，而界面写明了原因、旁边就是重试按钮）｜ B 启用时一并复位，代价是多一条
  票据没要的复位路径，且它会让「我已经修好了」这个判断由一个不相干的动作代为作出
- **不处理会怎样：** 已按 A 落地。用户按一次重试即复位，那本来就是复位路径之一。
- **状态：** open

## D77 · 票 14 · **停发**只挡「定时」与「监听」，串联与恢复不受约束

- **问：** 五个**发起方**里，`Trigger.IsAutomatic()` 只认定时与监听。串联（清理运行历史、
  链式哈希回填）与恢复（票 15 的重启续跑）都不受停发约束。
- **选项：** A 只挡这两个（推荐：票据正文写的正是「定时与监听不再为它发起」，而串联是上一件事
  带出来的收尾工作、恢复每次重启至多一次，两者都形不成「每小时白转一遍盘」那个循环；
  把清理运行历史挡在一个不相干的连败后面，历史就再也不清了）｜ B 恢复也挡住，
  代价是一个连败中的任务重启后连一次续跑的机会都没有，而重启常常正是「我插回硬盘了」的那一刻
- **不处理会怎样：** 已按 A 落地。票 15 落地时若发现续跑也需要挡，只改 `IsAutomatic` 一处。
- **状态：** open

## D78 · 票 14 · 界面上**退避中**不标红，标红留给不会自己恢复的那两条

- **问：** 后端交出的 `stall_reason` 有三条取值，而票据要的是「连败 ≥6 次停发并标红」。
  本票因此按原因分三档画：连败到阈值标红（那正是票据点名的那一条），人工禁用画中性徽章
  （用户自己按下的开关不是故障），退避画琥珀色并写明等到几点（它会自己走完）。
  三档共用同一行说明文字，写明连败几次、还要等多久、最后一次的错误是什么。
- **选项：** A 分三档（推荐，红色的含义因此恒是「要你去修那块盘」）｜ B 三条一律标红，
  代价是一次网络抖动导致的失败、一次用户自己按下的禁用都会让那一行红着，红色随之贬值
- **不处理会怎样：** 已按 A 落地。判据在 `TaskCenter.tsx` 的三个 `STALL_*` 常量与
  `stallColorClass` 一处。
- **状态：** open

## D79 · 票 14 · 监听派生的扫描被**停发**挡下时**不重排**，与被**合并**那条不同

- **问：** 监听器收到两条约定出口：`ErrScanCoalesced` 会重排一次清理（那条排着的扫描还没跑完），
  而本票新增的 `ErrScanSuppressed` 直接放弃这一轮，连**跟在它后面的那次清理**一起放弃。
  放弃清理不是策略而是安全条件：改名重连写在扫描末尾，扫描没发生就去删行，删掉的正是用户
  刚改过名的那些书。不重排则是因为条件不会因为再等一个去抖窗口而改变——重排只会每隔几秒再被挡
  一次并刷屏。单独排期的库清理（同轮没有扫描的那些）不走这条路，它受自己那个任务的停发约束。
- **选项：** A 不重排（推荐，见上）｜ B 照 coalesced 那条重排，代价是禁用一个库之后，
  只要还有文件变动，监听器就每隔几秒重试一次并各留一行日志
- **不处理会怎样：** 已按 A 落地。文件再有变动时会重新触发，届时若已解除停发就照常扫。
- **状态：** open

## D80 · 票 14 · 禁用开关按**任务 id** 寻址，与按**任务键**寻址的重试端点占同一段路径

- **问：** `POST /api/system/tasks/{taskID}/disable|enable` 与既有的
  `POST /api/system/tasks/{taskKey}/retry` 在同一段位置上用了两个不同的参数名。
  chi 各自取到自己那个（`TestDisabledTaskStopsAutomaticLaunchesButNotManualOnes` 经真路由验证过），
  但两个名字并列读起来确实容易看错。
- **选项：** A 维持（推荐：禁用是身份上的长期属性，只能按 id 寻址——外部库那两类的键带着会话 id，
  同一身份的两次运行键并不相同；而重试那条本来就是**过渡期**寻址，它改完这段路径就只剩一个参数名）｜
  B 给任务另开一段路径前缀，代价是同一个名词在 URL 里有两个位置
- **不处理会怎样：** 已按 A 落地。票据把重试改成按对象寻址之后，这条自然消失。
- **状态：** open

## D81 · 票 14 · **退避**三个数的归一化分在三处，溢出那一道落在 api 层

- **问：** 「三个数该是几」今天由三处共同回答：`config.NormalizeConfig`（小于 1 → 默认）、
  `api.backoffMaxDelay`（小时数收在 `maxBackoffHours` 内、负数交出 0）、
  `task.BackoffPolicy.orDefault`（不合法逐项退回默认）。code-review 判它是重复。
- **选项：** A 维持（推荐：它与同批次的保留三数**逐点同形**——`maxRetentionDays` 同样落在
  `internal/api/task_retention.go`，`NormalizeConfig` 同样只做「小于 1 → 默认」，判据同样由领域
  的符号 doc 拥有。只把退避那一份挪进 config，两个相邻设置就有了两种口径）｜
  B 把两处的溢出收口都挪进 `NormalizeConfig`，代价是配置层要认识 `time.Duration` 的量级，
  而那是翻译时才有的问题
- **不处理会怎样：** 已按 A 落地。三处各守一件事：配置层守「用户填的数」，翻译层守「换算会不会
  溢出」，领域层守「策略不合法时按什么办」。
- **状态：** open

## D82 · 票 14 · 禁用那两个端点自成一个生成器，没有与 `runControlHandler` 合并

- **问：** `setTaskAutoLaunch` 与既有的 `runControlHandler` 形状相同
  （`parseID` → 引擎 → `writeTaskControlError` → 202 加一句文案），code-review 判它是重复代码。
- **选项：** A 各自一个生成器（推荐：两者寻址的**对象不同**——一个是运行 id、一个是任务 id，
  连「无效 id」那句提示都不同；合并要多带一个路径参数名与一句提示，并改写三个本票没碰的端点，
  而共用的形状只有「解一个 id、调一次、回一句」）｜ B 合成一个按参数名取 id 的通用生成器，
  代价见 A
- **不处理会怎样：** 已按 A 落地。重试端点改成按对象寻址时这两个生成器会再照一次面，
  那时合并的收益比现在大。
- **状态：** open

## D83 · 票 15 · 重建索引不在**可续跑**白名单里

- **问：** 规格关键决定 7 的两份清单都没有 `rebuild_index`：它既不在可续跑那一列，也不在
  「改磁盘内容或花钱」那一列。白名单是**允许清单**，没列的一律不进，因此它重启后停在**中断**。
- **选项：** A 不进白名单（推荐：判据是清单本身，而它重灌完两份 FTS 索引还要**同步等一次全量扫描**
  ——那一段的实感与一次资料库扫描相同，白名单里却没有它；漏放一个只是多点一次重试）｜
  B 按「它不动磁盘内容也不花钱」的推理放进去，代价是把清单改成一条要现场推导的判据，
  而推错方向的代价不对称
- **不处理会怎样：** 已按 A 落地。重启之后索引重建停在中断，任务清单上那个重试按钮照旧。
- **状态：** open

## D84 · 票 15 · **恢复**不经**停发**闸门：退避中或被人工禁用的任务照样续跑一次

- **问：** `Trigger.IsAutomatic` 只认定时与监听，**恢复**不算，因此票 14 那道停发闸门管不到它——
  一个连败到停发、或者被人工禁用的任务，只要重启那一刻它正好有一条活动运行，就会被恢复一次。
- **选项：** A 维持（推荐：这条判据是票 14 落地时写进 `Trigger.IsAutomatic` 符号 doc 的，
  理由是「恢复每次重启至多发一次，形不成每小时白转一遍盘那个循环」；而禁用关的是「自己按节拍跑」，
  不是「已经在跑的那件事不许接上」）｜ B 让恢复也过停发闸门，代价是拔掉硬盘那个库的扫描
  在重启后彻底不再自愈，且「禁用」从此还多了一层「重启后不接上」的含义
- **不处理会怎样：** 已按 A 落地。被禁用的任务重启后至多多跑一次，随后照常不再自动发起。
- **状态：** open

## D85 · 票 15 · 同一个任务的两条中断运行只恢复一条

- **问：** 重启之前同一个任务可能既有一条活动运行、又有一条**排队中**的，两条转成中断之后
  白名单会同时挑中它们。而它们跑的是同一件事。
- **选项：** A 每个任务至多恢复一条，取序号最小的那条（推荐：两条都发起的话，后一条必然当场被
  **合并**进前一条，用户看到的是一条凭空带着合并计数的恢复运行——那个计数在这里没有任何意义）｜
  B 两条都发起，靠合并去重，代价见 A
- **不处理会怎样：** 已按 A 落地，`TestResumeYieldsAtMostOneRunPerTask` 守着它。
- **状态：** open

## D86 · 票 15 · 清理缩略图顺带进了重试注册表

- **问：** 白名单要求「清理缩略图」可续跑，而它此前没有**重启函数**——注册表是可重试的唯一来源，
  于是它在任务清单上没有重试按钮。续跑必须有人发得起，因此这一票给它补了一条重启函数，
  它随之也变成可重试的。
- **选项：** A 补上（推荐：**可续跑比可重试严格**，可续跑却不可重试是个说不通的组合——
  没人看着都能自己跑，用户点一下反而不行）｜ B 另开一条只给续跑用的发起路径，代价是同一件事
  有两个入口，而其中一个用户永远看不见
- **不处理会怎样：** 已按 A 落地。缩略图清理的中断运行从此有重试按钮，这是这一票的连带改动。
- **状态：** open

## D87 · 票 15 · 封面生成暂时不在白名单里，它随票 13 落地时补一行

- **问：** 票据的白名单列了「封面生成」，而封面还不是自己的一条运行（票 13 未落地），
  注册表里因此没有可写的键。
- **选项：** A 现在留空，票 13 建那条运行时在注册表里补 `Resumable: true`（推荐：白名单的键是
  类型名，而那个名字要等票 13 才定；先猜一个名字进去等于写下一条永远不匹配的白名单）｜
  B 预先写一个猜出来的类型名，代价见 A
- **不处理会怎样：** 已按 A 落地。`TestResumeWhitelistMatchesTheSpec` 逐项对着规格那份清单，
  票 13 补上封面运行时会顺手改到它。
- **状态：** open

## D88 · 票 15 · 续跑开关是配置里第一个 `*bool`

- **问：** 「默认开」的布尔值用零值表达不出来：配置文件里没写与写了 `false` 都是 `false`，
  升级上来的老配置会被当成「用户关掉了续跑」。
- **选项：** A 用 `*bool`，`NormalizeConfig` 把 nil 补成 true（推荐：它是唯一能分开「没写」与
  「关掉」的形状；代价是 `CloneConfig` 要多复制一个指针，`TestCloneConfigCopiesTheResumeSwitch`
  守着这一条）｜ B 反向命名成 `disable_resume_after_restart`（与 `disable_same_disk_page_cache`
  同形），代价是设置页上那个勾选框要取反渲染，而「勾上=关掉」是一类经典误读
- **不处理会怎样：** 已按 A 落地。读的那一侧（`taskResumeEnabled`）把 nil 也当成开着，
  归一化没跑到的路径不会静默关掉续跑。
- **状态：** open

## D89 · 票 15 · **发起方**成了重启函数的参数，包括不可续跑的那几个

- **问：** 续跑要把发起方记成**恢复**，而重启函数此前把 `task.TriggerManual` 写死在函数体里。
  只给白名单内的那几个加参数就够用，但注册表里会因此有两种签名。
- **选项：** A 全部收一个发起方参数，十一个启动点跟着改签名（推荐：一个忽略发起方的重启函数
  正是规格关键决定 2 拒绝的「猜一个发起方」——将来某个类型翻进白名单时，它会静默把恢复记成手动，
  而那不会有编译错误）｜ B 只改白名单内的那几个，代价见 A
- **不处理会怎样：** 已按 A 落地。改动是机械的：调用点一律显式写出 `task.TriggerManual`。
- **状态：** open

## D90 · 票 15 · **已暂停**与**取消中**的运行不进续跑，只有运行中与排队中进

- **问：** 重启把**活动态**（运行中 / 已暂停 / 取消中）与**排队中**一起转**中断**，
  而规格的状态机图只按**类型**分「自动重排队」与「停在中断」，没提状态。照字面实现的话，
  用户关机前刚按下取消的那条扫描，开机会被当成恢复重新发起。
- **选项：** A 只续跑重启前**运行中或排队中**的那些（推荐：暂停与取消是用户对这条运行的最后一次
  表态，而这一票的立论正是「无人看着时不要自己重跑」；漏续跑一条的代价只是多点一次重试）｜
  B 照字面来，四种状态一律续跑，代价是取消键与暂停键在一次重启之后失效｜
  C 只排除取消中，暂停照续，代价是「全部暂停」之后重启，盘立刻又转起来
- **不处理会怎样：** 已按 A 落地，`TestPausedAndCancellingRunsAreNotResumed` 守着它。
  这是相对票据字面的**收紧**，方向与用户故事 20 一致。
- **状态：** open

## D91 · 票 15 · 规格举的「可重试但不可续跑」两例，今天其实连可重试都不是

- **问：** 词汇表与规格关键决定 7 都用「外部库传输、ComicInfo 回写」举例说明可重试但不可续跑，
  而这三个类型（`scan_external_library` / `transfer_external_library` / `write_comicinfo`）
  从来没进过重启函数注册表，界面上没有重试按钮。本票的用例改用 `cleanup_library` 与刮削举证。
- **选项：** A 维持（推荐：给它们注册重启函数等于给**改磁盘内容**的工作加一个重试按钮，
  那是本票没要的行为变更，而且要先想清楚外部库那两类带会话 id 的键怎么重放）｜
  B 顺手补上三条重启函数，代价见 A
- **不处理会怎样：** 已按 A 落地。「可续跑 ⊂ 可重试」这条关系在代码里成立
  （`TestResumableIsStricterThanRetryable` 守着），只是规格举的那两个例子今天落在集合之外。
- **状态：** open

## D92 · 票 15 · 「续跑默认开」在四层各写了一遍

- **问：** `NormalizeConfig`（nil → true）、`Controller.taskResumeEnabled`（nil 按开着读）、
  `taskEngine.buildResumePolicy`（装配期没给开关按开着）、`task.ResumePolicy` 的零值，
  四处各自表达了一次「默认开」。code-review 判它是重复。
- **选项：** A 维持（推荐：与 D81 逐点同形——配置层守「用户填的数」，读取层守「归一化没跑到时
  往哪边倒」，装配层守「装配方什么都没说」，领域层守「策略缺省是什么」；四个问题不同，
  合并任意两个都会让另一条路径静默关掉续跑，而那正是这一票要修的）｜
  B 只留 `NormalizeConfig` 一处，其余直接读取值，代价是任何绕过归一化的构造（测试、
  手写 `Config{}`）都会静默把续跑关掉
- **不处理会怎样：** 已按 A 落地。`resumable` 里那处抢答（开关关着就不查库）按 code-review 删了，
  「续不续」如今只由 `ResumePolicy.Allows` 一处回答。
- **状态：** open

## D93 · 票 15 · `taskDispatchKey` 与 `task.ResumeKey` 是同一个（类型，变体）簇的两份

- **问：** api 用 `taskDispatchKey{Type string, Variant TaskVariant}` 做注册表的键，领域用
  `task.ResumeKey{Type Type, Variant Variant}` 做白名单的键，每次建策略都要转一遍。
- **选项：** A 维持（推荐：与 `TaskIdentity.domain()` 同一条边界——api 侧的类型是 string 与
  `TaskVariant`，领域侧是 `task.Type` 与 `task.Variant`，两侧各自的类型安全都值这一次转换；
  而把 `Config.Resume` 收成一个 `func(Identity) bool` 谓词能消掉这份重复，代价是白名单的判定
  从领域层搬回 api，票据明写它归领域层、要纯内存测）｜ B 收成谓词，代价见 A
- **不处理会怎样：** 已按 A 落地。转换只发生在 `buildResumePolicy` 一处，每次重启走一遍。
- **状态：** open


## D94 · 票 19 · 实况帧的取数没有世代号，慢网下会被自己的旧响应盖回去

- **问：** `BackgroundTasks.fetchLive` 直接 `setLive(res.data)`，没有 `fetchTasks` 那种请求世代号。
  轮询、控制动作与本票新增的「缺口重拉」都会发它，慢网下先发的响应后到就会盖掉推送帧刚更新过的
  实况区。本票把汇总帧做成了权威来源，这一幕因此更显眼——不过下一帧就会纠正回来。
- **选项：** A 维持（推荐：与 `fetchTasks` 那处世代号不同，实况帧一秒内就会被下一帧覆盖，
  而加世代号仍解不掉「HTTP 响应比 SSE 帧旧」那半边——那要拿汇总帧的序号跟响应比才行）｜
  B 照 `fetchTasks` 加一个 `liveRequestIDRef`，只挡住「旧请求盖新请求」那一半
- **不处理会怎样：** 慢网下实况区偶尔闪一下旧数，下一帧或下一次 60s 轮询纠正。
- **状态：** open

## D95 · 票 19 · 侧栏任务气泡不跟着重拉，丢帧之后它自己纠正不了

- **问：** 序号缺口触发的整份重拉只发生在任务中心页（它有 `/live` 与 `/summary` 两个端点可拉）。
  侧栏气泡由 `useTaskBubbles` 按**任务键**攒着，没有对应的「整份重拉」端点；丢掉的那一帧若恰好是
  终态，那个气泡会一直转到同一个键的下一帧或用户手动关掉。
- **选项：** A 维持（推荐：气泡本就是短命的提示，且终态气泡有 8s / 20s 的自动移除兜底，
  卡住的窗口有限）｜ B 让气泡也监听缺口事件并从 `/live` 重建，代价是气泡装的是**任务键**、
  实况帧装的是**运行**，两者的键不一样，重建要多一层映射
- **不处理会怎样：** 极少数丢帧下侧栏气泡比任务中心晚纠正一会儿。
- **状态：** open
