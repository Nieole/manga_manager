# 挂账清单 · task-run-model

> **2026-09-08 结算完毕。** 范围 D1–D123（划定时 open 100 条），一条不剩。
> 划定之后新记的条目归下一次结算。

本轮分布：记录 78 · 岔口 10 · 工作 12。记录的正文归 git，这里只留一行与去处。

## 已结算索引

| id | 来处 | 一句话 | 去处 |
|---|---|---|---|
| D1 | 票 05 | 作用域 id 用 `int64` 零值表示「没有」，还是 `*int64` | 记录 · 已知情，无待办 |
| D2 | 票 05 | 运行的展示标签落在哪张表 | 记录 · 已知情，无待办 |
| D3 | 票 05 | 落盘是同步写端口，还是承接今天那条异步落盘 goroutine | 工作 **W4** · 实测同步直写压不压任务 API，要压就在 `taskstore` 侧攒批 |
| D4 | 票 05 | 控制能力（可暂停 / 可取消 / 可恢复）落不落列 | 记录 · 已知情，无待办 |
| D5 | 票 05 | **排队中**运行的开始时刻留零值 | （前几轮已裁定） |
| D6 | 票 05 | `taskrun` 的包 doc 仍把句柄的归属写作 api 的任务引擎 | （前几轮已裁定） |
| D7 | 票 05 | 落盘端口只有写入面，**运行事件**的读取与计数还没有去处 | 记录 · 已被后续票消化 |
| D8 | 票 05 | `AGENTS.md` 的 `internal/*` 包清单漏掉 `internal/task` | （前几轮已裁定） |
| D9 | 票 06 | 身份表在过渡期不能叫 `tasks` | （前几轮已裁定） |
| D10 | 票 06 | i18n 占位参数落成 `runs` 的一列，还是自己一张侧表 | 记录 · 已知情，无待办 |
| D11 | 票 06 | 时间列存 epoch 毫秒而不是 `DATETIME` 文本 | 记录 · 已知情，无待办 |
| D12 | 票 06 | 新表不推 `currentSchemaVersion` | 记录 · 已知情，无待办 |
| D13 | 票 06 | 侧表只有写入面，入参与上限的读回还没有去处 | 记录 · 已被后续票消化 |
| D14 | 票 06 | `AGENTS.md` 说 SQL 来源只有 `sql/query.sql` 与 `schema.sql` | （前几轮已裁定） |
| D15 | 票 06 | `internal/database` 反过来依赖 `internal/taskstore` | 记录 · 已知情，无待办 |
| D16 | 票 04 | **变体**落盘走任务参数还是给老表加一列 | 记录 · 已知情，无待办 |
| D17 | 票 04 | `TaskStatus.variant` 要不要现在就进前端契约 | 记录 · 已知情，无待办 |
| D18 | 票 04 | 播种脚手架的身份是必填还是留一个默认 | 记录 · 已知情，无待办 |
| D19 | 票 02 | 任务体路径上的日志调用形状 | 记录 · 已知情，无待办 |
| D20 | 票 02 | 还有哪些日志没盖到，以及惯例该写在哪 | 工作 **W3** · `database`/`parser`/`koreader` 共 24 处日志补 ctx（惯例已写进 `AGENTS.md`） |
| D21 | 票 07 | 槽位与**排队中**本票不生效 | （前几轮已裁定） |
| D22 | 票 07 | 准入粒度从**任务键**改成**身份**，外部库那两类因此变了行为 | 记录 · 已知情，无待办 |
| D23 | 票 07 | 进度落盘从异步节流改成同步直写 | 并入 **D3**（同一个岔口：同步直写与攒批） |
| D24 | 票 07 | `pause_reason` 从此恒为空 | （前几轮已裁定） |
| D25 | 票 07 | 作用域显示名落成运行行上的**过渡期**列 | （前几轮已裁定） |
| D26 | 票 07 | 累计指标不再镜像成一份字符串塞进任务参数 | 工作 **W2** · 扫描报文十几个数从**重启入参**搬进**指标** |
| D27 | 票 07 | 同一任务键的多条运行，会在 SSE 到达时把彼此挤出当前页 | （前几轮已裁定） |
| D28 | 票 07 | 端口的读取面本票一次加了三个方法 | 记录 · 已被后续票消化 |
| D29 | 票 07 | 本票顺手删掉了票 08 清单上的一部分 | 记录 · 已知情，无待办 |
| D30 | 票 07 | 三条用例随它们守的机制一起消失或搬家 | 记录 · 已知情，无待办 |
| D31 | 票 08 | 丢弃旧表不推 `currentSchemaVersion`（D29 的处置） | 记录 · 已知情，无待办 |
| D32 | 票 08 | 健康报告的「最近一次任务键」改从运行表取 | （前几轮已裁定） |
| D33 | 票 08 | `TaskFilters` 随旧表方法一起搬出 `internal/database` | 记录 · 已知情，无待办 |
| D34 | 票 08 | 身份表改名（D9）本票没有做 | （前几轮已裁定） |
| D35 | 票 09 | i18n 键 `logs.taskStatus.*` 没有跟着改名 | **裁定 B**：五处一次扫干净，含重生前端契约 → 工作 W8 |
| D36 | 票 09 | 前端内部那条自定义事件仍叫 `manga-manager:task-progress` | 并入 **D35**（同一个岔口：改名扫到哪） |
| D37 | 票 09 | 任务体形参在 api 侧仍叫 `tp` | 并入 **D35**（同一个岔口：改名扫到哪） |
| D38 | 票 09 | `api.RunStatus` 与 `task.RunStatus` 同名不同物 | 并入 **D35**（同一个岔口：改名扫到哪） |
| D39 | 票 09 | api 侧其余 `Task*` 前缀的类型没有动 | 并入 **D35**（同一个岔口：改名扫到哪） |
| D40 | 票 09 | 身份表改名之后，回滚到票 08 那版会丢掉全部任务与运行 | 记录 · 已知情，无待办 |
| D41 | 票 03 | 「全部恢复」认状态不认**暂停原因** | 记录 · 已知情，无待办 |
| D42 | 票 03 | `storageio` 的后台暂停从此没有生产调用方 | **裁定 C**：删徽章与词条、机制留着 → 工作 W7 |
| D43 | 票 03 | 「有没有运行被暂停」这个答案仍挂在存储 IO 诊断接口下 | （前几轮已裁定） |
| D44 | 票 03 | 全部暂停不看界面上的筛选条件 | 记录 · 已知情，无待办 |
| D45 | 票 03 | 全部暂停只按得住**运行中**的运行，按不住**排队中**的 | （前几轮已裁定） |
| D46 | 票 01 | 中断运行那行「最后更新」显示的是重启时刻 | （前几轮已裁定） |
| D47 | 票 01 | 「心跳」是承重词，词汇表里没有它 | （前几轮已裁定） |
| D48 | 票 10 | **发起方**在每条运行上都显示，包括手动 | 记录 · 已知情，无待办 |
| D49 | 票 10 | 同一排筛选器在清单与清除两条路上判的对象不同 | **裁定 A**：维持两条口径，差别留在筛选条的说明文字里。closed |
| D50 | 票 10 | 任务清单的两句取数落在领域端口上 | 记录 · 已知情，无待办 |
| D51 | 票 10 | 任务清单与历次运行都没有分页 | 记录 · [产品缺口清单](../GAPS.md) |
| D52 | 票 10 | 顶部那五格计数撤掉，三条词条随之删除 | 工作 **W7** · 三条无人引用的 `logs.metric.*Hint` 词条随 D42 一并清 |
| D53 | 票 10 | 槽位上限报 0 表示「没有上限可报」 | （前几轮已裁定） |
| D54 | 票 10 | **排队中**运行的开始时刻在契约里是 Go 的零值时刻 | （前几轮已裁定） |
| D55 | 票 10 | 实况区的定序里没有「手动优先」 | （前几轮已裁定） |
| D56 | 票 10 | 实况汇总在前端也算了一遍 | 记录 · 已被后续票消化 |
| D57 | 票 11 | 「全部暂停」期间**新发起**的运行也进排队 | 记录 · 已知情，无待办 |
| D58 | 票 11 | 控制端点改按**运行 id** 寻址，重试与清除仍按**任务键** | 工作 **W1** · 重试改按任务 id 寻址，随它删掉 `runs.task_key` 一路 |
| D59 | 票 11 | **合并**丢掉的是后来那次的声明与任务体 | 工作 **W6** · 强制重扫被**合并**时 `force` 静默丢失 |
| D60 | 票 11 | 删库仍按三个**任务键**前缀取消，而不是按作用域筛 | （前几轮已裁定） |
| D61 | 票 11 | 按**任务键**取「最近那一条」时，排队中的那条排在最前 | 并入 **D58**（同一张票：重试改按任务寻址） |
| D62 | 票 12 | 建库首扫的**发起方**记「手动」而不是「串联」 | 记录 · 已知情，无待办 |
| D63 | 票 12 | 闸门撤掉之后，同一个库上真的可能有两次扫描并发 | 记录 · [产品缺口清单](../GAPS.md) |
| D64 | 票 12 | 监听器那两条出口**等运行收尾**才返回 | 记录 · 已知情，无待办 |
| D67 | 票 12 | 资料库清理仍不可取消、不可暂停 | 记录 · [产品缺口清单](../GAPS.md) |
| D65 | 票 12 | 「这个库有没有扫描在跑」看不见**排队中**的扫描运行 | 记录 · 已知情，无待办 |
| D66 | 票 12 | 运行量的第四个来源：监听器派生的清理（给票 18 的量级备注） | （前几轮已裁定） |
| D68 | 票 18 | 清理运行的**发起方**记「串联」而不是「定时」 | **裁定 A**：维持**串联**，与规格一致。closed |
| D69 | 票 18 | 清理运行不可暂停、不可取消，也不在重试注册表里 | 记录 · 已知情，无待办 |
| D70 | 票 18 | 清理在**启动时**先跑一次，而不是等满一天 | 记录 · 已知情，无待办 |
| D71 | 票 18 | **采样**那一层不看运行死活，跑了 7 天以上的运行会丢掉早期曲线 | 记录 · [产品缺口清单](../GAPS.md) |
| D72 | 票 18 | 清理运行要不要进票 15 的**续跑**白名单 | （前几轮已裁定） |
| D73 | 票 18 | 「阈值≤0 即这一层不裁剪」这条纯判据定死在 SQL 谓词里 | 记录 · 已知情，无待办 |
| D74 | 票 18 | 保留不提供「关掉」的出口，要留得久就把数字调大 | 记录 · 已知情，无待办 |
| D75 | 票 14 | **退避**曲线的起点是一个不可配的常数，三个可配数只决定形状 | 记录 · [产品缺口清单](../GAPS.md) |
| D76 | 票 14 | 重新**启用**一个任务不复位连败与**退避** | 记录 · 已知情，无待办 |
| D77 | 票 14 | **停发**只挡「定时」与「监听」，串联与恢复不受约束 | 记录 · 已知情，无待办 |
| D78 | 票 14 | 界面上**退避中**不标红，标红留给不会自己恢复的那两条 | 记录 · 已知情，无待办 |
| D79 | 票 14 | 监听派生的扫描被**停发**挡下时**不重排**，与被**合并**那条不同 | 记录 · 已知情，无待办 |
| D80 | 票 14 | 禁用开关按**任务 id** 寻址，与按**任务键**寻址的重试端点占同一段路径 | 并入 **D58**（同一张票：重试改按任务寻址） |
| D81 | 票 14 | **退避**三个数的归一化分在三处，溢出那一道落在 api 层 | 记录 · 已知情，无待办 |
| D82 | 票 14 | 禁用那两个端点自成一个生成器，没有与 `runControlHandler` 合并 | 并入 **D58**（同一张票：重试改按任务寻址） |
| D83 | 票 15 | 重建索引不在**可续跑**白名单里 | 记录 · [产品缺口清单](../GAPS.md) |
| D84 | 票 15 | **恢复**不经**停发**闸门：退避中或被人工禁用的任务照样续跑一次 | 记录 · 已知情，无待办 |
| D85 | 票 15 | 同一个任务的两条中断运行只恢复一条 | 记录 · 已知情，无待办 |
| D86 | 票 15 | 清理缩略图顺带进了重试注册表 | 记录 · 已知情，无待办 |
| D87 | 票 15 | 封面生成暂时不在白名单里，它随票 13 落地时补一行 | （前几轮已裁定） |
| D88 | 票 15 | 续跑开关是配置里第一个 `*bool` | 记录 · 已知情，无待办 |
| D89 | 票 15 | **发起方**成了重启函数的参数，包括不可续跑的那几个 | 记录 · 已知情，无待办 |
| D90 | 票 15 | **已暂停**与**取消中**的运行不进续跑，只有运行中与排队中进 | 记录 · 已改 `spec.md` 状态机：续跑只认运行中与排队中 |
| D91 | 票 15 | 规格举的「可重试但不可续跑」两例，今天其实连可重试都不是 | 记录 · 已改 `CONTEXT.md` **可续跑**词条：举例换成清理资料库与刮削 |
| D92 | 票 15 | 「续跑默认开」在四层各写了一遍 | 记录 · 已知情，无待办 |
| D93 | 票 15 | `taskDispatchKey` 与 `task.ResumeKey` 是同一个（类型，变体）簇的两份 | 记录 · 已知情，无待办 |
| D94 | 票 19 | 实况帧的取数没有世代号，慢网下会被自己的旧响应盖回去 | 记录 · [产品缺口清单](../GAPS.md) |
| D95 | 票 19 | 侧栏任务气泡不跟着重拉，丢帧之后它自己纠正不了 | 记录 · [产品缺口清单](../GAPS.md) |
| D96 | 票 19 | 缺口的判据换成了「链上的 prev」，不是票据字面的「序号不连续」 | 记录 · 已改 `spec.md` 关键决定 12：缺口按 `prev` 回指判 |
| D97 | 票 19 | 两个推送事件名在 Go 与 TS 各写了一份 | 记录 · 已知情，无待办 |
| D98 | 票 19 | 实况汇总数在哪一层：`Summarize([]Run)` 还是领域侧的 `Live(ctx)` | 记录 · 已知情，无待办 |
| D99 | 票 13 | 一批封面在派发前整批攒在内存里 | 记录 · [产品缺口清单](../GAPS.md) |
| D100 | 票 13 | 封面运行的续跑与重试改为「把还缺封面的书重排一遍」 | 记录 · 已知情，无待办 |
| D101 | 票 13 | 封面运行的发起口在扫描的入库协程上**同步**被调用 | 记录 · 已知情，无待办 |
| D102 | 票 13 | 扫描指标里去掉了 `generated_covers` 与 `thumbnail_write_ms` | 记录 · 已知情，无待办 |
| D103 | 票 13 | 扫描期间来的第二批封面归**下一条**封面运行，不并进正在跑的那条 | 记录 · 已知情，无待办 |
| D104 | 票 13 | 没有发起方接手时扫描器自己把那一批跑完，而这条路在生产不可达 | 记录 · 已知情，无待办 |
| D105 | 票 13 | 批与运行的配对表开在**发起侧**，而票据写的是「接收侧无侧表」 | 记录 · 已知情，无待办 |
| D106 | 票 13 | 扫描进度的节流水位抽成了与封面共用的 `reportThrottle` | 记录 · 已知情，无待办 |
| D107 | 票 16 | `runs.task_key` 撤不掉：重试仍按它寻址 | 并入 **D58**（同一张票：重试改按任务寻址） |
| D108 | 票 16 | 控制那一类事件由引擎发，不经**运行句柄** | 记录 · 已知情，无待办 |
| D109 | 票 16 | 阶段事件由「这一帧的阶段与上一条不同」触发，不另开一条上报通道 | 记录 · 已知情，无待办 |
| D110 | 票 16 | 溢出计数留在进程内存里，收尾时才落一条事件 | 记录 · [产品缺口清单](../GAPS.md) |
| D111 | 票 16 | 扫描的整批入库失败落成**告警**，不落成 N 条条目失败 | 记录 · 已知情，无待办 |
| D112 | 票 16 | 事件读取面一次最多 2000 条，截的是靠前那一段 | 记录 · 已知情，无待办 |
| D113 | 票 16 | 「原始日志」那条过滤换成运行标识，`task_key` 谓词留着没人用 | 并入 **D58**（同一张票：重试改按任务寻址） |
| D114 | 票 16 | **合并**只在第一次落一条事件，次数由运行行上的计数回答 | 记录 · 已知情，无待办 |
| D115 | 票 16 | 条目失败的上限**先判上限、后判活动态** | 记录 · 已知情，无待办 |
| D116 | 票 16 | 事件流认「哪张卡片」而不是「哪条运行」 | 记录 · 已知情，无待办 |
| D117 | 票 16 | `npm run test` 有一条与本批无关的既有抖动 | （前几轮已裁定） |
| D118 | 票 17 | 取点的**节拍**与它的**水位**各读一次时钟，偶尔会丢一个点 | 记录 · 已知情，无待办 |
| D119 | 票 17 | 曲线上的吞吐与运行卡片上的速率是**两个数** | **裁定 B**：采样那一格改名 `throughput_per_minute`，趁契约未发布 → 工作 W10 |
| D120 | 票 17 | 曲线撞上取回上限时截的是**最近**一段，早期看不到 | 记录 · 已知情，无待办 |
| D121 | 票 17 | 暂停段与停滞段在曲线上长得**一模一样** | 记录 · [产品缺口清单](../GAPS.md) |
| D122 | 票 17 | 「查看日志」这个按钮打开的已经不只是日志了 | **裁定 A**：维持「查看日志」，代价（代码名与界面名对不上）记在案。closed |
| D123 | 票 17 | 「隔多远算漏了点」两侧各有一份判据 | 记录 · 已知情，无待办 |

## Open

<!-- 本轮范围内一条不剩。新条目从这里往下追加。 -->
