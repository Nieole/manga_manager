# 01 — 系列详情页的提案加载改用批量查询

**What to build:** 打开一个有多条待裁决**提案**的**系列**时，页面不再随提案条数发起额外查询。收件箱早就用批量查询取字段行，系列详情页这条路径仍在提案列表上逐条取——同一个领域问题今天有两套实现，本票把它收成一套。

这是后续搬迁的 prefactor：先就地修掉，票 05 的迁移就是纯移动，不必一边搬一边改查询。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 系列详情页的提案加载改用批量字段行查询，与收件箱同一条通路
- [x] 逐条版字段行查询在该路径上不再有调用点
- [x] 响应内容一字不变：提案顺序、字段顺序、锁定标志、来源沿革均与改动前一致
- [x] 有一条计数替身用例守住「字段行查询只被调一次」，照 `prepare_transfer_test.go` 里那种计数替身的写法，不造行为替身
- [x] `GOCACHE="$(pwd)/.gocache" GOTMPDIR="$(pwd)/.tmp" go test ./...` 全绿
