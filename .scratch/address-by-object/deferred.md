# 挂账清单 · address-by-object

跑票过程中冒出的**非阻塞岔口**。流程不为它们停；由用户挑时机逐条处理（走 `/settle`）。
它不是[产品缺口清单](../GAPS.md)——那份装产品缺口、永不清空；这份装实现期的岔口、一批批清空。

## 一条条目要过三关

1. **另一条路站得住。** 写得出替代方案，且不是稻草人。只有一条路可走就不是岔口，那属于提交信息。
2. **翻案不出这张票。** 用户日后改选另一边，代价是一次编辑，不是拆掉已落地的活。翻案要伸出这张票——那是**停线**，不是条目。
3. **你能推荐一个。** 走过一遍才有的上下文，交出去时要带着结论。

## 停线只有三种

1. 照做会写下错的产物，且撤销要伸出这张票（典型：票的指令与上游仍然生效的决定相抵触）。
2. 动作不可逆，且不止一个方向站得住（典型：删或改一个被到处引用的标识、字段、文件）。
3. 前提不存在，票根本走不下去（典型：「在 X 上建 Y」而没有 X）。

其余全部：记一条、走最站得住的那条路、继续。**票里的数与你量到的对不上、验收条读着含糊、
票没预料到的第三种情形、顺路撞见的无关缺陷**——都属于这一类。

## 编号

本文件一条序列，`D<n>`。并行派工时由工头按票**分块**预留，谁都不用问号。
条目标题自带来处（`票 NN`），因为 id 里不带票号。

## 一条条目长什么样

```markdown
## D<n> · 票 NN · <一句话：岔口是什么>

- **问：** 岔在哪里，为什么票答不了。带上可跳转的位置（路径 + 符号名）。
- **已做：** 走了哪条路。**必填**——合并抽检看的就是这一格。
- **选项：** A …（推荐，因为…）｜ B …
- **不处理会怎样：** 不裁定的话，留在代码里的是什么。
- **归谁裁：** 用户 / 票 NN / 下一次结算
- **状态：** open
```

## 已结算索引

> **2026-09-10 结算完毕。** 范围 D1–D3、D11–D15、D21–D27、D31–D39、D41–D45、D51–D53、D61、D63，共 **34** 条，一条不剩。
> 划定之后新记的条目归下一轮。本轮分布：记录 27 ·
> 岔口 2 · 工作 5。
> **每票条目数 34 ÷ 7 = 4.9**（对着 1 读，见 [SETTLE-LOG](../SETTLE-LOG.md)）。
> 正文归 git，这里一行一条。

| id | 来处 | 一句话 | 去处 |
|---|---|---|---|
| D1 | 票 01 | 显示名进匹配串后撞上单边折叠：SQLite `LOWER` 只折 ASCII、Go 侧折整个 Unicode | 记录 · [产品缺口清单](../GAPS.md) |
| D2 | 票 01 | 显示名是落库那一刻的快照，库改名后历史运行只认旧名 | 记录 · [产品缺口清单](../GAPS.md) |
| D3 | 票 01 | 顺路撞见的偶发红用例 | 记录 · 并入 **D63** |
| D11 | 票 02 | 按**任务键**取运行的两个函数搬进用例文件 | 记录 · 已由 **D21** 接手并删除 |
| D12 | 票 02 | 「没有可重试的运行」与「任务不存在」分开答 | 记录 · 已知情，无待办 |
| D13 | 票 02 | 只有活动/排队运行的任务，重试 202 → 404 | **岔口** · 用户裁定：**维持 404** |
| D14 | 票 02 | ADR 0007 的「尚未实施」状态行没人改 | 记录 · 票 03 已改成「已实施（2026-09-09）」 |
| D15 | 票 02 | 端点路径变更要不要记 CHANGELOG | 记录 · 已记；v1.6.3 改标时一并落定 |
| D21 | 票 03 | D11 的裁决：删掉那两个函数，用例把手换成启动登记表 | 记录 · 已知情，无待办 |
| D22 | 票 03 | 删 `task.Run.Key`，ctx 装饰函数改收 `RunSpec` | 记录 · 已知情，无待办 |
| D23 | 票 03 | 前端两个按键认领推送的读者，ADR 与票面都没列 | 记录 · 已改判 `task_id` / `session_id` |
| D24 | 票 03 | 票 01 漏改内存 Store 替身，匹配串还判在键上 | 记录 · 已顺手补上 |
| D25 | 票 03 | 外部库两条与 ComicInfo 一条端点仍返回 `task_key` | **工作** · 无家可归 → `/grill-with-docs` |
| D26 | 票 03 | 用例按身份取数后，外部库分不出同一个库的两次会话 | 记录 · 边界已写进登记表 doc |
| D27 | 票 03 | 侧边栏深链上填不出值的死参数 `?task=` | 记录 · 已删，Ops 页从没读过它 |
| D31 | 票 04 | 改名扫到把旧类型名逐字拼进自己名字的那几个函数 | 记录 · 口径已立，D41 沿用 |
| D32 | 票 04 | 状态文案的 i18n 键实为九条，第九条是「全部状态」 | 记录 · 已知情，无待办 |
| D33 | 票 04 | 浏览器自定义事件取哪个名 | 记录 · 取 `manga-manager:run-snapshot`，对齐 SSE 前缀 |
| D34 | 票 04 | 覆盖变体那条事件没有任何派发方 | **工作** · 无家可归 → `/grill-with-docs` |
| D35 | 票 04 | 改名扫到文档：活文档跟改、`docs/changelog/` 不动 | 记录 · 口径已立，沿用上一次同类改名的先例 |
| D36 | 票 04 | 被推翻的关键决定 16 只写进了提交信息 | 记录 · 已就地标注 `task-run-model/spec.md` |
| D37 | 票 04 | `RunSnapshot` 撞上领域侧的同名方法 | **岔口** · 用户裁定：改 `task.(*Engine).SnapshotOf`，本轮已落地 |
| D38 | 票 04 | 前端载荷名与接入函数名落在 D31 口径之外 | 记录 · 已一并改，别名撤掉 |
| D39 | 票 04 | 改名撑歪 tag 对齐，而门禁清单里没有 `gofmt` | 记录 · 已补进 `AGENTS.md` |
| D41 | 票 05 | 另有两个函数把 `TaskLimits` 逐字拼在名字里 | 记录 · 沿用 D31 口径 |
| D42 | 票 05 | 改名改到「符号名 + 它自己那句 doc」为止 | 记录 · 口径已立 |
| D43 | 票 05 | `TaskRuntime` 是零引用的死导出类型，名字也按旧概念取 | **工作** · 无家可归 → `/grill-with-docs` |
| D44 | 票 05 | 前端组件层仍按旧概念命名，本批十票没一张覆盖 | **工作** · 无家可归 → `/grill-with-docs` |
| D45 | 票 05 | `golangci-lint` 缓存跨工作树回放已删树的路径 | 记录 · 已补进 `AGENTS.md` |
| D51 | 票 06 | 三个不是任务体的函数也带着同一个句柄形参 | 记录 · 口径已立 |
| D52 | 票 06 | 用例外层捕获变量已经占着 `handle` 这个名字 | 记录 · 已知情，无待办 |
| D53 | 票 06 | 新工作树 `go build` 卡在被忽略的 `web/dist` | 记录 · 开树流程已补拷贝步骤，并写进 `AGENTS.md` |
| D61 | 票 07 | 采样表那一列跟着改名，补一条幂等迁移 | 记录 · 已随 v1.6.3 发布 |
| D63 | 票 07 | `TestStorageIOCoverRateComesFromTheCoverRun` 负载敏感假失败 | **工作** · 无家可归 → `/grill-with-docs` |

## Open

<!-- 本轮范围内一条不剩。新条目从这里往下追加。 -->
