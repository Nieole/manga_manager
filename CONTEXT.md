# manga_manager

自托管的漫画/轻小说库管理服务：扫描本地归档、刮削元数据、供阅读与同步。本文件只记录本项目特有的领域词汇，不记录实现方式。

## 藏书

**资料库 (Library)**:
用户配置的一个本地目录树，是扫描与权限的最外层归属单位。
_Avoid_: 书库、仓库、collection

**系列 (Series)**:
同一部作品的集合，是元数据（作者、标签、简介、封面）的挂载点。
_Avoid_: 作品、title、manga

**书 (Book)**:
磁盘上的一个归档文件，属于且只属于一个系列。用户视角下通常是「一卷」或「一话」。
_Avoid_: 卷、章节、file、item

**外部库 (External Library)**:
不在任何资料库目录内的一个绝对路径，用于把书传输进来或传输出去。它不参与扫描，只在传输会话期间被读取。
_Avoid_: 外部目录、远端库

## 编排

**合集 (Collection)**:
人工挑选成员的一组系列。
_Avoid_: 书单、分组

**智能书架 (Smart Collection)**:
成员由过滤条件推导而来的合集；成员随藏书变化而变化，不被人工编辑。
_Avoid_: 动态合集、智能合集

**作品群 (Franchise)**:
由系列之间的关系（续作、前作、外传等）连通而成的一组系列。它是推导出来的，不由人工建立。
_Avoid_: 系列组、宇宙、IP

**提案 (Proposal)**:
刮削或 AI 产出的一条待人工裁决的元数据变更建议。被接受才会写入系列，写的是**替换**而不是补充——
标签与作者这类集合字段整体换成提案值，提案里没有的会被删掉，用户要保住的内容靠锁定字段挡。
AI 分组给出的合集成员建议不是提案——它说的是合集该怎么分，不是某个系列的元数据该改成什么。
_Avoid_: 建议、候选、suggestion、review（表名与路由已拼作 review，那不是第二个概念，新代码仍写提案）

## 后台任务

**任务 (Task)**:
某个目标上的某类后台工作，可被反复发起，例如「1 号资料库的扫描」。它是身份而不是一次执行——
用户口中「我的扫描任务」指的正是它。
_Avoid_: 作业、job

**运行 (Run)**:
一个任务的一次具体执行，有自己的开始与结束、终态、进度与指标。任务中心里的每一条是一次运行；
重试发起的是同一个任务的**新一次运行**，上一次原样留着。
_Avoid_: 执行、尝试、次、attempt、job run

**变体 (Variant)**:
同一类工作在同一个作用域上的第二个身份，例如书哈希重建分「用户在维护页发起的前台重建」与
「资料库扫描收尾串联的低优先级回填」两个变体：跑法不同（批次大小、批间停顿），因此是两个任务
而不是同一个任务的两次运行。

判据不是「跑法不同」——那太宽。**两个变体是两个身份，而两个身份允许同时跑**：立变体等于声明
这两种跑法**同时进行是可以接受的**。哈希重建那两个正是如此（前台重建与低优先级回填共存是特性）。
反例是资料库扫描的「强制」：它跳过增量拦截、重读每个归档，跑法确实不同，但它与普通扫描是
同一件事的两种彻底程度，同一个库上同时扫两遍不是任何人想要的——所以**强制是一次运行的属性，
不是一个身份**。
_Avoid_: 子类型、模式、flavor

**任务键 (Task key)**:
一个任务的可读标识串，由启动点自己拼出来，例如 `scan_library_1`。它**只给日志与人眼辨认用**，
**不是寻址方式**（ADR 0007）：控制动作按**运行**标识寻址，重试与禁用按**任务**标识，
**运行**的对外快照与 `runs` 表上都没有它（外部库与 ComicInfo 那三条端点的响应体里还留着一个
同名字段，那只是「这一步我发起过」的标记，不拿来寻址）。同一个键此刻可以有两条仍会变化的运行——它认得出「哪件事」，认不出「哪一次」。
身份本身是任务那一行，不是这个串，两者也不互相推导。
_Avoid_: 任务 ID、task id

**发起方 (Trigger)**:
一次运行是被什么发起的：手动、定时、监听、串联、恢复。它不改变运行怎么跑，只回答「这活是谁叫来的」——
半夜硬盘在转时，用户要的正是这个答案。
_Avoid_: 来源、触发器、source

**排队中 (Queued)**:
运行已被登记、但还没拿到运行槽位的状态。它既不是**活动态**也不是**终态**：不占槽位，却仍会开跑。
_Avoid_: 等待中、pending、waiting

**运行槽位 (Run slot)**:
全局同时运行数的上限，界面上写成「槽位 2/2」。它是**一个数字**而不是按磁盘卷分开的几组配额——
后者更贴近真实瓶颈，一个数字更好解释（ADR 0005 的知情取舍）。占用它的只有**活动态**的运行，
超出的留在**排队中**等放行；上限可在设置里改，改动对新的放行生效，不打断已经在跑的。
_Avoid_: 并发数、worker、令牌（**存储令牌**是另一回事）

**合并 (Coalesce)**:
同一个任务已经有一次排队中的运行时，再次发起不新建运行，而是并进那一条并记一次数。
守护扫描与监听扫描要的只是「确保扫过」，并进去与各排一条的效果相同，而后者会堆成一串同样的运行。

**「效果相同」是前提，不是保证。** 两次发起的声明不同（例如一次带强制、一次不带）时前提不成立：
被并掉的是**后来那次的声明与任务体**，跑的仍是先排上的那一份。
_Avoid_: 去重、折叠、debounce

**作用域 (Scope)**:
一个任务作用的对象层级：系统、资料库或系列。决定任务在界面上挂在哪里。
_Avoid_: 范围、层级、target

**活动态 (Active)**:
一次运行仍占着运行槽位的状态，含运行中、已暂停、取消中三种。
_Avoid_: 进行中、in flight

**取消中 (Cancelling)**:
取消已被请求、但任务体尚未收尾的状态。它属于活动态——此时同一个任务不得再次发起。
_Avoid_: 正在停止、stopping

**终态 (Terminal state)**:
一次运行不会再变化的最终状态，共四种：完成、已取消、失败、中断。**排队中**的运行被取消也进这里。
_Avoid_: 结束态、完成态（「完成」专指其中一种，不要用它泛指全部四种）

**完成 (Completed)**:
任务体正常跑完的终态。仅指这一种，不涵盖已取消与失败。
_Avoid_: 结束、finish、done

**中断 (Interrupted)**:
服务重启时，仍处于活动态的运行被转入的终态。它不是失败——任务体没有出错，只是没跑完，因此可重试。
_Avoid_: 异常终止、崩溃、失败

**阶段 (Phase)**:
一次运行当前所处的工序，例如清理缓存、读取元数据、生成封面。阶段变化是用户正在等的语义变化，与进度计数相互独立——一个阶段内计数可以推进很多次，也可以一次都不推进。
_Avoid_: 步骤、状态、step、stage

**计数推进 (Advance)**:
一次运行已处理条目数的前进。它只回答「做完了多少」，不回答「在做什么」——后者是阶段。
_Avoid_: 进度更新、progress

**运行事件 (Run event)**:
一次运行途中值得事后回看的一个瞬间：阶段切换、某个条目失败、一次告警、一次控制动作。
它记的是**发生过什么**，与回答「此刻怎么样」的快照是两回事。
_Avoid_: 日志、log、记录、条目

**采样 (Sample)**:
按固定间隔对一次运行的计数与吞吐取的一个点，连起来是这次运行的吞吐曲线。点上那个吞吐量的是
**自上一个点以来那一段**，不是这次运行至今的平均速率——平均值在停滞段只会缓慢下滑，
而曲线要答的恰恰是「它此刻还在不在产出」。只对**活动态**的运行取点：**排队中**还没开跑，
**终态**不会再动。它只用来看趋势——计数的事实来源是快照，不是采样点，丢一个点只该让曲线少一段。
_Avoid_: 打点、埋点、metric、时间序列

**实况汇总 (Live summary)**:
任务中心实况区顶上那几个数：占了几个**运行槽位**、几条在跑、几条**排队中**、「全部暂停」的闸门关没关着。
它由后端数好了发过来，浏览器不从自己手上那份运行列表里数——那份列表是推来的帧攒出来的，丢过一帧就少一条，
而少一条数出来的占用不会报错，只会静静少 1。
_Avoid_: 概览、统计、状态栏

**投递链 (Push chain)**:
推给界面的每一帧都带着上一帧的序号，串成一条链。它只回答一个问题：中间掉过东西没有。
序号本身连不连续说明不了什么——被节流吞掉的那些帧照样取了号；接不上链才是漏帧，界面据此整份重拉一次，
而不是停在一个过期的进度上等下一次轮询。
_Avoid_: 序列号、游标、确认、ack

**分层保留 (Tiered retention)**:
运行历史按三个阈值分层裁剪：每个任务留最近若干次**终态**运行、终态运行留若干天（两者取先到者），
**采样**留得比运行本身短——过期之后运行还在，曲线没了。**活动态与排队中的运行永不被它带走**，
这不是策略而是前提。清理本身也是一条运行，每天一次，因此「它什么时候跑的、清了多少」有据可查。
_Avoid_: 过期清理、GC、清表

**暂停闸门 (Pause gate)**:
任务体在各个可中断点上向其询问「现在能继续吗」的东西。用户按下暂停后，任务体会停在下一个可中断点
而不是当场中止，因此暂停不会丢失已完成的工作。「全部暂停」只是把每一条活动运行逐个按下，
不是第二套机制——因此不可暂停的运行不受它影响，而受影响的运行状态如实写作**已暂停**。
它同时关上**运行槽位**的放行：**排队中**的运行还没起任务体，没有闸门可按，拦住放行是唯一
按得住它们的地方，否则按下全部暂停之后队列里的东西照样一条条接上去跑。
_Avoid_: 暂停信号、pause flag

**暂停原因 (Pause reason)**:
一条**已暂停**的运行是被哪一次按下停住的：用户按了它自己的暂停键，还是按了「全部暂停」。
取值是一个封闭枚举，只在暂停期间有话说——恢复、取消与收尾都会把它清掉。它不解释「为什么按不下去」，
那是这条运行有没有可中断点的事，见**暂停闸门**。
_Avoid_: 暂停来由、停止原因、pause cause

**运行句柄 (Run handle)**:
任务体干活所需的全部资格——写**这一次运行**的进度、在可中断点询问**暂停闸门**、发起**磁盘作业**。
任务体拿到它才能做这三件事，而不是谁拼对了任务键谁就能做。
_Avoid_: 任务句柄、进度句柄、回调、reporter

**扫描观察者 (Scan observer)**:
一次扫描把进度与指标交给的对象，由发起扫描的一方交出。每一次会动磁盘的扫描都归属于某一次运行，
因此都有它——守护扫描、watcher 派生的扫描与建库后的首扫也不例外。它只管扫描本身：异步生成的
封面归属另一次运行，那些报文经封面作业自带的**运行句柄**走，不经它。
_Avoid_: 回调、监听器、sink

**退避 (Backoff)**:
一个任务连续失败后，自动发起的间隔被逐次拉长，直到不再自动发起。手动发起一次或成功一次即复位。
它防的是同一个不可达目标被无限重试——那既刷屏，又每次白转一遍盘。它只约束**发起**：
一条已经在跑的运行不会因为退避被打断。
_Avoid_: 限流、冷却、熔断

**停发 (Stalled)**:
一个任务此刻不会被自动发起，以及它为什么不会：**退避**还没到期、连败到了阈值，或者被人工禁用。
它是**任务**的一条属性，不是运行状态，也不是**终态**——「这个任务停发了」与「这次运行失败了」
在界面上要分得开。手动发起始终绕过它。
_Avoid_: 熔断、已停用、暂停

**可续跑 (Resumable)**:
一个任务类型在服务重启后可以由系统自动重新排队，不必等人裁决。它比可重试严格：可重试只要求
「能再发起一次」，可续跑还要求「在无人看着时再发起一次也不会造成意外」——因此**清理资料库**
与**刮削**可重试但不可续跑。改磁盘内容或花钱的类型（外部库扫描与传输、ComicInfo 回写）连
重启函数都没有，两样都不是。
_Avoid_: 可恢复、可自动重试、resumable

**重启函数 (Relauncher)**:
按一次已进入终态的运行的作用域与参数，为同一个任务发起新一次运行的方式。一个任务类型是否可重试，
等价于它有没有重启函数。
_Avoid_: 重试器、retry handler

**存储令牌 (Storage token)**:
任务体在动磁盘之前必须先取得的许可，用于把并发 IO 限制在卷的承受范围内，并让阅读请求优先于后台作业。
_Avoid_: 信号量、限流器、lease

**磁盘作业 (Disk work)**:
一次必须先经**暂停闸门**放行、再取得**存储令牌**才能开始的磁盘操作，是限流与暂停的最小单位。
能并发几个由作业的种类决定，与发起它的是哪一处无关。前台的阅读取页不是磁盘作业——它不过暂停闸门，
且优先于一切后台作业。
_Avoid_: IO 任务、磁盘操作、工作单元

## 同步与协议

**KOReader 设备账号 (KOReader account)**:
KOReader 同步协议里的一个身份，由设备自助注册产生，须归属到本服务的某个用户后才承载阅读进度。
_Avoid_: 设备、client、device account

**指纹 (Fingerprint)**:
从书的内容或路径导出的稳定标识，用于在文件改名或跨设备时把阅读进度重新对上同一本书。
_Avoid_: 哈希、hash、签名

**未匹配 (Unmatched)**:
KOReader 报上来的一条阅读进度，其指纹在本服务的藏书里找不到对应的书。
_Avoid_: 孤儿进度、orphan
