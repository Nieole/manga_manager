# manga_manager

自托管的漫画/轻小说库管理服务：扫描本地归档、刮削元数据、供阅读与同步。本文件只记录本项目特有的领域词汇，不记录实现方式。

## 藏书

**资料库 (Library)**:
用户配置的一个本地目录树，是扫描与权限的最外层归属单位。
_Avoid_: 书库、仓库、collection

**系列 (Series)**:
同一部作品的集合，是元数据（作者、标签、简介、封面）的挂载点。
_Avoid_: 作品、title、manga

**书 (Book)**:
磁盘上的一个归档文件，属于且只属于一个系列。用户视角下通常是「一卷」或「一话」。
_Avoid_: 卷、章节、file、item

**外部库 (External Library)**:
不在任何资料库目录内的一个绝对路径，用于把书传输进来或传输出去。它不参与扫描，只在传输会话期间被读取。
_Avoid_: 外部目录、远端库

## 编排

**合集 (Collection)**:
人工挑选成员的一组系列。
_Avoid_: 书单、分组

**智能书架 (Smart Collection)**:
成员由过滤条件推导而来的合集；成员随藏书变化而变化，不被人工编辑。
_Avoid_: 动态合集、智能合集

**作品群 (Franchise)**:
由系列之间的关系（续作、前作、外传等）连通而成的一组系列。它是推导出来的，不由人工建立。
_Avoid_: 系列组、宇宙、IP

**提案 (Proposal)**:
刮削或 AI 产出的一条待人工裁决的元数据变更建议。被接受才会写入系列。
_Avoid_: 建议、候选、suggestion

## 后台任务

**任务 (Task)**:
一次长时间运行的后台作业，用户可在任务中心观察其进度、暂停、取消与重试。
_Avoid_: 作业、job

**任务键 (Task key)**:
一个任务的身份。同一时刻同一个任务键只能有一个活动任务——这是「同类任务已在运行」这条拒绝的依据。
_Avoid_: 任务 ID、task id

**作用域 (Scope)**:
一个任务作用的对象层级：系统、资料库或系列。决定任务在界面上挂在哪里。
_Avoid_: 范围、层级、target

**活动态 (Active)**:
任务仍占着运行槽位的状态，含运行中、已暂停、取消中三种。
_Avoid_: 进行中、in flight

**取消中 (Cancelling)**:
取消已被请求、但任务体尚未收尾的状态。它属于活动态——此时同一个任务键不得再次启动。
_Avoid_: 正在停止、stopping

**终态 (Terminal state)**:
任务不再占用运行槽位后的最终状态，共四种：完成、已取消、失败、中断。
_Avoid_: 结束态、完成态（「完成」专指其中一种，不要用它泛指全部四种）

**完成 (Completed)**:
任务体正常跑完的终态。仅指这一种，不涵盖已取消与失败。
_Avoid_: 结束、finish、done

**中断 (Interrupted)**:
服务重启时，仍处于活动态的任务被转入的终态。它不是失败——任务体没有出错，只是没跑完，因此可重试。
_Avoid_: 异常终止、崩溃、失败

**阶段 (Phase)**:
任务当前所处的工序，例如清理缓存、读取元数据、生成封面。阶段变化是用户正在等的语义变化，与进度计数相互独立——一个阶段内计数可以推进很多次，也可以一次都不推进。
_Avoid_: 步骤、状态、step、stage

**计数推进 (Advance)**:
任务已处理条目数的前进。它只回答「做完了多少」，不回答「在做什么」——后者是阶段。
_Avoid_: 进度更新、progress

**暂停闸门 (Pause gate)**:
任务体在各个可中断点上向其询问「现在能继续吗」的东西。用户按下暂停后，任务体会停在下一个可中断点而不是当场中止，因此暂停不会丢失已完成的工作。
_Avoid_: 暂停信号、pause flag

**进度句柄 (Progress handle)**:
写某个任务进度的资格。任务体拿到它才能写，而不是谁拼对了任务键谁就能写。
_Avoid_: 进度回调、reporter

**扫描观察者 (Scan observer)**:
一次扫描把进度与指标交给的对象，由发起扫描的一方交出。守护扫描、watcher 派生的扫描与建库后的首扫都不属于任何任务，因此没有它。它活得比那次扫描调用久——封面是异步生成的。
_Avoid_: 回调、监听器、sink

**重启函数 (Relauncher)**:
把一个已进入终态的任务按其原有作用域与参数重新发起的方式。一个任务类型是否可重试，等价于它有没有重启函数。
_Avoid_: 重试器、retry handler

**存储令牌 (Storage token)**:
任务体在动磁盘之前必须先取得的许可，用于把并发 IO 限制在卷的承受范围内，并让阅读请求优先于后台作业。
_Avoid_: 信号量、限流器、lease

## 同步与协议

**KOReader 设备账号 (KOReader account)**:
KOReader 同步协议里的一个身份，由设备自助注册产生，须归属到本服务的某个用户后才承载阅读进度。
_Avoid_: 设备、client、device account

**指纹 (Fingerprint)**:
从书的内容或路径导出的稳定标识，用于在文件改名或跨设备时把阅读进度重新对上同一本书。
_Avoid_: 哈希、hash、签名

**未匹配 (Unmatched)**:
KOReader 报上来的一条阅读进度，其指纹在本服务的藏书里找不到对应的书。
_Avoid_: 孤儿进度、orphan
