// Package task 是任务子域的领域层：**任务**是身份（类型、**作用域**、作用域 id、**变体**四项唯一
// 确定），**运行**是它的一次执行。状态机、四条**终态**的裁决、运行槽位与**排队中**都在这里。
//
// 它不知道 SQL、也不知道 HTTP：落盘只经本包声明的 Store 端口，投递只经注入的两个函数（一帧运行
// 快照、一帧**实况汇总**），因此状态机、槽位与终态裁决可以在纯内存下测（ADR 0006）。
//
// 边界：**任务键**怎么拼、i18n 文案码与 HTTP 语义属于 api；**磁盘作业**的限流规则属于 diskwork
// 与 storageio；**暂停闸门**的实现属于 taskcontrol；表结构与查询属于 taskstore。
package task
