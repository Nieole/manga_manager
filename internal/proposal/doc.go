// Package proposal 承载**元数据提案**子域：把一次刮削结果折成待人工裁决的字段提案并入队，
// 以及把一条提案应用进系列或拒绝掉。入队前按系列的当前**锁定字段**过滤，并与该系列的
// 待裁决提案、最近的拒绝记录逐字去重；应用时再按**当时**的锁定集过滤一次。
//
// 每一种结局都是结果值而不是错误——已入队、复用既有提案、无差异、差异全被锁、此前已拒绝
// 由 QueueStatus 分类，全部应用、部分应用、全被锁跳过、无变更、被抢先由 ApplyStatus 分类，
// error 只留给故障。被抢先无论是前置检查发现的还是事务内 CAS 撞上的，都是同一个分类。
//
// 边界：HTTP 路由与编解码、字段标签与面向用户的提示文案属于 internal/api，SQL 属于
// internal/database，本包内部只认字段名。AI 分组产出的合集成员建议不是提案，不在本包内。
package proposal
