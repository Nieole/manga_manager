# Changelog

本文件记录项目各版本的功能新增、改动与修复。

---

### 🚧 未发布（扫描 cbr/rar 藏书不再把每本整卷解压一遍）

> 扫描一个 cbr/rar 藏书时，默认档位会为每本变化的书探测一次 `ComicInfo.xml`，而这次探测顺带把整卷
> 图片页全部解压了出来——扫描器紧接着就关掉归档，这份缓存一次都用不上。本该只读几十 KB 的条目头，
> 实际把整个归档从盘上读了出来：一个 24 MB / 60 页的 CBR，未命中的探测耗时 93.4ms、解压并缓存 60 个
> 条目共 24,576,000 字节，修复后是 372.8µs、0 个条目 / 0 字节；命中的探测从 9.9ms、7 个条目 /
> 2,457,639 字节降到 174.8µs、1 个条目 / 39 字节。机械盘与外置盘上 5000 本 CBR 的库，一次扫描因此
> 少读上百 GB，内存侧也不再为每个归档撑起最大 64 MiB 的会话缓存（还要乘以并发 worker 数）。阅读
> 路径的预取与缓存不受影响，顺序读全 60 页仍是 92.6ms → 97.3ms 的同一量级。数据库结构与线上契约
> 均未改动，升级无需额外步骤。

#### 修复
- **元数据探测与阅读取页各走各的扫描。** rar 是前向只读流，两者此前共用同一个前滚函数：它为顺序
  阅读预取途经的图片页，于是只要目的是探测元数据，整卷页字节也照解不误——注释写着要避免的正是这件
  事，代码却在做。现在探测单开一个临时 reader 逐条目头前进，只解压宽松匹配命中的那一个条目，途经
  条目由 `rr.Next()` 零解压跳过；前滚预取只服务 `ReadPage`，顺序阅读的滑动窗口缓存一如既往。
- **探测元数据不再把正在阅读的会话游标打回开头。** 探测此前无条件从头重开归档，读者的下一页因此
  要重新前滚一遍。临时 reader 不碰会话游标，边读边刮元数据不再拖慢翻页。

---

### 🚧 未发布（带搜索词的资料库链接刷新后不再被弹回第 1 页）

> 在资料库搜索框输入关键词、翻到第 3 页，按 F5 或把地址栏链接分享给别人打开：页面先正确加载第 3
> 页，约 300ms 后自己跳回第 1 页并重发一次请求，看起来像链接坏了。同一处守卫在相反方向也会出错：
> 某个资料库在本机的第一次访问（浏览器禁用或隔离本地存储时则是每次访问），翻到第 3 页后第一次改
> 排序、每页数量或任一筛选，页码不回到第 1 页，且旧排序下算出的游标被带进新查询，那一页显示的是
> 错位、跳过的结果。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **页码与游标的重置只认用户操作，不再靠数水合轮数来避让。** 守卫此前是一个「跳过水合那一轮」的
  开关，可水合到达页面的轮数并不固定：带 `q` 的深链要两轮（先是筛选项，300ms 后才轮到防抖出来的
  关键词），首访且本地无存档时则一轮都不改。挡少了就把深链里的页码冲回第 1 页，挡多了就白吃掉用户
  的第一次操作。现在判据落在状态自己身上——`useLibraryFilters` 只在公开 setter 被调用时记一笔用户
  改动，`useResetPageOnFilterChange` 只认这一笔，水合分几轮到达都与它无关。
- **反向的两条行为保持不变。** 深链 `?page=3` 仍停在第 3 页；从 A 库切到 B 库仍恢复 B 库存档里的
  页码，且切库之后第一次改筛选照样回到第 1 页并清空游标。

---

### 🚧 未发布（编辑元数据时，后台任务完成不再冲掉没保存的输入）

> 打开系列元数据编辑弹窗，改了简介、加了标签还没点保存，此时任一后台任务完成——扫描、刮削、封面
> 重建都算——编辑框里的内容就被静默换回服务端版本。弹窗还开着，用户不一定立刻发现，回头点保存，
> 存下去的是服务端那份，用户在编辑期间点过的字段锁也一并被抹掉。数据库结构与线上契约均未改动，
> 升级无需额外步骤；已经被冲掉的输入没有留存副本，需要重新填一遍。

#### 修复
- **表单重置同时看「是否在编辑」与「是否换了系列」。** 任务的进度与完成事件会推动 `refreshTrigger`，
  系列上下文随之静默重取，`series`/`tags`/`authors`/`links` 换成新引用（内容可以一模一样），表单
  重置 effect 就跟着跑，把用户正在写的内容盖掉。现在编辑态下只有换了系列才重置：同一系列的后台
  刷新不再动编辑框，非编辑态仍照旧跟随服务端最新值。
- **编辑期间切到别的系列，表单跟着换成新系列的值。** 「表单里这份内容属于哪个系列」记在 ref 里（与
  `useSeriesContext` 判断切换同一套写法），与新到达的 `series.id` 不符即重置——不会因为「正在编辑」
  就把上一个系列的简介与标签留在框里，跟着被保存到新系列头上。

---

### 🚧 未发布（快速切换系列时，详情页不再挂着上一个系列的内容）

> 在关系图、合集或作品群里连点两个系列，第一个系列的 `/context` 还没回来就跳到了第二个：地址栏
> 是系列 B，简介、书列表、标签与提案却全是系列 A 的——A 的响应晚到一步，把 B 的内容整个盖掉了。
> 页面上还没有任何加载指示，用户不知道自己看的是上一个系列，在这一页上做批量已读，进度就写到了
> A 的书上——这是真的写错数据，且从界面上看不出来。切系列瞬间点保存也会把 A 的 title/summary/tags
> 打到 `PUT /api/series/info/{B 的 id}`。数据库结构与线上契约均未改动，升级无需额外步骤；已经被
> 写错的阅读进度与元数据没有留存副本，需要人工改回。

#### 修复
- **系列上下文按世代号收口，迟到的响应不再写页面。** `/context` 在发起时取一个世代号，写内容、写
  错误、清加载态三条路径都只认最新一次请求（与 `useLibrarySeries`、`useSeriesScraping` 同一套写
  法）。连点系列时 A 的响应到得再晚，也只会被丢弃。
- **切系列时先清空上一个系列的内容，并显示加载态。** 加载态此前由 `!series && books.length === 0`
  推断，而 effect 里读到的是上一轮渲染的闭包值——切系列时它还是上一个系列的非空值，于是加载态直接
  为 false。现在「当前内容属于哪个系列」记在 ref 里，与新的 `seriesId` 不符即判为切换：清空内容、
  显示加载指示；同一系列的刷新仍然静默，不闪加载态。
- **切系列窗口内的保存不再落到新系列 id 上。** 保存以 `series` 非空为前提，而 `series` 在切换瞬间
  已被清空，直到新系列的上下文到达才重新有值，那段「id 是 B、内容是 A」的窗口就此不存在。

---

### 🚧 未发布（保存的智能视图不再丢掉高级筛选条件）

> 在资料库里设好「未读 + 评分≥8 + 最近 30 天加入」，存成智能视图，之后应用它——这三个条件被静默
> 清空，只剩标签、作者、状态、首字母与排序，结果集比保存时大得多，视图看上去像是坏了。保存按钮的
> 启用条件本来就把高级筛选算在内，于是只设了高级筛选、没设标签作者的那一次，存出来的是一个条件
> 完全为空的视图。后端 `smart_filters` 的六列与 `UpsertSmartFilterRequest` 一直都收这些字段，丢
> 的是前端这一侧。数据库结构与线上契约均未改动，升级无需额外步骤。但此前存下的视图，库里那六列本
> 来就是空的，升级不会把它们凭空补回来：需要重新设好高级筛选，再**用原来的名字存一次**——同名保存
> 会就地覆盖那条视图，不会多出一条。没有重存的旧视图按「这六维都不筛选」应用，与升级前表现一致。

#### 修复
- **保存视图时把六个高级筛选维度一起发给后端。** 阅读状态、评分区间、进度区间、加入天数此前根本
  没有进过请求体，落库的那条视图从一开始就是残缺的——应用时的清空只是这件事的显影。现在
  `saveSmartFilter` 收下当前的 `AdvancedFilters`，六个维度与标签、作者一样是视图的组成部分。
- **读回与应用时把六个维度还原成筛选状态。** 从后端读回的行经 `normalizeRemoteSmartFilter` 补齐
  缺省，应用时经 `smartFilterToSnapshot` 落回 `AdvancedFilters`。缺省一律补 `null` 而不是留空：
  「这一维不筛选」判的是 `!== null`，`undefined` 会被当成设过，凭空多出一个不可见的筛选条件。
- **「一条视图由哪些维度组成」只由一处回答。** 这份字段清单此前在保存、旧数据迁移、应用三处各写
  一份，只有其中一份漏了高级筛选，前端就此与后端的六列漂移。现在三处都问
  `smartFilterRequestBody` 与 `smartFilterToSnapshot`，新增筛选维度时不必再重新想一遍。
- **保存失败后不再在视图列表里留下幽灵条目。** 保存是乐观插入的，请求失败时只弹了提示、没有回滚，
  留下的条目带着客户端 id，后端并不认识它：用户去删只会拿到 404，再被删除那侧的回滚原样放回来，
  只能刷新页面才消得掉。现在保存失败与删除失败一样会把列表还原。

---

### 🚧 未发布（页数未知的书，阅读进度不再被钉死在第 1 页）

> 用快速扫描首次入库的书页数记作 0。此后在阅读器里读到第 42 页，上报返回 200「Progress updated」，
> 落库的却是第 1 页：下次打开这本书从头开始，「继续阅读」、系列已读统计、读完判定跟着一起错。同一个
> 页码改走离线队列补传反而写得对——同一份输入，两条端点结果不一致。数据库结构与线上契约均未改动，
> 升级无需额外步骤：已被写成第 1 页的进度没有留存副本，需要重新翻到原处上报一次。这些书的页数本身
> 仍要等一次强制扫描才补得上（普通扫描因文件未变而跳过），但在那之前进度已能正常记录。

#### 修复
- **页数未知时不再把进度夹成第 1 页。** 单本进度端点先把页码夹到 `page_count` 上界、再把小于 1 的
  抬回 1。`page_count` 为 0 表示页数未知，不是「这本书只有 0 页」，于是任何页码都先被夹成 0、再被
  抬成 1，永远落在第 1 页。现在上界只在页数已知时才生效，页数未知的书按用户报的页码原样记录；
  页数已知的书照旧夹到上界，小于 1 的页码照旧归一化为 1。
- **「把用户报的页码夹进本书合法范围」只由一处回答。** 这条边界此前由单本进度、批量进度同步、阅读
  书签三条写入路径各写一份字面量，只有单本那份漏了「页数未知」的判断，同一份输入于是在两条端点上
  给出不同结果。现在三处都问 `clampPageToBook`，日后新增写入点不必再重新想一遍。

---

### 🚧 未发布（缩略图选 AVIF 不再把封面生成拖成小时级）

> 在设置页「媒体」里把缩略图格式选成 AVIF 之后，生成一张 400px 封面要 55 秒，同一张图换成
> WebP 只要 60 毫秒——慢了约 480 倍。千本规模的库首次扫描，封面阶段从分钟级变成小时级：扫描
> 显示早就完成了，封面却整夜一张一张往外冒，封面 worker 全程占满 CPU。阅读页带 `?format=avif`
> 取图、AI 放大把 AVIF 中间态落盘也走同一条编码路径，同样中招。现在同一张封面 115 毫秒编完。
> 数据库结构与线上契约均未改动，升级无需额外步骤：已经生成的 AVIF 封面本身是完好的文件，原样
> 保留，不需要重建；此后新生成的封面体积比编码库的默认档还小两成左右。

#### 修复
- **AVIF 编码不再掉进最慢档。** 编码参数只填了质量，档位（`Speed`）留空。这个字段的 Go 零值是
  `0`，而在 libavif/aom 里 `0` 恰恰是**最慢**的一档，不是「没指定」——编码库对质量判的是
  `<= 0` 才回落默认值，对档位判的却是 `< 0`，于是零值被当成「用户显式要求最慢档」原样送进
  编码器。两个字段对零值的处理不一致，正是踩空的地方。现在档位一律显式给出。
- **档位取 8，是耗时与体积曲线的拐点。** 实测同一张 400px 封面：最慢档 71 秒 / 11250 字节，
  8 档 99 毫秒 / 13149 字节，编码库默认的 10 档 23 毫秒 / 15250 字节。从 10 降到 8，体积省掉
  约两成而耗时仍在百毫秒级；再往下降到 7 及以下，耗时逐档翻倍，体积却只再省个位数百分比。
  缩略图每本书一张、批量生成，速度权重更高，但也不必把 AVIF 本来的体积优势全部让掉。
- **色度取样同样显式给出。** `ChromaSubsampling` 的 Go 零值是 4:4:4，而编码库的默认值是
  4:2:0——又一处「零值不等于未指定」。交付给客户端的封面与页图取 4:2:0，同档位下比 4:4:4 又小
  又快；AI 放大只喂给外部引擎、不外发的中间态取 4:4:4 加满质量，保住细节。
- **「AVIF 该用什么参数」只由一处回答。** 这套参数此前在两个调用点各写一份字面量，两处都漏了
  档位。现在封面/页图与放大中间态各自问 `avifDeliveryOptions` 与 `avifIntermediateOptions`，
  日后新增调用点不必再重新想一遍，也不会再漏掉某个字段。

---

### 🚧 未发布（清理未引用的封面不再顺手删光页图磁盘缓存）

> 跑一次「清理未引用的封面 / 清理缩略图」任务，整个页图磁盘缓存连同 `<cache.dir>/pages/` 目录
> 一起被删光。`cache.dir` 有默认值且会被强制补齐，所以只要开着页图磁盘缓存，这条路径必然踩中。
> 数据库结构与线上契约均未改动，升级无需额外步骤：被误删的页图缓存没有留存副本，首次重新翻阅
> 时会重新解码转码一遍，之后恢复正常。

#### 修复
- **清理缩略图不再删掉页图磁盘缓存。** 缩略图目录就是 `cache.dir` 本身，页图磁盘缓存是它下面的
  `pages/` 子目录；清理器按「不在 `cover_path` 集合里就删」逐文件判定，页图缓存文件一条也不在
  那个集合里，于是整棵子树被删干净，收尾移除空目录时连 `pages/` 自己也没保住。现在清理遍历跳过
  这些子目录，被引用的封面照旧保留、未被引用的照旧清掉。
- **「cache.dir 下哪些不是缩略图」只由一处回答。** 这条边界此前由重建缩略图与清理缩略图两条
  路径各记一份，后者漏了，误删就此发生。现在缩略图目录、页图缓存目录与两者的包含关系都收进
  `config.ThumbnailDir` / `config.PageCacheDir` / `config.NonThumbnailDirs`，各清理路径只问它；
  日后新增缓存子目录只需登记一次，不会再漏掉某一条。

---

### 🚧 未发布（手改的系列标题不被扫描打回目录名）

> 系列里只要有一本书带 `ComicInfo.xml`，一次重新解析（强制扫描、该书 mtime/size 变了、该系列
> 新增一卷）就会把用户在详情页手改的标题打回目录名，A-Z 索引的首字母跟着回退；同时把用户为放行
> 刮削而解开的 `title` 锁重新锁死，此后刮削永远写不进标题。数据库结构与线上契约均未改动，升级
> 无需额外步骤：库里已被扫描锁上的 `title` 保持原样，但现在在详情页解锁一次就一直有效，不会再被
> 下一次扫描锁回去；已经被打回目录名的标题没有留存副本，需要重新填一次。

#### 修复
- **手改的系列标题不再被扫描打回目录名。** 扫描把归档内嵌的 `ComicInfo` 增补进系列时，
  `summary`、`publisher`、`status`、`language`、`rating` 都过了一道过锁函数，唯独 `title` 绕开它、
  无条件写成目录名。判据不只是锁：扫描能给 `title` 的唯一值就是目录名，而目录名已经原样存在
  `name` 列里——库里一旦有 `title`，扫描就不带来任何新信息。现在扫描只给还没有 `title` 的系列用
  目录名兜个默认值，已有的标题原样保留；新系列首次入库仍然取目录名。
- **解锁过的 `title` 不会被扫描重新锁上。** 增补分支里有一句无条件的「系列名默认始终锁定」，
  并把重组后的锁写回库中，用户主动解开的锁于是被扫描悄悄撤销；此后写入器里的
  `if !locked["title"]` 永不成立，刮削与 AI 提案都写不进标题。锁定字段是用户在详情页开合的开关，
  归用户所有：扫描只读它来决定自己能写哪些字段，不再写它。目录名与默认锁只是新系列建行时的初值。
- **A-Z 索引的首字母跟着实际标题走。** 增补分支固定按目录名重算 `name_initial`，用户把标题改成
  中文后，系列仍留在按目录名算出的字母下。现在首字母由实际写入的标题推出，与人工编辑、刮削写入
  两条路径口径一致。

---

### 🚧 未发布（取消掉的任务，气泡不再永久钉在侧边栏）

> 取消任何一个后台任务后，它的气泡会一直转着 spinner 留在左下角：既不自动消失，也不被
> 「清除已完成」清掉，连关闭按钮都不给，用户只能刷新页面才赶得走它。服务重启后转入中断的
> 任务同样如此。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **取消掉的任务，气泡会自己消失，也关得掉、清得掉。** 后端写进任务状态的是 `cancelled`
  （双 l），侧边栏气泡四处判断却都写成 `canceled`（单 l），永远匹配不上：终态延时移除不安排、
  「清除已完成」跳过它、图标落到转圈那一档、关闭按钮不渲染——一个拼写错误同时造成四种后果，
  是因为同一份终态判断在气泡 hook 与组件里各抄了一遍。现在终态与活动态的判定收进
  `utils/taskStatus`，`isTerminalTaskStatus` 与 `isActiveTaskStatus` 互补，气泡与任务中心都只问它。
- **服务重启后中断的任务，气泡同样赶得走。** `CONTEXT.md` 定义的终态有四种，气泡只认三种，
  中断不在内，于是中断的气泡与被取消的一样永久留在侧边栏。现在四种终态齐全；中断另给琥珀色
  警示图标，不与失败共用红叉——它不是失败，任务体没出错，只是没跑完，可重试。
- **进度条右侧显示的是状态文案，不是词条 key。** 总数未知（`total == 0`）时那一格渲染
  `taskBubble.status.<状态>`，而 `cancelled`、`cancelling`、`interrupted` 三个词条两份语言表里
  都没有，用户看到的是词条 key 本身。现在三个词条在 zh-CN 与 en-US 同时补齐，后端任何状态都
  到不了的死词条 `taskBubble.status.canceled` 一并删除。
- **一个任务正在取消时，气泡头部不再显示「所有任务已完成」。** 侧边栏把活动态只算成运行中与
  已暂停，而取消中也属于活动态：任务体还在收尾，计数却已经归到已完成那一侧。现在活动态含
  运行中、已暂停、取消中三种，与任务中心的口径一致。

---

### 🚧 未发布（弹窗里打字，焦点不再被抢到关闭按钮上）

> 全局搜索弹窗与「新增/编辑资料库」表单里打字，打到一半焦点会跳到右上角的关闭按钮，后续按键
> 全部丢失，空格还会直接把弹窗关掉；扫描进行中即使不打字，焦点也会随进度推送被反复夺走。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **弹窗里打字，焦点不会再被抢到头部的关闭按钮上。** 共享弹窗壳 `ModalShell` 把 `onClose`、
  `closeOnEsc` 放进了 effect 的依赖数组，而调用方普遍传内联箭头（`onClose={() => setOpen(false)}`），
  每次外壳渲染都是新引用，effect 随之卸载重装：卸载时把焦点还给打开弹窗的那个元素，重装时再用
  `requestAnimationFrame` 把焦点抢回弹窗的第一个可聚焦元素——也就是头部的关闭按钮。而搜索词与
  资料库表单值都是外壳的 state，在弹窗里打字必然让外壳重渲染，于是每敲一下就把焦点抢走一次；
  搜索响应返回、SSE 推扫描进度同样触发重渲染，不打字也会被夺走。现在这两个入参改由 ref 持有，
  effect 只依赖 `open`，弹窗从打开到关闭之间只装一次；聚焦逻辑也先判焦点是否已在弹窗内。
- **打开弹窗时，焦点直接落在第一个输入框，而不是关闭按钮。** 头部在正文之前渲染，「第一个可聚焦
  元素」永远是关闭按钮，用户每次都得先点一下输入框才能打字。现在优先取第一个输入类控件，没有
  才退回第一个可聚焦元素——焦点仍然必须进弹窗，读屏软件与键盘导航不受影响。
- **多层弹窗叠加时，一次 Esc 只关掉最上面那层。** 每个挂载中的弹窗都往 `document` 上装自己的
  keydown，谁都没有「我是不是最顶层」的判定，一次 Esc 会把所有层一起关掉，Tab 也会被下层的
  焦点陷阱拽回它自己的面板。现在弹窗打开时登记进一个模态栈，键盘只由栈顶那层响应。
- **多层弹窗叠加再经历一次重渲染，关闭后页面滚动能恢复。** 锁滚动前的 `body.style.overflow`
  由每层各记一份，而 React 重渲染时先跑完所有 cleanup 再跑所有 setup，后装的那层会把已经是
  `hidden` 的值当成原值记下来，此后无论怎么关弹窗，整页滚动都卡在 `hidden`。现在原值由模态栈
  在栈从空变满时记一次，栈空时才还原。
- **弹窗打开时，全局快捷键整体让位。** 应用外壳与资源库页的快捷键只判「事件目标是不是可编辑
  元素」，而弹窗里的焦点常停在按钮上，判不出来：按 `/` 会再叠一个搜索框，按 `g`+`s` 会直接路由
  跳走、把弹窗留在新页面上，在资源库页按 `e` 会在弹窗背后切换批量选择模式。现在两处都先问模态栈
  有没有弹窗打开。

---

### 🚧 未发布（卷视图里的「全选」不再选中整个系列）

> 系列详情从卷列表点进某一卷后，批量选择的「全选」会选中整个系列的书，用户以为只标了这一卷，
> 实际把整个系列都标成了已读。数据库结构与线上契约均未改动，升级无需额外步骤；已经被误标的
> 阅读进度，在同一处重新框选后「标为未读」即可改回来。

#### 修复
- **在某一卷的视图里点「全选」，只选中这一卷的书。** 卷视图（`?volume=...`）只渲染当前卷的书，
  底部选择栏却渲染在这个分支之外、与整页共用，而「全选」取的是系列全部书的 ID，与当前卷无关；
  接着点「标为已读」，`POST /api/books/bulk-progress` 就带上了整个系列的每一本——写错的是数据
  范围，页面上还看不出来。现在全选的范围等于页面此刻渲染的那批书：在卷视图里就是这一卷。
- **点完「全选」后，按钮文案翻成「取消全选」。** 可选总数按「卷数 + 独立书数 + 全部书数」算，
  独立书被算两遍、卷又额外加一遍，永远大于全选实际选中的数量，于是 `allSelected` 始终为假、
  文案不翻转，用户无从判断自己到底选中了什么。现在可选总数与全选实际选中的那批书同源。

---

### 🚧 未发布（并发刮削不再把候选写串系列）

> 资料库页对一个系列点了刮削、响应还没回来时又对另一个系列点一次，弹窗里的候选与「当前值」
> 对比列可能属于前一个系列。数据库结构与线上契约均未改动，升级无需额外步骤；已经因此生成的
> 错误提案在提案中心拒绝即可。

#### 修复
- **资料库页并发刮削，弹窗里的候选与对比列不再串到另一个系列上。** 卡片只禁用发起刮削的那一颗
  按钮，别的卡片照常可点，于是 A 的取候选请求在飞时用户仍可以对 B 再点一次。取候选的三个请求
  与弹窗里的重搜此前都没有世代判定，谁最后返回谁就写进弹窗状态：B 先返回把弹窗指向 B，A 后返回
  再把 A 的详情、标签与候选盖上去，用户挑一条应用就给 B 生成了一条由 A 的候选拼出来的提案。
  现在取候选与重搜共用一个世代号，只有最新一次的响应能写弹窗状态、能清加载态、能弹错误提示。

---

### 🚧 未发布（fast 档位的强制扫描不再清空页数与封面）

> `scanner.scan_profile` 设为 `fast_scan` 时，任何强制扫描都会把整库每一本书的页数清零、封面抹掉，
> 且此后不会自愈。数据库结构与线上契约均未改动，升级无需额外步骤；已经被清空的藏书要切到
> `metadata_scan` 这类打开归档的档位再强制扫描一次才能补回来——fast 档位没有页数与封面的来源。

#### 修复
- **`fast_scan` 档位下的强制重扫、重建搜索索引、重建缩略图，不再把整库的页数清零、封面抹掉。**
  fast 档位不打开归档，页数与封面只能沿用已入库的旧值，而扫描开头那份已入库快照此前只在非强制
  扫描下填充，于是强制扫描里「保留旧页数/封面」那条分支永远拿不到旧值，入库时把 `page_count`
  写 0、`cover_path` 写空。这三条路径都以强制扫描发起，而被清空之后 fast 档位既不打开归档也不排
  封面任务，后续的增量扫描又因 mtime+size 没变而跳过这些书，页数与封面再也回不来。现在这份快照
  无论是否强制扫描都完整填充；强制扫描仍然不吃增量跳过——跳不跳过由遍历里那处独立的守卫决定。

---

### 🚧 未发布（改名重连不再被同轮的库清理抢跑）

> watch 模式下给一本书改名，阅读进度会丢。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **监听模式下改名一本书，阅读进度、书签、合集归属与阅读清单条目不再丢失。** 改名会产生
  Rename(旧名) + Create(新名) 两个事件，分别给这个库排期一次清理与一次扫描。此前两者共用同一个
  去抖窗口并在同一次 tick 里并发派发，而清理只做 stat、比要走完整棵树并在末尾才写改名重连的扫描
  快得多，于是稳定抢先把改名前那一行当成「文件已消失」删掉，挂在这本书上的记录随之级联消失——
  改名重连在 watch 模式下等于不存在。现在同一个库的清理接在该库这次扫描之后串行；扫描没跑成时
  不清理，改为重新排期等下一个窗口，宁可让幽灵记录多留一会儿也不冒删错的风险。

---

### 🚧 未发布（分页排序补齐末位唯一键）

> 十处 `LIMIT/OFFSET` 分页的 `ORDER BY` 在排序键平局时没有区分先后的末位键，行序由查询计划决定。
> 排序键不平局时行序逐字不变；数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **提案中心、AI 分组提案、合集、阅读清单、智能书架，以及 OPDS / Mihon 的系列分页，排序末位
  统一补上唯一键。** 这些分页此前止步于置信度、时间、`sort_order` 或名称——都可能整批相等：
  提案是批量入库的，同一批置信度与时间逐字相同；OPDS / Mihon 按 `COLLATE NOCASE` 比名称，
  大小写不同即算相等。平局行之间谁先谁后此前由查询计划决定，翻页因此可能在页边界重复或漏掉
  条目。补上末位唯一键后，同一批数据的翻页与一次性取数逐位相同。

---

### 🚧 未发布（资料库翻页不再把同一条目发两遍）

> 按加入时间或更新时间排序翻页时，第 1 页的最后一个条目会再次出现在第 2 页开头。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **按「最近更新」「最近加入」排序翻页，不再重复条目、也不再漏掉与翻页边界同一秒的条目。**
  这两个排序的翻页从第 2 页起走键集游标，边界谓词把上一页末行的时间当参数与 `created_at` /
  `updated_at` 比大小。这两列由 `CURRENT_TIMESTAMP` 写入，存的是 `2026-05-01 12:00:00` 这样的
  文本，而驱动会把 `time.Time` 参数写成 `2026-05-01 12:00:00 +0000 UTC`——SQLite 比的是文本，
  同一时刻的两种写法既比不出相等、前者还总是排在后者之前，于是倒序把边界行又发了一遍、
  正序则把与边界同一秒的行整批跳过。现在参数按列的存储格式绑定。按名称、收藏、册数、卷数、
  页数排序的翻页不受此问题影响。

---

### 🚧 未发布（任务体干活的资格收进一个句柄）

> 后台**任务**的任务体干活所需的三样资格——写这个任务的进度、在可中断点问一句**暂停闸门**、
> 发起一次**磁盘作业**——收进一个交下来的**任务句柄**。除下面这一条外，任务的进度、文案、
> **终态**与并发徽章逐字不变；配置结构、数据库结构与线上契约均未改动，升级无需额外步骤。

#### 改动
- **KOReader 的指纹重建不再把被暂停闸门或存储令牌挡下的书计入「已哈希文件数」。** 那些书一个
  字节都没读。此前该任务与文件身份重建对同一个指标给出不同答案——后者是在读盘出错的检查之后
  才 +1——现在两处口径一致：只数真正被读过的书。慢盘上排队与暂停最多，这个数字与实物的偏差
  也最大，而它是任务面板上用户唯一能看到的吞吐量。

---

### 🚧 未发布（后台磁盘作业收进单一入口）

> 后台**任务**每动一次磁盘，此前都要自己抄一遍同一套仪式：解析存储策略 → 按工种挑并发上限 →
> 申领**存储令牌** → 过**暂停闸门** → 干活 → 归还令牌 → 把等待与暂停的耗时折进指标。全仓八个
> 取用点各一份，`scanner` 与 `api` 还各自维护了一份取令牌的辅助方法与一份逐字相同的上限公式。
> 本批把这套仪式整个收进新的 `internal/diskwork`：调用方交出「干哪一类活、字节落在哪」和一个
> 闭包，其余全部由模块承担。八个取用点逐个转换完毕，旧接口在同一批里删除；另有一处从来就没抄过
> 这套仪式的读盘（KOReader 的书籍指纹重建）一并收进来，见下。
>
> 配置结构、数据库结构与线上契约均未改动，升级无需额外步骤。

#### 改动
- **每个工种能并发几个，改由工种自己决定，不再取决于发起它的是哪个子系统。** 此前同一个工种在
  两个包里会拿到两个不同的上限——`identity_hash` 在扫描器里是 min(扫描并发, 哈希并发)，在维护
  任务里是 min(哈希并发, 归档打开并发)，而它根本不打开归档；`metadata_scan` 同样分叉。现在按
  「这个工种到底在干什么」裁定：读归档元数据用归档打开并发、算文件指纹用哈希并发、构建封面用
  min(归档打开并发, 封面并发)、写缓存用封面并发。
  - **影响面只有手工把某个库的四项并发配成不同正值的部署。** 四种存储档位的内置值一个都不受
    影响：默认档与 SSD 档四项全为零（本就不限流、不排队），外置硬盘档与网络存储档四项全为一，
    新旧公式在它们上面结果完全相同。
- **按下暂停后，后台不再继续往盘上写缩略图。** 此前从上一次暂停检查到缩略图落盘之间隔着整个
  图片转码，暂停在这一段是不生效的——用户按下暂停，盘上仍会继续多出文件。现在写文件之前必过
  一次闸门。
- **存储策略的解析源统一成「字节落在哪」。** 此前有五处按**资料库根**解析——扫描器的归档打开与
  两处指纹取这次扫描的库根，维护子域的两处指纹回填取那本书所属资料库的路径——现在一律按真正被
  读写的那个文件的路径解析。
  存储策略走最长前缀匹配，因此只有**配了比库根更深的策略条目**时，这几处生效的档位才会变；
  只按库根配策略的部署不受影响。缓存目录的写入另有一条规则：卷键仍由缓存目录本身决定，而 IO
  策略在它与藏书同在一块盘上时服从藏书那一侧——它通常落不进任何资料库的前缀，却和藏书抢同一
  块盘。
- **慢盘上，KOReader 的书籍指纹重建此后为阅读让路。** 这个任务逐本读完整个文件算指纹，此前
  全程不取令牌——外置硬盘档与网络存储档下，用户一边翻页一边跑指纹重建，盘被两边一起抢。现在
  它和其余磁盘作业同形：过闸门、取令牌，因而遵守「为阅读让路」与「只在空闲时干重活」两项设置，
  等待与暂停的耗时也进了任务面板。匹配刷新任务的第一阶段就是这次重建，一并受益。
  默认档与 SSD 档不限流，不受影响。
- **慢盘上「等令牌等了很久」现在九处都会记日志。** 此前只有封面构建与缓存写入两处会留下记录，
  扫描、指纹与 ComicInfo 回写卡在令牌上是无声的——而它们才是慢盘上真正会堵住的那几处。日志带
  工种、路径、存储档位、卷键与等待毫秒；等了很久最终被取消的那次申领也会留下记录。缩略图落盘
  自己那条慢日志不变，它另有一半在计写入耗时。

#### 架构
- 新建 `internal/diskwork`，位于 `config`、`storageio`、`taskcontrol` 三个叶子包之上，由
  `scanner`、`api` 与 `koreader` 共用。对外只有一个入口：交出一份**磁盘作业**声明和一个闭包。
- **三条契约由结构而不是调用方的记性保证**：闸门必然先于令牌，被暂停的作业不会攥着令牌把别人
  堵死；令牌的归还不依赖闭包走哪条出口，闭包 panic 时同样已归还；能并发几个只由工种决定。
  相应地，两处为「提前归还 + 兜底归还」手写的幂等布尔、一处为「取到令牌之后再看一眼取消」
  手写的 `select` 一并消失。
- **等待与暂停的耗时是返回值而不是副作用**，模块不持有也不上报任何指标；取用点上只剩一行
  「把它折进我的指标」，此前逐处手写的耗时折算与 `nil` 守卫沉进两个包各自的那一个方法里。
- **令牌的持有区间钉死在真正的 IO 上。** 归档打开那一处此前握着令牌多跑了一段纯 CPU 的路径
  解析、复合哈希与排序号提取，ComicInfo 回写那一处多跑了一段聚合与序列化——在慢盘上这些都在
  虚占归档打开的并发额度。
- **两份一模一样的指纹回填循环合成一处。** 低优先级的全量哈希回填此前在 `api` 里重抄了一遍
  KOReader 的**指纹**重建（数缺口 → 按游标切批 → 读一遍文件 → 回填 `file_hash`）；本批抹平了
  两者最后一处差异——取不取令牌——之后，它收成一个薄适配器：批大小、批间停顿与匹配模式作为
  选项交给 `koreader`，`api` 只留自己的任务指标与节奏常量。坏书那条警告日志的文案随之统一成
  一条（「Failed to fingerprint book」）。
- 前台的阅读取页**不是**磁盘作业，仍直接向调度器申领：它不过暂停闸门、上下文刻意去掉取消、
  耗时进请求诊断而非任务指标，收进来只会换得一个「这几件事都反着来」的开关。
- 删除两个包各自的取令牌辅助方法（含 api 那份里没有任何调用点的封面构建死分支），以及那条从
  扫描器内部伸手进未导出方法、靠固定时长 `sleep` 去测 `storageio` 自身排队语义的用例——该包
  已有一条等价用例。扫描器包内不再有任何用例触碰取令牌的内部。
- **新增覆盖**：模块的契约用例压在唯一的新接缝（构造点）上，注入新建的调度器与桩配置函数，
  无数据库、无文件树、无扫描器，覆盖四个工种的上限、短路、闸门先于令牌、取消、归还的必然性
  （闭包返回错误与闭包 panic 两条路径）与 `Stats` 如实返回。另补上三组此前**零覆盖**的失败
  出口：归档打开失败与页目录读取失败、封面那一页读不出、指纹算不出来时的分页游标推进——它们
  正是本次删掉手写归还闭包之后唯一可能漏掉令牌或卡死游标的地方。KOReader 的书籍指纹重建另有
  三条：哈希令牌被占满时它不该动手、前台正在读这块盘时它该让路、每本读盘的实况要落到任务面板的
  存储 IO 那一行。

---

### 🚧 未发布（元数据提案独立成子域）

> 元数据提案的入队、裁决与读取从 `internal/api` 整体搬进新的 `internal/proposal`。
> 绝大部分是内部改动，用户可见的只有下面两条。无数据库改动，升级无需额外步骤。

#### 改动
- **批量处理时「已被别人抢先」的条目不再报成失败。** 批量应用与批量拒绝的响应各新增一个
  conflict 桶，收「这条提案已经不是待裁决」这一种结局——最常见的情形就是另一个标签页、
  另一个人或上一次操作已经处理过它们。此前它们混在 failed 里，于是一堆并没有坏掉的东西
  会让整条汇总提示变红。现在 failed 只收真故障（查询报错、写入失败、提案查不到），
  提示的严重度也只看 failed。两个语种的批量结果文案各加了一项「已被处理 N 项」。
  - 单条应用与单条拒绝的 409 语义不变。
- **打开有多条待裁决提案的系列时不再随提案条数发起额外查询。** 此前系列详情页在提案列表上
  逐条取字段行（N 条提案 = N 次查询），收件箱那条路径早就是批量取。两处现在同一条通路。

#### 架构
- 新建 `internal/proposal`，承载提案子域的全部规则：入队去重（对待裁决提案与最近的拒绝记录
  逐字比对）、按**锁定字段**过滤、应用与拒绝，以及三条读路径（按系列列出、
  收件箱分页、待裁决计数）。元数据写入器一并迁入，那条把事务句柄递出去的 hook 参数随之消失。
- **每一种结局都是结果值而不是哨兵错误。** 入队的五种（已入队 / 复用既有提案 / 无差异 /
  差异全被锁 / 此前已拒绝）、应用的六种、拒绝的三种各由一个分类表达，`error` 只留给故障。
  四个哨兵错误与 api 侧四个 handler 里的 `errors.Is` 一并删除——此前「良性结局不退化成
  500」全靠「每个哨兵都必须被识别到」这条纪律撑着，漏一个，用户看到的就是「服务器错误」
  而不是「先去解锁字段」。被抢先无论是前置检查发现的还是事务内 CAS 撞上的，都是同一个分类。
- 「锁定状态按**当前**值算」从视图映射里的一个 `||` 迁进模块，成为字段上的终值。它是一条
  裁决规则而不是渲染细节：它防的是「先入队、后加锁」的字段没有锁徽章、用户点了应用却被
  静默丢弃。
- 模块只声明自己要用的查询，字段行只暴露批量版——那条 N+1 因此在包内写不出来。
- **`internal/api` 与提案表之间立了一条源码扫描守卫**（`TestAPIDoesNotQueryProposalTables`）。
  符号名不写死，从 sqlc 生成物里按 SQL 反推，新加一条打提案表的查询会自动纳入；不开白名单。
  它存在是因为编译器在这里拦不住：api 仍握着全量的 `Store`（收窄经评估不做，见 v1.4.0），
  「顺手查一下提案表」永远编译得过。
- **故障路径的报错更诚实了**（成功响应逐字未变）。此前单条应用遇到数据库故障会报
  404「提案不存在」或「系列不存在」——一次数据库抖动被说成「这条东西没了」，用户会去找一件
  其实还在的东西。现在只有真的查不到才回 404，其余故障回 500。相应地：
  - 收件箱三条查询原先各配一句 500 文案，统一为 `Failed to list metadata reviews`。
  - 单条应用的两句文案（`Failed to load review fields`、`Series not found`）并入
    `Failed to apply metadata review` 与 `Metadata review not found`。

---

### 🏷️ v1.4.1 — 2026-07-30（工具链与依赖升级）

> 纯维护版本，无功能变更、无数据库改动。v1.4.0 的产物是这批升级**之前**的代码。
> 从 v1.4.0 升级无需任何额外步骤（v1.4.0 本身的两条升级须知见下一段）。

#### CI
- **七个 GitHub Action 全部升到原生 node24。** GitHub 已弃用 Actions runner 上的 node20，
  此前有**四个** action 还在 node20（GitHub 的弃用告警只点了其中两个，另外两个是逐个查
  各自 `action.yml` 的 `runs.using` 才发现的），靠 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`
  被强制降级运行——CI 能过，但等 node20 真从 runner 上移除时会集体失效。
  - `golangci-lint-action` v7.0.1 → v9.3.0 · `upload-artifact` v5.0.0 → v7.0.1
    · `download-artifact` v6.0.0 → v8.0.1 · `action-gh-release` v2.6.2 → v3.0.2
    · `setup-node` v6.5.0 → v7.0.0 · `setup-go` v6.5.0 → v7.0.0
  - 逐条核对过跨大版本的破坏性变更对本仓用法的影响。其中 `download-artifact` v8 把
    「下载哈希不匹配」从警告改为**报错**——对发布流水线是净收益（我们本来就产出
    `SHA256SUMS.txt` 供自建者校验）。
  - `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` **保留不删**：所有 action 现已原生 node24，
    它因此变成安全网而非必需项——将来某次依赖升级若又引入 node20 的 action，
    它能让其继续跑，而不是等 runner 移除那天 CI 突然全红。已补注释写清这一点。

#### 依赖
- **Go 依赖 7 项**：`modernc.org/sqlite` 1.51.0 → 1.55.0（内嵌 SQLite 版本随之变化，
  因此单独验过迁移、FTS5 gram 索引、部分唯一索引与 RAR 夹具）、`golang.org/x/crypto`
  0.39.0 → 0.54.0（bcrypt 在鉴权路径上）、`x/image` 0.43.0 → 0.44.0、
  `go-chi/chi` 5.3.0 → 5.3.1、`rardecode` 2.2.3 → 2.3.0、`gen2brain/avif` 0.4.4 → 0.6.0。
- **前端依赖 21 项**，含四个大版本：`eslint` 9 → 10、`@vitejs/plugin-react` 5 → 6、
  `vite` 7 → 8、`@types/node` 25 → 26，以及 `@yui540/comimi` 0.5 → 0.18（阅读器的
  Comimi 主题）。前端高危漏洞从 **7 个降到 2 个**。
  - **TypeScript 暂留在 6**：`typescript-eslint` 目前没有任何版本支持 TS 7
    （最新 8.65.0 的 peer 仍是 `typescript <6.1.0`），强行升级会让 `npm ci` 直接
    ERESOLVE 失败。等上游支持后再单独升。
  - 剩下的 2 个高危漏洞是同一条——react-router 的 RSC 模式 CSRF 绕过
    ([GHSA-qwww-vcr4-c8h2](https://github.com/advisories/GHSA-qwww-vcr4-c8h2))。
    **对本仓不可达**（我们用的是最朴素的 `BrowserRouter`，没有 RSC 与 server actions），
    且**没有修复版可升**（通告范围 7.12.0–8.2.0，而 npm 上最新就是 7.18.2；
    `npm audit` 建议的「修复」是降到 7.11.0）。
- ⚠️ **待人工确认**：`comimi` 0.5 → 0.18 的运行时行为（手势、默认设置、菜单）无法用类型
  检查覆盖，建议在阅读器的 Comimi 主题上过一眼。它是**可选**主题，默认主题不受影响。

#### 修复
- eslint 10 默认启用了 `preserve-caught-error` 与 `no-useless-assignment`，报出 4 处真问题：
  登录 / 初始化 / 改密三处 `throw new Error(...)` 把原始的 axios 错误整个丢掉了
  （状态码与响应体这些排查要用的信息全没了），改为挂到 `cause` 上；
  另有一处读不到的变量初值。

---

### 🏷️ v1.4.0 — 2026-07-30（全量代码审计的逐批修复）

> 对整个代码库做了一次系统性体检并逐批修复，共 62 个提交。每条修复都配了回归测试，
> 高危项做了**反向验证**（临时回退修复、确认用例变红）；部分方案在对抗式复核后被判为
> 「不做」并在下面写明理由。
>
> **升级须知（两条，务必先读）：**
>
> 1. **不可回滚到 v1.4.0 之前的二进制。** `metadata_review_fields.status` 这一列已被删除
>    ——它是死数据（唯一写入点硬编码 `'pending'`，全仓无 UPDATE，且读路径只查 pending
>    审核，写进别的值也永远读不出来）。旧版二进制的 SELECT/INSERT 显式带这一列，
>    启动后会报 `no such column`。升级前请备份数据库。
> 2. **首次启动后 franchise 合集的链接会失效一次。** 这类系统合集改用稳定自然键后，
>    存量的无键行会被换成带键新行（id 变化）。如果你在 Mihon/OPDS 客户端里按 id 收藏过
>    franchise 合集、或在站内存了合集链接，需要重新添加一次；之后就不会再变了。
>    **手工创建的合集完全不受影响。**
>
> **对外行为变更：**
>
> - 元数据审核：对已处于终态的记录重复 apply/reject 由 200 变为 **409**。
> - 多用户站点新注册的 KOReader 设备在被管理员指派之前，其同步进度不计入任何用户
>   （此前会被静默记到 id 最小的管理员名下）。
> - 普通用户不再看到资料库增删改与系统设置的入口（后端一直是管理员专属，前端此前照样
>   渲染，点了只会吃 403）。
> - 批量应用元数据的响应新增 `partial` 桶。
> - 设置页「保存本分区」不再连带提交其他分区未保存的草稿；反过来保存后也不再抹掉它们。
>
> 下面按主题列出本次的全部改动。

## 更早的版本

- [2026-08](docs/changelog/2026-08.md) — 1 条
- [2026-07](docs/changelog/2026-07.md) — 95 条
- [2026-06](docs/changelog/2026-06.md) — 3 条
- [2026-05](docs/changelog/2026-05.md) — 58 条
- [2026-04](docs/changelog/2026-04.md) — 42 条
