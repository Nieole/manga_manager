# Changelog

本文件记录项目各版本的功能新增、改动与修复。

---

### 🏷️ v1.6.0 — 2026-09-03（阅读器的六个重采样滤镜真正生效：把显示尺寸下发给服务端）

> Lanczos3 / Bicubic / Mitchell / Lanczos2 / B-Spline / Catmull-Rom 是缩放用的插值核，不是一次独立
> 操作。上一版已经把「有核无尺寸」识别成无操作、改回原始字节透传，代价是这六项选下来画面确实完全一样。
> 缺的那一环是目标尺寸——只有浏览器知道容器多大，服务端补不出来。这一版把它补上：阅读器按适应模式
> 算出目标尺寸并下发，服务端在「原图 → 目标尺寸」这一大步上真正跑重采样，插值核的差别落到字节上。

#### 新增
- **阅读器按适应模式下发目标尺寸**：适应宽度只发宽、适应高度只发高、适应屏幕两条边一起发（服务端按
  框等比缩，不拉伸也不放大）；条漫按宽铺满，只发宽。原始尺寸不缩放，因此不发尺寸——那一档下这六个
  滤镜本就无事可做，设置面板会直接说明并指路到三个适应模式。
- **尺寸按 256 的倍数向上取档**：容器 1100 与 1250 都请求 1280，命中同一份缓存；图略大于显示尺寸，
  浏览器再做一次小幅缩小，画质损失可忽略。高 DPI 屏按物理像素要图，DPR 采纳上限 2、单边上限 3072。
- **档位化在服务端再夹一次**，缓存键记的是取档之后的尺寸：目标尺寸是外部输入，不能只信前端，否则同
  一页会在每个窗口宽度上各留一份缓存。
- **`fit=inside` 这一档的语义是「框」**：等比缩进框内、不放大。封面与缩略图不带它，仍按画布精确缩放。
- 顺带修好 B-Spline 与 Catmull-Rom 只给一条边时交出整页空白——底层的 Fit 对缺边直接返回空图，另一条
  边现在按源图纵横比补齐。
- 代价如实记账（Apple M1，1600x2300 的页，取 1280 档，冷缓存单次请求）：PNG 页 22.7 ms → 339.7 ms，
  JPEG 页 20.9 ms → 213.6 ms。只有选了这六个滤镜之一才付；同一页同一档第二次起走缓存。

---

### 🏷️ v1.6.0 — 2026-09-03（普通账号打开侧边栏，就拿到了服务器上每个资料库的绝对路径）

> `/api/libraries` 对已登录用户一律开放——普通账号的侧边栏、仪表盘、整理页都靠它拿资料库的
> id 与名字。但响应里还带着 `path`，那是**宿主机上的绝对路径**：一个只该看漫画的账号据此就能
> 拼出服务器的目录结构。守卫没漏挂，漏的是字段这一层。

#### 修复
- **`path` 按角色裁掉**：只有管理员拿得到真值——新建/编辑资料库本就是管理写，编辑弹窗要把它填
  回去；普通用户界面一处都不读它。字段本身保留并置空，前端契约不变。取不到用户时按普通用户
  处理，与事件流同口径。

---

### 🏷️ v1.6.0 — 2026-09-03（OPDS/Mihon 搜索翻页，同名系列可能重复出现或整本消失）

> 协议搜索的两条分页查询以「标题（不分大小写）」收尾排序，末位没有唯一键。同名系列之间谁先谁后
> 由查询计划决定，翻页就可能在页边界重复或漏掉条目。现有的守卫用例只扫 `sql/query.sql`，
> 而这两条是 Go 里的内联字符串；另一条手写守卫的覆盖面写的是「两处」，包里实际有四处。

#### 修复
- **两条查询的 `ORDER BY` 补上 `s.id` 兜底**，排序恢复全序。
- **守卫用例改成扫源码**：包里每一处 `LIMIT ? OFFSET ?` 都被找出来核对末位唯一键，覆盖面从两处
  扩到四处；排序由 Go 函数拼出的那些必须登记后逐一过子句，新增一处却不登记就直接变红。

---

### 🏷️ v1.6.0 — 2026-09-03（在任务中心搜索框打「scan」，四个字符发了四次请求，结果还可能是「sca」的）

> 任务中心与日志页的搜索框每敲一个字符就发一次查询（日志页每次还带 `limit=300`），慢网下响应
> 乱序时，最后落地的可能是半截关键词那一次的结果，而输入框里已经是完整的词。附带地，任务中心
> 15 秒轮询的定时器随取数函数身份不断重建，打字期间永远不到点。
>
> 两处 UI 本来就摆着回车与「查询」/「刷新」按钮——本意是打完再查。

#### 修复
- **改成按需查询**：输入框里的内容与生效条件分开存，只有回车或按下按钮才提交。任务中心的
  「目标 ID」输入框同理。SSE 增量帧也按已提交的条件判去留，与列表里那批任务同一把尺子。
- **两处取数都加世代号**：响应回来时世代号对不上就整份丢弃，迟到的旧结果盖不掉新结果。

---

### 🏷️ v1.6.0 — 2026-09-03（连点两个阅读清单，标题是新的、列出来的是上一个的系列）

> 阅读清单切换后的取数没有任何防护：没有世代号、没有 AbortController、没有 cleanup。慢网下快速
> 连点两个清单，先发的响应后到就会盖掉已经切过去的那一份。合集页右栏是同一个交互形状，那边做了
> 世代号，这边没有。
>
> 阅读器里另有一处形状不同但症状相似的：取系列上下文的请求**在发出之前**就把「已经取过这个系列」
> 写死，失败路径不回滚。于是碰上一次网络抖动之后，同一系列内再怎么换书都不会重取，顶栏的章节
> 列表按钮永久消失，直到整页刷新——且没有任何报错。

#### 修复
- **阅读清单右栏照合集页的形状加世代号**：整份覆盖右栏的两条取数（切换清单、加完系列后的重取）
  都只认最新一次的响应。
- **阅读器的系列上下文标记兼作世代号**：取失败时退回标记，同一系列内换一本书就会重取；响应回来时
  系列已经换过则整份丢弃。

---

### 🏷️ v1.6.0 — 2026-09-03（一次扫描失败，侧边栏任务气泡画着绿色对勾说「所有任务已完成」）

> 资料库扫描失败后，收起的任务气泡显示**绿色对勾 + 「所有任务已完成」**——不展开就以为一切正常。
> 只要气泡里已无活动态任务、且剩下的终态里有一个不是「完成」，终态气泡那 20 秒存活期一直是错的。
> 反过来把唯一一个任务暂停掉，折叠条又说「1 个任务进行中」并持续转圈，展开后同一个任务却标着
> 「已暂停」，同一处 UI 自相矛盾。
>
> 折叠条只按「有没有活动态任务」二选一：有就转圈报数，没有就绿对勾报「已完成」，四种终态不作区分。

#### 修复
- **折叠条先看活动态、再按严重度收口。** 还有活动态任务就报活动态计数；都进了终态就在
  失败 > 中断 > 已取消 > 完成里挑一种作结论，只有全是「完成」才画绿对勾。图标改与列表条目
  共用一套：终态各画各的，活动态里没有推进中的（全被暂停）改画暂停符——转圈表示还在推进。
- **文案改口径。** 活动态含运行中、已暂停、取消中三种，「进行中」是它的禁用词，英文 running
  更是「运行中」这一个状态的名字（气泡展开后它就译作 Running）。改称「N 个活动任务」/
  「N active tasks」。清空按钮的中文同理改「清除终态任务」——它清掉的是四种终态，不止「已完成」。

---

### 🏷️ v1.6.0 — 2026-09-03（英文界面上，「1 day」紧挨着「Longest streak 1 days」）

> 统计页同一张卡的上下两行，大字写「1 day」、小字写「Longest streak 1 days」；仪表盘同一个
> 活跃天数，热力图标题是「Active 1 day in the last 7」，卡片副标题却是「Active 1 days in the
> last 7」。藏书数、字段数、合集数、端点数……凡是数值为 1 的地方都在照复数渲染。
>
> 复数模板（`one=…|other=…`）这套机制是有的，但 94 条带 `{{count}}` 的英文词条里只有 9 条用上了。

#### 修复
- **45 条计数后面跟可变形名词或动词的词条迁移成复数模板**，覆盖排查里点名的全部「1 books」式渲染。
- **另 40 条按两类留在原样**：series 这种英语单复数同形的名词，以及计数后面根本没有可变形词的
  （滑块与设置项的当前值、以形容词或过去分词收尾的）。两类都写成用例里的具名清单，逐条记下理由。
- **加守卫用例**：凡带 `{{count}}` 的英文词条都必须是合法复数模板，例外只能来自那两张清单；
  清单里的 key 消失、或反过来写成了模板，同样变红。将来新增词条漏写模板不会再溜到界面上。

---

### 🏷️ v1.6.0 — 2026-09-03（同一个资料库，界面上有「资源库」「书库」「资料库」三种叫法）

> `CONTEXT.md` 定的词是**资料库**，中文界面上却是 106 处「资源库」、3 处「书库」、2 处「资料库」；
> **智能书架**在界面上一律叫「智能合集」（禁用词），而同目录的代码注释用的是「智能书架」；
> 审核中心的说明把**提案**与**候选**正好用反了——元数据变更建议就是提案，AI 分组给出的合集成员
> 建议才是候选，文案却写成「集中处理元数据候选和 AI 分组提案」。

#### 修复
- **资料库归一。** 「资源库」「书库」全部改为「资料库」；其中的「外部资源库」改为 `CONTEXT.md`
  的**外部库**，而不是顺手改成「外部资料库」——外部库不在任何资料库目录内，本来就是另一个词。
- **智能书架归位**：来源标签、编辑与删除弹窗、快照说明与空态一并改口；顺带把 collections 目录里
  几处还写着「智能合集」的注释对齐。
- **提案与候选各归各位**：审核中心说明改为「集中处理元数据提案和 AI 分组候选」，元数据审核页的
  「元数据候选」改为「元数据提案」，系列详情对照列的「建议值」改为「提案值」。
- **加守卫用例**：中文词条里出现「书库」「资源库」「智能合集」「动态合集」「进行中」即变红。
  「建议」「候选」要看上下文，不一刀切。

---

### 🏷️ v1.6.0 — 2026-09-03（阅读器里换本书，离线面板还挂着上一本的下载态）

> 点「缓存本书」下载一本长书，然后继续往下翻——翻到末页会自动跳到下一本，也可以从卷内章节列表
> 直接切。切过去之后新书的离线面板写着「正在缓存…」，分子是**上一本**的已下载页数、分母却是新书
> 的总页数，进度条给出一个毫无意义的百分比；「缓存本书」与「移除」两个按钮全部禁用，一本长书下完
> 要几分钟，这几分钟里用户对当前这本书什么也做不了。上一本的下载失败（配额不足、页图 404、
> 会话过期 401）时，错误红字也挂在新书的面板上。
>
> 下载态各存一份单值，只有写回缓存状态那一处比对了「现在读的还是不是发起时那本」。

#### 修复
- **下载态与删除态按书分格存**：面板读的是当前这本书那一格，换书之后新书的面板是干净的；
  切回原来那本仍看得见它在下载、下到第几页，失败的红字也还在它自己那儿。下载在换书后继续跑完
  是有意的——用户点「缓存本书」要的就是它下完，所以「换书就清空」是另一种撒谎。
- **读回离线状态也认发起时那本书**：A→B→A 快切时先发后回的那次不会再拿别的书的状态覆盖当前这本。

---

### 🏷️ v1.6.0 — 2026-09-03（离线书架上连删两本，删掉的那本会爬回来；删除途中下好的书反而消失）

> 在离线书架上点完第一本的删除、紧接着点第二本，刷新后其中一本又回到列表里，写着「0%，0 / N 页」，
> 点「打开阅读器」却什么也读不到——盘上的字节已经删干净了，索引里却留着完整的 `urls` 与下载令牌。
> 反过来，删除某本书的这段时间里另一本刚好下载完，那本下好的书会从书架上凭空消失、字节却留在盘上：
> 「缓存大小」照样有占用，而「清空离线缓存」因为书目为空是灰的，用户只剩显式登出这一条出路。
>
> `deleteOfflineBook` 在开头定格了一份书目索引的快照，删字节要跨好几段 await，收尾又把这份过期
> 快照整份写回去——期间别的书的删除与下载收尾全被盖掉。删一本几百页的书要几百次 `cache.delete`
> 加一次全量扫描，窗口是几百毫秒到秒级。

#### 修复
- **删除只改自己那一格**：字节删完之后，读索引、摘掉这本书、写回连成一段没有 await 的同步动作，
  与下载收尾同一条口径。顺序也钉死为「先删字节、后摘索引」——反过来的话，字节删到一半失败就留下
  索引说没有、盘上还在的孤儿，那时这本书在书架上已经看不见，单本删除再也够不着它；而这个顺序下
  失败只是「删了一半」，书还在书架上，用户能再点一次。
- **孤儿字节有了回收入口**：「清空离线缓存」的可用与否改看盘上实际占用，而不是书目条数；书目为空
  而字节仍在时，空态里直接说明还留着多少、该点哪里回收。
- **一次只准删一本**：书架上的删除按钮改成只要有删除在跑就全部禁用，不按各自那本单独判。

---

### 🏷️ v1.6.0 — 2026-09-03（AI 分组的候选合集里删过一个系列，这条候选就再也应用不了）

> 审阅中心里点「应用此合集」只换回一句「服务器错误」，反复点都一样；点整条审阅的应用同样 500，
> 而且这条审阅里其他本来没问题的候选合集一条都建不出来。用户唯一的出路是驳回。
>
> 触发条件是审阅生成之后、应用之前，候选点名的某个系列从库里消失了——重扫清理会这么干。
> `ai_grouping_review_collections.series_ids` 是一串裸 ID，没有外键，那些悬空 ID 插进
> `collection_series` 就撞上 `series` 外键（`INSERT OR IGNORE` 不抑制外键错误），整个事务回滚。
> 读侧早把「系列已被删除」当正常情形跳过，写侧却撞外键。

#### 修复
- **应用时在事务内滤掉已被删除的系列**，用剩下的建合集——与读侧口径一致。
- **一条候选的系列全没了就不建空合集**：单条应用回 409 并说清缘由，这条候选留在待应用等用户驳回；
  整批应用只跳过它自己，同一审阅里其他候选照建，审阅也因此不收尾；整批一条都建不出来时回 409。
- **候选合集的 `series_ids`、`series` 与 `series_count` 三者一律只讲还在的系列**：不会再出现
  「2 个系列」却只列出 1 个，编辑草稿也不会把那个既看不见也点不掉的悬空 ID 原样写回去。

---

### 🏷️ v1.6.0 — 2026-09-03（在 AI 分组审阅里处置一个候选合集，收件箱塌回第一屏）

> 往下滚动加载了好几屏（比如 40 条），选中第 30 条，对它下面某个候选合集点「应用」「拒绝」或编辑后
> 「保存」，动作成功了，左栏却瞬间缩回只剩前 20 条，右栏详情跳回第 1 条审阅——用户被踢回队首，
> 得重新滚回刚才的位置。
>
> 三个动作收尾都重新拉第一页并整份替换列表，选中项不在新的第一页里就落到队首。

#### 修复
- **处置单个候选合集只就地更新列表**：被处置的那条按接口回执落状态（一条审阅里最后一个候选处置完，
  审阅跟着落定），已加载的那几页重取后按 id 就地覆盖，条数、顺序与选中项都不变——与资料库页的
  `refreshLoadedPages` 同一条线。

---

### 🏷️ v1.6.0 — 2026-09-03（重试后台哈希回填，起的是另一条任务）

> 任务中心里那条由扫描收尾自动串联的**哈希回填**失败或中断后，点「重试」回 202，但那一条原地不动，
> 任务中心里多出一条标题一模一样的新任务在跑——而且是前台档：大批次、无批间停顿，正是低优先级回填
> 刻意避开的抢盘方式，匹配模式也从钉死的二进制哈希变成跟着当前配置走。
>
> 任务重试的注册表按**任务类型**分发，而 `rebuild_book_hashes` 这个类型下有两条**任务键**：维护页
> 发起的前台重建，以及扫描收尾串联的低优先级回填。注册项把任务本身丢掉了，于是无论用户点的是哪一条，
> 重启的都是前台那条。刮削那个类型同样是一类多键，它是按键分发的。

#### 修复
- **哈希重建的重启函数按任务键分回各自那条启动点**：回填重试回回填（沿用原来的发起理由、压低的批次
  与钉死的二进制哈希），前台重建重试回前台重建。
- **重试准入认的是活动态而不只是运行中。** 取消中与已暂停的任务同样占着运行槽位，此前对它们按重试
  会放行，只在重启函数恰好回到同一条任务键时才被下游闸门兜住——一类多键的类型上兜不住。

---

### 🏷️ v1.6.0 — 2026-09-03（暂停一小时去吃饭，回来速率掉到八分之一，ETA 从 2.6 小时变成 18 小时）

> 扫一个大库，中途暂停、稍后恢复，任务中心里的处理速率就从 60/min 掉到 8.6/min，ETA 从「约 2.6 小时」
> 变成「约 18.3 小时」——任务实际一点没变慢。数字从恢复那一帧起失真，一路失真到终态。
>
> 速率与 ETA 的分母是「开始时刻到此刻」，中间不扣任何暂停：等用户回来的那一小时被整段算成了在干活。

#### 修复
- **暂停时长从速率与 ETA 的分母里扣掉。** 引擎在每次离开暂停（恢复、取消、暂停中直接收尾）时把那一段
  折进任务的累计暂停时长，随任务落盘、读回后仍然算得对。它与 IO 面板上的 `paused_ms` 不是一回事：
  那个是磁盘作业为阅读让路而等掉的时长，期间任务本身仍在跑。
- **暂停中被取消的任务不再带着「暂停原因：手动暂停」进入取消中**：闸门在取消那一刻已被放行。

---

### 🏷️ v1.6.0 — 2026-09-03（服务重启后，中断的任务显示的是它停下前那句话）

> 服务重启后，任务中心里那条**中断**任务的消息行显示的是它停下前的「已暂停」或「正在扫描 vol01.zip」，
> 而「任务因服务重启而中断，可重试」一次都不会出现。若任务是在已暂停状态下被重启中断的，详情面板里
> 还挂着一行「暂停原因：手动暂停」。
>
> 重启恢复那笔批量 UPDATE 只盖了状态、消息与时间，任务参数一个字没动，而上一轮活动态的展示字段全在
> 参数里：文案码、文案占位参数、暂停原因与暂停时刻、可暂停/可恢复。前端按文案码优先渲染，那句中断
> 文案因此永远走不到。

#### 修复
- **转成中断时一并清掉上一轮的展示态**：文案码与它的占位参数、暂停原因、暂停时刻、可暂停/可恢复，
  以及可取消。
- **进度、阶段、当前条目、累计指标与重启函数要读回的入参一概留着**：中断是可重试的终态，用户要知道
  它断在哪，重试也要拿得回原始入参。
- **任务句柄的三条写入通道统一认活动态**：此前「加指标」那条只在运行中放行，暂停中与取消中收到的
  `io_wait_ms`／`paused_ms`／`hashed_files` 这些累加值被直接丢掉，且不会补回来。
- **总数未知的任务收尾后不再画那条来回跑的不定进度条**：它一个数字都答不出，只说「还在动」。

---

### 🏷️ v1.6.0 — 2026-09-03（AVIF 页开自动裁边，整页花屏，清缓存前刷新也不恢复）

> 页面是 AVIF 的书，打开阅读器的「自动裁边」后整页颜色错乱、糊成一片；坏字节还进了内存与磁盘页缓存，
> 刷新拿到的仍是同一张花屏。四条同时成立才会碰上：开了自动裁边、输出落在 avif、没给缩放尺寸、
> 且裁切框的左上角恰好落在 (0,0)——也就是页面只有右边或下边有留白。
>
> 裁切得到的子图会继承**父图**的行步长，而 avif 编码器对 `*image.RGBA` 直接把像素缓冲整段读走，
> 按「一行的字节数就是步长」解释，于是逐行错位。裁切后本有一道归一化专门防这件事，它的判据却只看
> 图像起点在不在 (0,0)：裁切框落在左上角时起点是干净的、步长不是，正好从判据底下漏过去。
> 绝大多数漫画页是 JPEG，解出来的像素类型走的是编码器的另一条安全通路，所以一直没被发现。

#### 修复
- **归一化的判据改成同时看起点与行步长**，行间有空隙的缓冲一律先重绘成紧凑的一张再交给编码器。
  喂给 AI 放大引擎的中间图走同一道归一化，同样不再花屏。
- **归一化的画布类型按源图选**，顺带把四边都有留白那条常见裁边路径也修快了：同一张 900x1300 的页，
  JPEG 源从 41.8 ms 降到 22.7 ms（每页分配次数 120 万降到 10 万），PNG 源从 39.2 ms 降到 33.6 ms。
- **页缓存键加了一位转码结果版本号。** 缓存键此前只描述请求，描述不了「这版代码会转出什么字节」，
  已经写坏的条目不会自行失效——浏览器侧的 ETag 也是从缓存键导出的。版本号一进，存量坏图整体失效、
  重算一次即可，旧文件由磁盘缓存的预算回收自然清走，不需要手动清空缓存目录。

---

### 🏷️ v1.6.0 — 2026-09-03（阅读器里选 Lanczos3 之类的图像滤镜，画面与不选逐字节相同，CPU 却白付）

> 阅读器设置里的 Lanczos3 / Bicubic / Mitchell / Lanczos2 / B-Spline / CatmullRom，选了之后画面与不选
> **逐字节相同**，但每页都要多付一次完整的解码 + 重编码，还丢掉了原始字节透传。
>
> 这些滤镜是重采样用的插值核，只在缩放时才有意义。阅读器按 CSS 把图缩到容器大小，从不向服务端下发目标
> 尺寸，服务端于是按源尺寸「重采样」一次——而同尺寸重采样在底层库里就是原样返回。

#### 修复
- **没有目标尺寸时不再假装加工**：这类请求识别为无操作，整条解码-重编码链直接跳过，原始字节透传。
  给了 `w`/`h` 的请求（封面与缩略图）滤镜照旧生效。
- **这类请求不再当作派生图写进缓存**，此前缓存下来的其实是一整张原始页图。
- 阅读器里这六个滤镜目前只剩浏览器侧的缩放质量提示（`image-rendering`）这一层作用，彼此等价。
  是否保留、或改成只在缩放场景下出现，是待定的产品决定，本次不动界面。

---

### 🏷️ v1.6.0 — 2026-09-03（离线读完的那一卷，联网补传后「继续阅读」还是停在更早读过的那一卷）

> 服务器时区不是 UTC 时，凡是走过离线队列补传的阅读进度都会错位：离线读完第 5 卷、联网补传后，看板与
> 系列页的「继续阅读」仍停在更早在线读过的第 3 卷；年度/月度回顾还会把同一次阅读劈成两段——「翻过的页」
> 算进 10 月、「读完的书」算进 9 月。库内「最近阅读的系列」的顺序同理错乱。
>
> last_read_at 落库的是一串带时区的墙钟文本，而所有排序与期间筛选都对这一列做 SQL 文本比较。在线写入
> 落的是服务器本地墙钟，补传却把客户端报上来的 ISO 8601 原样落库——那是 UTC。UTC+8 下同一瞬间的两种
> 写法差 8 小时，补传那条要比在线那条晚 8 小时以上才抢得回位置。TZ=UTC 的容器碰巧两种写法同格式，不受影响。

#### 修复
- **外部报上来的时刻一律先归一到服务器本地时区再落库。** 归一收在数据访问层的进度写入口，
  离线补传、单用户全局进度、KOReader 与今后任何客户端都走同一道口子，前端继续发标准的 ISO 8601（带 Z）
  是对的，不必也不该改。
- **已经写坏的行在启动时改回本地墙钟**，并重算派生的「继续阅读」与系列统计。判据是文本里有没有
  Go 时刻串的单调钟后缀：本服务自己写的时刻必带它，客户端报上来的不带——真正跑在 UTC 的服务器因此
  不会被改坏，它的行本来就正确，改写结果也与原文逐字相同。
- **在线写入的文本一个字节都没变**，新旧行之间的比较不受影响；归一只对不在本地时区的值动手。

---

### 🏷️ v1.6.0 — 2026-09-03（扫描等级设成 fast 后改一个文件名，这本书的页数与封面就归零且再也补不回来）

> fast_scan 不打开归档，页数与封面它一个都读不出来。扫描器本来有一道保留：读不出来就沿用库里那份。
> 可这道保留是按**路径**去查旧值的，而改名恰恰是路径变了——改名重连保住了 books.id，却正好绕开了这半边。
> 于是改名后的书页数变 0、封面变空；只要还停在 fast 档位，之后每次扫描又因修改时间与大小没变而跳过它，
> 永不自愈。阅读进度百分比、读完判定与系列总页数一并算错。切回其它扫描等级才能修回来。

#### 修复
- **「该不该保留」的判据从「这条路径的旧值是多少」改成「这一趟到底开没开归档」**，并且落在入库那一步：
  开过归档的扫描写页数与封面，没开的那趟根本不碰这两列。改名的书写的是改名重连刚改挂过去的那一行，
  判据因此不再受路径变化影响。
- **判据不看读出来的值是不是空的。** 归档真被换成一页都没有的版本时，页数照样归零——「读出来是 0」
  与「压根没读」是两回事，混在一起会让系列统计一直挂着一个再也不存在的页数。
- **fast 档位仍然只跳过没变的文件**，没有因此退化成每次全量重读。

---

### 🏷️ v1.6.0 — 2026-09-03（自己换过的书封面，点一次「重新扫描该系列」就变回自动封面）

> 在书详情页上传封面、或指定某一页作封面之后，只要点一次「重新扫描该系列」或「强制全量读取」，封面就退回
> 自动生成的第一页缩略图。「重新扫描该系列」这个按钮看起来无害，发出去的却是强制扫描，所以这不是边角场景。
>
> 自动缩略图按「路径 + 修改时间 + 大小」命名，用户设的封面按图片内容命名，两个名字永不相等，扫描于是认不出
> 哪一张是人设的：自动那张还在盘上就把封面改回自动那张，不在了就把封面清空、再重新生成一张。

#### 修复
- **用户设的封面带上封面锁**，扫描入库一律不得改写它。这是书这一侧的锁定字段，与系列元数据上那套
  「用户要保住的内容靠锁挡」是同一个意思。
- **锁只由「设为封面 / 上传封面」置上。** 扫描自己从不置锁，没被动过的书照旧跟着文件走——归档内容换了，
  自动封面跟着换。
- **「重建缩略图」会把锁解开。** 那个任务把缩略图文件连同用户上传的那张一并删光，锁留着只会让这本书的
  封面永远空着。

#### 后续
- **升级之前设过的封面还没有锁。** 库里已有的 cover_path 分不出是自动生成的还是人设的——两者都只是一串
  哈希文件名，唯一能拿来猜的就是「自动封面按路径+修改时间+大小命名」这条约定，而它认错时会把一张**过期的
  自动封面**永久钉死在那本书上，且悄无声息。所以升级不做回填：这些封面下一次强制扫描仍会被冲掉一次，
  重新设一次即可永久保住。

---

### 🏷️ v1.6.0 — 2026-09-03（写入 ComicInfo 到文件，把归档里原有的字段抹掉了）

> 「写入 ComicInfo 到文件」自述的是提升数据可迁移性，实际是换软件就丢：回写从不读归档里已有的那份
> ComicInfo.xml，而是拿本项目建模的结构体重新生成一份整个换掉。一份 ComicTagger 打好标的 28 元素文档
> 回写后只剩 13 个——出版日期、其余署名、阅读方向、分级、角色、扫描信息全没了。这些字段本项目既不显示
> 也不给编辑，用户无从知情；写入是原子替换且不留备份，只能从原始文件重新拿回。
>
> 连本项目自己已经建模的字段也保不住：Manga / Web / Rating 一个都不填，`omitempty` 于是把它们从输出里
> 删掉；这一卷没有嵌字或翻译时，Letterer / Translator 同样蒸发。
>
> 还有一处是往用户文件里写**错误数据**：`Count` 是 ComicRack 的「本系列共几卷」，回写却拿「资料库里收了
> 几卷」去填——只收藏了 3 卷时，原本正确的 `Count=20` 被改成 3。

#### 修复
- **回写改成节点级合并，不再整份重建。** 归档里已有的 ComicInfo.xml 当基底，本项目只覆盖自己确有值的
  元素；其余元素连同原有顺序、注释、命名空间声明与 `<Pages>` 子树原样留下。「本项目管哪些字段」不再由
  某个函数的填写清单界定，而由这次写入的值是否非空界定——填不出值的字段自然由归档原值兜住。
- **本项目里为空的字段保留归档原值，不清空。** 本项目不提供「清除该字段」这个意图：空既可能是没刮到、
  也可能是用户删过，两者不可区分，而误留可以再改一次，误删不可逆也没有备份。
- **不再写 `Count`。** 本项目没有「本系列共几卷」这个数据，原文件有就留着，没有就不加；导出的
  ComicInfo.xml 同样不再带这个字段。
- **新增的元素按 ComicInfo.xsd 的 sequence 顺序插入。** 乱序会让按 schema 校验的阅读器读不出内容。
- **原有文档不是 UTF-8 或语法有误时拒绝写入，归档保持原样。** 本项目不带转码器，硬改写会把原有的中日文
  正文变成一串替换字符。单本回写返回 415，整系列回写按「跳过」计，不算失败。
- **前端二次确认改口径**：说明这是合并写入、只覆盖本项目管理的字段，并点明不可撤销且不留备份。

#### 后续
- 回写仍不留备份。合并之后原有字段的损失已经为零，备份的收益随之变小，而它要连带定清理策略与磁盘占用，
  属于另一个决定，本轮不做。
- 本项目仍不建模出版日期、分级、角色这些字段——本轮的目标是不丢用户已有的数据，不是让本项目支持它们。

---

### 🏷️ v1.6.0 — 2026-09-03（收件箱里批量「只填空字段」，把用户手写的简介盖掉了）

> 「只填空字段」是收件箱批量条的默认模式，它的承诺是不覆盖系列上已有的数据。判「空不空」看的却是提案
> **入队瞬间**写进字段行的那份当前值快照，而快照此后没有任何路径会刷新。于是有两条路都能让它变成覆盖：
> 入队之后用户在系列详情页手工填了简介、出版社或评分，快照仍是空的；同一个系列上有两条提案（换个数据源
> 再刮一遍就有），一次全选应用，后一条按自己那份空快照，把前一条刚写进去的值盖掉。
>
> 收件箱的 diff 面板展示的也是这份快照：用户看到「当前值：(空)」，其实那个字段早就填好了。

#### 修复
- **「空不空」改按系列此刻的值算。** 与锁定字段同一个口径——那一条本来就是每次裁决按系列当前状态重算的，
  同一个流程里不该再有第二种口径。
- **「当前值」在提案模块里收敛成唯一定义。** 入队写进字段行的、裁决判空用的、收件箱与系列详情页展示的，
  都出自同一处；将来多一个字段却忘了给它当前值，用例会变红。
- **集合字段的「空」是「系列上一个成员都没有」。** 标签与作者的写入是整体替换：系列上有 3 个标签、提案给
  5 个，写进去等于先把原来那 3 个删掉，因此「只填空字段」跳过它；一个成员都没有时照写。
- **批量应用仍是逐条各自一次事务**，前一条写进系列的值对后一条立即可见——这正是「不覆盖已有数据」在一次
  全选里也成立的前提。
- **收件箱与系列详情页展示的当前值一并改成实时算的。** 只修写入侧的话，用户会看到一个「当前值：(空)」却在
  应用时被跳过的字段。收件箱一页跨多个系列，标签与作者一次批量取回，不逐个系列查。

---

### 🏷️ v1.6.0 — 2026-09-03（提案字段挂着锁徽章，点应用却照样被写进系列）

> 老库里可能留着「入队瞬间已锁定」的字段行快照——那时的版本会把已锁定的字段照样入队，并在行上记一个
> 锁定标记。收件箱与系列详情页把这个标记与系列当前的锁定字段取或之后再渲染，应用那边却只认当前的锁定
> 字段：用户看到锁徽章、以为点应用不会动它，点下去它被写进了系列。反过来，用户把该字段解锁之后，徽章
> 还是一直挂着，锁定计数也一直算它。新装的库不会有这种行。

#### 修复
- **展示与应用统一到「只认系列当前的锁定字段」。** 行上的标记是入队瞬间的快照，而锁定字段根本不会入队，
  新行恒为未锁定，留在老行上的那些只是陈旧数据。反过来统一（展示与应用都取或）意味着老库里那些字段
  永远应用不了、解锁也没用，只能驳回——那不是用户要的。
- **启动时把老行上遗留的锁定标记清掉。** 语句幂等；留着它，下一个读到的人会把陈旧数据当成一条裁决规则。

---

### 🏷️ v1.6.0 — 2026-09-03（资料库根本身是软链时，扫描报「完成」却一本书都不入库）

> 把资料库根填成一个指向目录的符号链接（`~/Manga -> /Volumes/ext/Manga`，或迁移之后在原地留一条链），
> 建库校验会通过——它用 `os.Stat`，跟进软链后确实是目录。但遍历一层都不下降：`filepath.WalkDir` 对自己的
> 起点也只做 `lstat`，拿到的是链接本身而不是目录，于是回调把它当软链攒起来准备事后跟进；跟进那一步解析出
> 真实目录，却撞上遍历开头**已经用同一个真实目录登记过的那笔去重记录**，直接返回。结果是回调一次都没被
> 调用、错误为空，扫描当作成功收尾：用户看到「扫描完成」，`discovered_archives` 是 0，整库内容静默不可见。
>
> 同一个形状还有另外两处出口：**系列目录是软链**时（多盘位 NAS 把外置盘目录软链进库根，正是被鼓励的组织
> 方式），系列详情页的「重新扫描该系列」起点就是这条链，于是永远「0 本新增」；**新建的软链系列目录**被扫描
> 观察者看见后走的也是这条遍历，注册到 0 个目录，而一个目录都没注册上时监听还不报错——用户以为热重载开着。
>
> 边界：清理用 `os.Stat` 判存在、它跟进软链，所以换成软链根**不会**误删已入库的书，后果只是内容不可见与
> 热重载失效。数据库结构与线上契约均未改动。

#### 修复
- **交给遍历的起点先解链，报出去的路径再改写回调用方那一侧。** 去重登记的仍是解析后的真实目录，所以指回
  起点的软链照旧被挡在去重集合外，不会走第二遍；而调用方看到的路径仍落在它给的根之下——库归属、系列派生
  与 `books.path` 都依赖这一点，报真实路径会让这些书归属到库外，删库与清理都定位不到它们。
- **没有改成「起点不登记」或「跟进时跳过去重」。** 那两种同样能让软链根扫得到，但会把已修的重复入库放回来：
  前者让 `<库根>/self -> <库根>` 这类指回起点的链把根这一层的散装归档再入库一遍，后者让两个链接指向同一
  目录时那批文件被走两遍。两侧的用例现在都在仓库里，改回任何一种都会变红。
- **解不开链时按原路径继续，不报错。** 断链、权限不足、以及 Windows 上解析失败的路径都退回原来的行为，
  由遍历自身的错误回调处置——与扫描「宁可少扫不可误删」的既有姿态一致。

---

### 🏷️ v1.6.0 — 2026-09-03（外部库路径填成软链就能绕过「不得与资料库根重叠」这道闸）

> 外部库不得落在资料库根之内：传输出去的副本会被下一次扫描收编成重复书籍，用户还得手动去重。这道闸只比
> **字面路径**，而外部库路径填成符号链接是常态（`/mnt/external`、macOS 的 `/tmp/...`、NAS 挂载点）。链接
> 目标落在库根内时字面上毫无关系，闸就放行了，会话正常建立，副本物理落进库根。
>
> 同一个「这是不是同一个目录」的问题，扫描侧早已按解链之后判定，外部库侧没有——两端口径不一致。

#### 修复
- **重叠判定改为字面与解链后各判一次，任一成立即算重叠。** 字面那次不能省：库内一条指向库外的链同样在扫描
  的遍历范围里，传进去的副本照样会被收编。
- **解不开链时只剩字面那次比对，不报错。** 「填一个待创建的目录」是正常情形，不该因为 `EvalSymlinks` 失败
  而被拒。库行指向一块已经拔掉的盘时同理。
- **未共用一个跨包的判定函数。** 两侧问的不是同一个问题：扫描侧要的是遍历去重用的目录身份（配合 `os.SameFile`
  在大小写不敏感的文件系统上定夺），外部库侧要的是两条路径的包含关系。共用的只有「尽力解链、解不开就原样
  交回」这一句，为它提一个公共包不划算。

---

### 🏷️ v1.6.0 — 2026-09-03（OPDS/Mihon 侧的站点口令可以被无限次在线猜）

> 网页登录侧，同一个账号连错 5 次即锁定，且跨来源 IP 生效。协议侧（OPDS/Mihon）用的是同一个
> 站点口令、同一条校验路径，失败计数却只按**来源 IP** 分一格：换 IP 猜同一个账号，一次都不会被
> 拦。更要命的是校验成功后会把整格 IP 计数**整条删掉**——攻击者手上只要有任意一个有效账号（一个
> 普通家庭成员账号就够），在单个 IP 上每猜几次穿插一条自己的正确凭据，计数就永远归零，于是不换
> IP、不用代理池也能无限次猜管理员的口令。
>
> 同一格还带来反向的误伤：一台阅读器存着过期口令把这格打满，同一出口 IP 后面的其它家庭成员跟着
> 一起吃 429。

#### 修复
- **失败计数改按三把尺子分格**：`(来源 IP, 用户名)` 挡单机爆破，也让「一台设备存着过期口令」只锁
  它自己那一格；`用户名` 跨来源 IP 生效，挡换 IP 打同一个账号；`来源 IP` 只作口令校验的 CPU 闸门。
- **一次成功只清它证明了的那两格**：这台设备为这个账号存的口令、以及这个账号本身。共享的 IP 闸门
  不再被任何一次成功清零——否则穿插一条有效凭据就能把它擦干净。
- **协议侧的用户名格与网页登录的刻意分开**：协议客户端会拿保存的旧口令自动重试，共用就意味着用户
  自己的阅读器能把他锁在网页端门外，而网页端正是他唯一能把口令改回来的地方。
- **没带凭据的请求不再计入失败**：HTTP Basic 本就是「先吃一个 401、再带凭据重试」，那一步一次口令
  校验都不跑，把它算成失败等于把协议自身的握手当成攻击。

#### 升级提示
- **无需迁移，可直接升级或回滚**：不动表结构、数据与接口契约。协议侧此后会出现按账号的 429 锁定
  （此前只按来源 IP）；阅读器换上正确口令后，第一次成功即解除。

---

### 🏷️ v1.6.0 — 2026-09-03（后台任务的完整快照经实时事件流推给了每一个登录用户）

> 任务列表接口只对管理员开放，普通账号请求它拿到的是「仅管理员可执行此操作」。可**同一份任务
> 记录**还有第二条出口：全站实时事件流把每一帧任务快照原样广播给所有在线连接。快照里带着作用域
> 显示名、任务参数与失败原因，而失败原因常常是一句带宿主机**绝对路径**的系统错误
> （`/srv/private-manga/资料库A/秘密.cbz: permission denied`）。于是普通成员只要开着任意一个页面，
> 管理员跑的每一个后台任务都会把服务器上的目录结构送进他的浏览器——那条 403 等于没有。
>
> 事件流不能整条改成管理员专属：普通用户界面靠它收「管理员扫完库了，你这边的列表该刷新了」这类
> 帧，关掉就是拿一个泄露换一个功能故障，还会让前端反复重连一个必然 401 的端点。

#### 修复
- **事件按角色分档投递**：订阅登记时带上这条连接的角色，任务快照走「仅管理员」这一档，刷新类
  事件仍投给所有人。普通用户的实时刷新一如既往，任务快照一帧也收不到。

#### 升级提示
- **无需迁移，可直接升级或回滚**：不动表结构、数据与接口契约。普通账号此后收不到任务进度帧——
  他们本就无权查看任务，界面上也没有对应入口。

---

### 🏷️ v1.5.1 — 2026-09-01（监听器停机可能在它刚派出去的扫描仍在跑时就返回）

> `FileWatcher.Stop` 的契约是「返回即代表事件循环与本 watcher 派出去的 `ScanLibrary` /
> `CleanupLibrary` 都已退出」——优雅关闭正是靠它保证没人再往一个即将关掉的 store 里写。可它等的
> 那个 `WaitGroup` 只在事件循环一侧被加人，加人处由 `fw.mu` 罩着，`Wait` 却在锁外。关掉停机信号
> 并不足以让事件循环立刻停手：`select` 在停机信号与轮询节拍同时就绪时随机挑一支，于是「计数刚
> 归零、Stop 开始等」与「节拍到点、又派出一份工作」会撞在一起。撞上时 Stop 立刻返回，被它漏掉的
> 那次扫描/清理仍在正在关闭的 store 上跑；这对无序的读写也可能被运行时判成 `WaitGroup` 误用而
> 直接 panic。
>
> 窗口只有一次派发那么宽，生产的 2 秒轮询下极难撞上，因此表现为进程退出时偶发的写入报错或崩溃。

#### 修复
- **Stop 先与事件循环在 `fw.mu` 上握一次手，再去 `Wait`**：拿到锁即证明没有派发正在进行，新增的
  `stopping` 标志置上之后 `dispatchDueLocked` 也不会再往 `inFlight` 里加人。加人与等待之间因此有了
  确定的先后，Stop 返回时不可能还剩下漏网的扫描或清理。

#### 升级提示
- **无需迁移，可直接升级或回滚**：不动表结构、数据与接口契约，改的只是停机路径上的同步。

---

### 🏷️ v1.5.1 — 2026-09-01（智能书架固化的重名查重在事务外，被抢先一步就能建出第二个同名合集）

> 「哪里都不许出现两个同名合集」这条不变量有三条写入路径。建合集与 AI 分组应用都把查重放进了
> 写入的那个事务里，**智能书架固化**这条没跟上：它先查一次重，再另开事务建合集。两者之间的窗口
> 没关——另一个标签页（或另一个人）恰在这一瞬间用同一个名字建号或固化，两笔各自查重都通过，
> 列表里于是并排出现两个同名合集，用户分不清哪个是哪个。这条路径的注释一直写着「不能只在
> preview 端点提示，否则会得到两个同名合集」，防的正是这个结局，只是防线短了一截。
>
> 固化请求不带名字时回退到智能书架自己的名字，那条回退路径没去首尾空白：查重时裁、落库时不裁，
> 两头判的不是同一个字符串。书架名在它自己的写入口已经裁过，因此要库里存着一个带空白的旧书架名
> 才踩得到，但这条路径的口径本身就是错的。
>
> 固化弹窗对 `409` 没有任何交代：响应被静默吞掉，弹窗不关也不提示，用户只会反复点「创建快照」。

#### 修复
- **查重移进 `ExecTx`**，与建合集、AI 分组应用同一个写法：store 的 DSN 带 `_txlock=immediate`，
  `BeginTx` 即取写锁，并发的固化与建号因此被串行化，不会各自读到「不存在」再各建一个同名合集。
  `collections.name` 上没有唯一约束兜底，这道串行化就是这条不变量在固化入口的全部保障。撞名
  整笔回滚并返回 `409`，成员一条也不会留下。
- **事务外那次查重删掉**：同一件事判两次不多一分保障——它读的是随时会过期的旧快照——留着反而让
  下一个读代码的人以为窗口已经关上。preview 端点那次查重不动：它是只读的建议性提示，本就不写库。
- **回退到书架名时也去首尾空白**，与请求带名字时同口径。
- **固化弹窗收到 `409` 时提示重名并保持打开**，用户改个名字就能重试。预览里那句「仍可继续创建
  快照，但它会作为独立合集出现」与服务端行为不符（提交会被拒），改成提示换个名字。

#### 升级提示
- **无需迁移，可直接升级或回滚**：本次不动表结构与数据，接口契约也不变——撞名此前此后都是 `409`，
  变的是它现在挡得住并发。

---

### 🏷️ v1.5.1 — 2026-09-01（离线下载收尾的最后一段仍会把刚删掉的书写回索引，僵尸书重现）

> 「下载中删掉的书又复活成读不了的书」那条修复只堵到了收尾检查那一步：检查通过之后还要清扫这本书
> 名下的陈旧缓存键，而清扫要先列一遍整个 Cache Storage 再逐个删，缓存大时这段不短。用户恰好在这段
> 时间里删掉这本书（或点「清空全部离线书」），收尾最后那句写回不问归属，把整条记录连同 urls 与
> 下载令牌一起塞回索引——字节已经被删除清掉了。离线书架上于是又留下一本读不了的书，点开进不到
> 阅读界面。窗口比上一条窄，但触发条件一样日常：下载快跑完时点删除。清扫本身不会挖走并发重下刚
> 写下的页——页图的缓存键去掉了 query，而画质、格式、滤镜、自动裁边全都只落在 query 上，换个画质
> 重下同一本书得到的是同一批键，被作废的那次清扫保留的正是新下载写下的那些。

#### 修复
- **收尾改为「只更新当前索引里仍带着自己令牌的那条」**，而不是把下载开始时那份记录整条覆盖回去。
  只补一次归属检查不够——检查与写回之间还隔着一次索引重读，仍留着窗口；现在读、判、写连成一段
  中间不等待任何异步操作的动作，删除与「清空全部」都插不进去，作废时改为如实回报「这本书不在本机」
  并把本次写下、当前索引没有认领的字节删掉。
- **收尾最后读回状态时再判一次归属**。这一步同样要等 Cache Storage，中间被删掉的话，此前会拿一个
  按本次下载凑出来的「已缓存」状态交给阅读器，界面把刚删掉的书显示成还在本机。现在这种情况一律
  回报「不在本机」，界面回到「未缓存」。

#### 升级提示
- **无需迁移，可直接升级或回滚**：纯前端逻辑，不动表结构，也不动任何接口契约。
- **已经中招的僵尸条目不会自愈，要手动清一次。** 修复只堵住新的下载，本机索引里已有的那条记录
  照旧留着：离线书架仍会列出这本书（已缓存页数是实时数，所以它显示成 0/N 而不是满格），点开读不了。
  在离线书架上把这本书**删掉**即可清干净（索引与残留字节一起清），需要的话再重下一次；也可以用
  「清空全部离线书」一次清完。

---

### 🏷️ v1.5.1 — 2026-09-01（系列元数据保存：链接写失败被吞掉，空版本号把并发校验静默关掉）

> 手工保存系列元数据这一笔事务里，「先清空、再重建」的三块只有两块守住了自己那段注释。标签与
> 作者每个错误都向上返回让 `ExecTx` 回滚，链接那块的 `ClearSeriesLinks` 与 `LinkSeriesLink`
> 却把错误丢给了 `_`：清空成功而重建失败时事务照样提交，用户的链接被静默清空、接口还返回
> `200`。同一个函数里的并发校验则是选择性的——`expected_version` 是空串就整段跳过，而唯一的
> 前端入口发的是 `formVersionRef.current || ''`。详情载入失败、时序没赶上、将来某个新入口忘了
> 带，请求照发、服务端照收，校验静默关闭，退回后写覆盖先写，界面上那句「有人改过了」再也不会
> 出现——用户以为自己受着保护。

#### 修复
- **链接的清空与重建改为向上返回错误**，与同一段里的标签、作者同口径：任一条写不进去就整笔
  回滚，接口报错而不是回 `200`。`series_links` 上没有唯一约束，重复链接本来就是合法写入，
  这一改不会把原本能过的保存变成 `500`；空 name / 空 url 仍按老规矩静默跳过。
- **`expected_version` 改为三态**：字段缺席仍是明示的「不参与并发控制」，脚本与旧客户端照旧
  放行；带了字段就必须带真版本，**空串返回 `400`**。空串只会来自「本该带版本却没拿到」的调用
  方，放行等于把防护栏静默关掉。
- **前端拿不到版本就不保存**：详情页的保存先要版本，没有则弹错误提示让用户刷新重试，不再把
  空串发出去。

#### 升级提示
- **无需迁移，可直接升级或回滚**：本次不动表结构与数据。
- **接口行为有变**：`PUT /api/series/info/{seriesId}` 在 `expected_version` 为空串时，由静默
  跳过校验改为 `400`。不参与并发控制的脚本请**整个省掉这个字段**，不要传空串。

---

### 🏷️ v1.5.1 — 2026-09-01（阅读清单增/删/排三个入口对「目标不存在」给出三种结局）

> 同一个资源的三个兄弟接口，对着一个不存在的 id 分别返回 500、200、200。加系列时不校验清单
> 在不在场，不存在的 `listId` 直接撞 `reading_list_items` 的外键，兜底成 `500`——同一个请求里
> 两个 id，系列错了报 `404`，清单错了报服务端故障。删条目与重排的 SQL 都是 `:exec`，丢掉影响
> 行数：删一个不属于这个清单的 `itemId` 返回 `200 {"status":"removed"}`，重排里混进一个别的
> 清单的 `itemId` 则那条 UPDATE 静默 no-op、整批仍返回 `200 {"status":"reordered"}`。
> 前端把这两个 `200` 当成改动已保存，把条目划掉、把顺序换过来，刷新后原样回弹。
> 重排入口自己的注释早就写过这个毛病，只是它只在清单这一层挡住了，没挡条目这一层。

#### 修复
- **「清单不存在」三处统一返回 `404`**。加系列不再靠外键兜底：清单与系列都先查在场，缺哪个报
  哪个的 `404`。外键那条 `500` 兜底保留，兜的是真正的异常。
- **删除条目影响 0 行返回 `404`**，与删合集同口径。这条 `DELETE` 分不清「条目早就没了」和
  「这个 `itemId` 属于别的清单」，回 `200` 就等于对后一种情况谎报成功。
- **重排遇到不属于本清单的条目返回 `404` 并整批回滚**。只覆盖清单里的一部分条目仍然合法
  （并发新增会让前端手里的快照少一条），没送到的条目保留原 `sort_order`。
- **加系列的两次在场校验与插入合进同一个事务**。store 的 DSN 带 `_txlock=immediate`，
  `BeginTx` 即取写锁，因此「查到在场」与「插入」之间不再有别人删掉清单或系列的窗口。
- **前端不再无条件当作成功**：移除失败时条目留在原位，重排失败时顺序退回去，两者都弹出错误提示。

#### 升级提示
- **无需迁移，可直接升级或回滚**：本次不动表结构与数据。
- **接口状态码有变**：`POST /api/reading-lists/{listId}/items` 在清单不存在时由 `500` 改为
  `404`；`DELETE /api/reading-lists/{listId}/items/{itemId}` 与
  `POST /api/reading-lists/{listId}/items/reorder` 在目标条目不属于该清单时由 `200` 改为 `404`。
  把删除的 `200` 当成「反正已经没了」的第三方脚本需要改判。
- OPDS 与 Mihon 侧只读阅读清单，不受影响。

---

### 🏷️ v1.5.1 — 2026-09-01（建合集不去首尾空白、不查重名，AI 分组应用同样漏查）

> 「哪里都不许出现两个同名合集」这条不变量，重命名与快照固化两条路径都在维护，唯独**建合集
> 这个正门**没有：请求只挡了真空串，`"   "` 直接建出一个在列表里看不出名字的合集，`" 科幻"`
> 与 `"科幻"` 并存成两条记录，`Sci-Fi` 与 `sci-fi` 也一样。重命名路径的注释声称与固化路径
> 共用同一判定，而这条不变量在入口处就是破的。
>
> 同一个漏洞在 AI 分组审核的候选合集应用路径上也在：候选名由 AI 给出，落地的却是普通合集，
> 一次应用就能把一个与既有合集同名（甚至同一批里两条互相同名）的合集放进列表。

#### 修复
- **建合集与重命名同口径**：名称先去首尾空白再判空，纯空白名返回 `400`；重名返回 `409`，
  与重命名、快照固化共用 `CollectionNameExists` 那条判定（SQL 为 `name = ? COLLATE NOCASE`，
  即 ASCII 大小写不敏感）。
- **查重与插入放进同一个事务**。store 的 DSN 带 `_txlock=immediate`，`BeginTx` 即取写锁，
  两个并发建号请求因此被串行化，不会各自读到「不存在」再各插一条同名合集。
- **AI 分组应用候选合集时同样查重**，判定落在事务内的 SQL 上，因此整批应用时先建的那几条也
  算数——同一批里两条同名候选，第二条会被挡下。撞名整批回滚并返回 `409`，用户改名或驳回那条
  候选后重来。
- **描述字段的首尾空白一并裁掉**（建合集与重命名两处）。此前 `"   "` 会落成一个非 `NULL`
  却渲染为空的描述，绕过了「空描述存 `NULL`」这条约定。

#### 升级提示
- **无需迁移，可直接升级或回滚**：本次不动表结构与数据。
- **库里已有的带空白名或重名合集不会被自动清理**——服务分不清它是这个缺陷的产物还是用户
  有意为之。可用
  `SELECT id, name FROM collections WHERE name <> TRIM(name) OR EXISTS (SELECT 1 FROM collections o WHERE o.id <> collections.id AND o.name = collections.name COLLATE NOCASE);`
  找出来，逐条改名或删除。
- 查重比的是**库里存着的名字**，不会先 `TRIM` 一遍。若库里遗留一条名为 `" 科幻"` 的合集，
  新建 `"科幻"` 仍然建得进去；把遗留的那条改名后即可恢复一致。
- 作品群（`system_franchise`）合集也在查重范围内，且它不可人工改名。若 AI 给出的候选名与某个
  作品群重名，应用会被拒——改候选名即可。

---

### 🏷️ v1.5.1 — 2026-09-01（用户名存不存在能从登录耗时上看出来；仅大小写不同的两个账户共用一个失败计数桶）

> 两处都出在「账户身份的口径」上。
>
> 其一，网页登录与阅读协议（OPDS/Mihon）的 Basic 鉴权用的是同一个写法：账户查不到时拿到的是
> 零值 `User`，空口令哈希在 bcrypt 看来格式非法，于是**一次 KDF 都不跑**就返回；命中的用户名
> 则要走完整轮 bcrypt。两条分支的开销差一个数量级，而 bcrypt 慢是**故意**的——正因为慢，
> 这个差就足以把「这个用户名存不存在」变成一个可观测的事实（用户名枚举）。协议那条更暴露：
> 客户端每个请求都带凭据。
>
> 其二，账户按用户名**字节精确**判重（`Alice` 与 `alice` 是两个账户，收藏、阅读进度、离线授权
> 全按 `user_id` 分家），登录失败计数的分桶键却折叠了大小写。于是两个独立账户共用一个桶：
> 对着其中一个连续失败，另一个跟着被锁在门外。`CreateUser` 上那句「大小写冲突由调用方预检」
> 的注释描述的是一个不存在的机制——两个建号入口都没有预检，而且精确匹配的查询本就挡不住变体。

#### 修复
- **口令校验的开销不再随用户名是否存在而变。** 账户查不到时改用一个进程启动时现算的占位哈希
  走完同一次比对，其 cost 与真实账户的哈希取自同一个常量，不会各自漂移。网页登录与协议 Basic
  鉴权收敛到同一个校验入口，两边同口径。
- **登录失败计数按精确用户名分桶**，与账户身份同一把尺子。仅大小写不同的两个账户互不牵连，
  同一个用户名连续失败仍照常锁定。

#### 升级提示
- **无需迁移，可直接升级或回滚**：本次不动表结构与数据。
- 用户名的口径正式定为**字节精确**：`Alice` 与 `alice` 依旧是两个独立账户（此前即如此，本次
  只是把限流分桶也拉回同一口径）。为什么不改成大小写不敏感、以及那条路上的升级风险，
  记在 `docs/adr/0003`。
- 协议侧用不存在的用户名发起的请求，现在也会消耗一次 bcrypt。按来源 IP 的失败限流（5 分钟内
  10 次失败即锁定）本就在这条路径上，成本仍然有界；把该限流关小或让反向代理不透传真实客户端
  IP，会同时削弱这道防护。

---

### 🏷️ v1.5.1 — 2026-09-01（AI 分组审核的候选合集可能被应用两次，收尾那条还不报新建合集 id）

> 「应用候选合集」的待应用检查在事务外做，事务内没有对应守卫：两个请求只要都读到同一份
> 待应用快照——两个标签页、双击穿透按钮的禁用、或「整条审核应用」与「单条候选合集应用」
> 撞车——就会各建一个同名合集。先建的那个还会被后写的 `created_collection_id` 顶掉，
> 从此在审核记录里无迹可寻，只能在合集列表里认出来。元数据提案那条裁决通路早有这道
> 事务内 CAS，AI 分组这条没有。
>
> 同一个函数里还有第二处：新建合集的 id 在「本条是最后一条待应用」的提前 return 之后才被
> 记下，于是收尾那一条永远报不出 id——而「整条审核只有一条候选合集」恰恰是最常见的形态，
> 应用完提示会变成「已创建合集 #」，点不到刚建出来的东西。

#### 修复
- **一条候选合集只可能被应用一次。** 状态推进改为事务内 CAS，抢先失败时整批回滚，
  已建的合集与成员一并撤销。单条与整条审核两个入口同口径返回 `409`，与它们各自的前置检查
  是同一个结局：「已经被别人处理过了」对用户就是一件事，刷新后重来即可。
- **应用成功必带新建合集的 id**，含只有一条候选合集的审核。

#### 升级提示
- 修复只挡住新的重复，**已经建出来的重复合集不会被自动清理**——服务分不清它是这个缺陷的
  产物还是用户自己建的同名合集。可疑对象是同名、`source_type = 'ai_grouping'`、且未被任何
  审核记录引用的那一个（失联的先建者）：

  ```sql
  SELECT id, name, created_at FROM collections
  WHERE source_type = 'ai_grouping'
    AND id NOT IN (SELECT created_collection_id FROM ai_grouping_review_collections
                   WHERE created_collection_id IS NOT NULL);
  ```

  审核随资料库删除后，其名下的合集也会落进这张表，所以请照着结果在合集列表里逐个确认再删，
  不要直接批量删除。

---

### 🏷️ v1.5.1 — 2026-09-01（强制改密只在浏览器一侧执行，协议客户端不受约束）

> 「管理员分配初始口令、用户首次登录必须改密」这条约束只在浏览器会话那条鉴权路径上执行。
> 阅读协议（OPDS / Mihon）走的是另一条 HTTP Basic 路径，它不看这个标志；KOReader 同步链
> 用的是账户自己的同步密钥，也从不过问所绑定站点账号的状态。结果是：多用户部署下，尚未
> 完成首登改密的账号可经协议侧绕过该约束——它本该只能去改密，实际却能长期读取全库，Mihon
> 那侧还能写回阅读进度。
>
> 约束早写在代码注释里，却只在一条路径上落地。**现在三条非浏览器链路同口径拒绝**，且读写
> 一并拒绝：初始口令的分发方式本就在这条约束的假设之外，只拦写操作等于默认「读全库」无所谓。
>
> 拒绝的原因得能在客户端看出来。Basic 鉴权的响应体多数阅读器不显示，所以 OPDS / Mihon 的
> `401` 把原因写进 `WWW-Authenticate` 的 realm——阅读器的口令弹窗会显示这行字；用 `401` 而不是
> `403`，是因为用户改完密码后在同一个弹窗里填新口令就恢复了，而 `403` 在阅读器上是条死路。
> KOReader 没有「换个凭据重试」的语义，返回 `403` 并在消息里说明，同时在设置页的 KOReader
> 诊断里落一条可查的失败记录，免得表现成「某台设备忽然不同步了」。

#### 修复
- **未完成首登改密的账号不再能经 OPDS / Mihon 读取书库或写回阅读进度。** 返回 `401`，真实原因
  写在 `WWW-Authenticate` 的 realm 里。
- **绑定到这类账号的 KOReader 同步账户一并停用**，返回 `403`；未绑定站点账号的自助注册账户
  不受影响（其进度本就回落全局，不归属任何账号）。

#### 升级提示
- 如果你站点里有账号从建好起就没在网页端改过密码、却一直用 OPDS / Mihon / KOReader 在读，
  升级后这些客户端会立刻失效。恢复办法是**用同一个账号登录网页端、按提示完成改密**，再把新
  口令填回阅读器；KOReader 的同步密钥不变，改密后自动恢复。管理员可在设置 → 用户管理里看到
  哪些账号还标着「待首次登录改密」。

---

### 🏷️ v1.5.1 — 2026-09-01（`-config` 指向不存在的目录，首启以一句临时文件名退出）

> 把 `-config` / `MANGA_MANAGER_CONFIG` 指向父目录尚不存在的位置——容器卷子路径、
> `/etc/manga/config.yaml` 之类——首次启动生成默认配置会失败，屏幕上只有一句
> `open /etc/manga/.config.yaml.tmp-...: no such file or directory`：一个随机临时文件名，
> 既看不出问题出在配置目录不存在，也不知道该去建哪个目录。退出之后还在**当前工作目录**
> 留下一个空的 `data/`。
>
> 生成默认配置这条路径从不建配置文件自己的父目录，却无条件建一个写死的 `./data`。那个
> `./data` 是默认 `database.path` / `cache.dir` 的父目录在 cwd 下的一份手抄件——配置指向
> 别处时它就是个没人用的空目录，而真正该建的目录一直没人管，于是原子写在建同目录临时
> 文件那一步就失败。
>
> **两个目录各归其主。** 生成默认配置只建配置目录，权限 `0700`，与那份含明文密钥的
> `config.yaml` 的 `0600` 配套；目录已存在时不改权限，不去动用户自己摆好的共享目录。
> 数据目录改由服务启动时按 `database.path` 的真实取值创建——sqlite 只开文件、不补建目录，
> 这一层本就需要有人负责，按真实取值建也顺带让绝对路径的 `database.path` 首启即可用。

#### 修复
- **`-config` 指向父目录不存在的位置时，首启会把配置目录建出来并生成默认配置。** 建不出来
  时报的是「配置目录 X 不存在且无法创建：<原因>」，而不是一个原子写的临时文件名。
- **生成默认配置不再在当前工作目录留下多余的空 `data/`。**

---

### 🏷️ v1.5.1 — 2026-09-01（提案收件箱的「锁定字段数」徽章恒为 0）

> 那枚琥珀色徽章从来没出现过：用户在收件箱列表里看不出哪些提案含被锁的字段，点进去却看到
> diff 面板给字段老老实实打着锁标记。同一个响应体里两个数自相矛盾，后果是批量应用后收到
> 一串 `locked_skipped`，事前完全无从预判。
>
> 徽章数来自 SQL 对 `metadata_review_fields.locked` 的聚合，而那一列是入队瞬间的快照。自从
> 「锁定字段归用户、扫描只读不写」定下来，锁定字段根本不入队，这一列对新行恒为 `FALSE`，
> 也没有任何 UPDATE 会改它——聚合结构性地只能数出 0。锁标记走的是另一条路：按系列**当前**的
> `locked_fields` 现算，用户随时开合的锁因此都算得进去。
>
> **口径统一在 Go 侧，不是在 SQL 里 join 现算。** 现算要在 SQL 里把 `locked_fields` 那串逗号
> 分隔的字段名重新拆一遍、再逐个匹配，等于给同一条规则造第二份实现——正是这次出错的形态。
> 收在 Go 侧则是两个计数都数眼前那批字段视图，与 diff 面板画锁标记读的是同一份数据，想不一致
> 都难。代价是零次额外查询：系列的锁定集本来就随收件箱那行一起选出来，字段行本来就为了渲染
> diff 批量取回，分页的列表接口一次查询都不多。`field_count` 一并归位，免得下一个人照着 SQL
> 里那半边的写法再数一遍。
>
> `metadata_review_fields.locked` 保留：老提案的行里可能真留着 `TRUE`，读路径与当前锁定集取或
> 后仍认它，锁只会让字段更不可写。它的语义（快照、恒为假、别拿来聚合）写在列定义上。

#### 修复
- **收件箱列表的「锁定字段数」徽章数得出锁了。** 包括入队之后用户才加的锁——那正是入队快照
  做不到的事。全被锁住的提案现在事前就显示成「锁定 N / 共 N」，与批量应用后的 `locked_skipped`
  对得上。

---

### 🏷️ v1.5.1 — 2026-09-01（智能书架个个写着「无筛选条件」，列表里分不清谁是谁）

> 合集页左栏的筛选芯片从来没显示出来过：芯片组件读的是 `activeTag`、`readState` 这些字段，
> 而 `/api/collection-views` 的列表项一个筛选字段都不带，芯片数组恒为空，只剩「无筛选条件」。
> 条件信息只以 `tag=Action    read=reading` 这种拼接串出现在 description 里。
>
> **响应形状选了嵌套而不是铺平。** 列表项是手工合集与智能书架共用的一个结构，把十一个只对智能书架
> 有意义的字段铺进去，等于让每个消费者自己记住哪些字段配哪种 kind；现在它们收进可选的
> `smart_filter`，出现即代表这是智能书架，手工合集的 JSON 一个字段都不多带。这个对象直接复用
> 书架成员响应里已有的 `SmartFilter`，前端因此只有一份筛选条件的表示，不会再各写一份而漂移。
>
> 顺带补上同一响应里缺失的 `view_id`：左栏靠它认选中项，缺了它每一行都判定为「选中」。

#### 修复
- **智能书架列出它实际的筛选条件。** 标签、作者、连载状态、首字母、阅读状态、评分区间、进度区间、
  加入天数都显示成人话；左栏窄，最多平铺三项，其余折叠成「+N 项筛选」，折叠掉的挂在 title 上。
  排序不算筛选条件——默认的「名称正序」不占芯片，非默认的排在最后单独一枚。
- **排序芯片不再把词条键甩给用户。** 词表原本按 `created_at`、`last_read_at` 这类键写，与后端认可的
  九个取值只对上两个，其余七个会把 `collections.smartChip.sort.volumes` 原样显示出来；现在两个语言
  包都按后端的九个取值补齐。
- **连载状态芯片认得别名。** 状态取值来自刮削源，`publishing`、`已完结` 这类别名原本一律漏出原始键；
  现在归一到全站共用的状态词条，认不出的取值原样显示，不谎报成「未知」。

---

### 🏷️ v1.5.1 — 2026-09-01（普通用户看到一排管理动作，点哪个都吃 403）

> 上一批只补了整页入口与路由，页面内部的动作按钮全都照旧渲染给所有人。系列详情页尤其明显：编辑
> 元数据、刮削、重扫、打开目录、写回 ComicInfo、加入合集、系列关系增删改、元数据提案的采纳/驳回、
> 失败任务重试——这些在后端 `authorize` 里一律要管理员（改写方法只对 `isRegularWritablePath` 里的
> 每用户操作放行，失败任务重试更落在 `/api/system/` 下连读都要管理员）。普通用户面前是一排看着能点
> 的按钮，点下去只换回一句泛化的失败提示。
>
> **呈现方式选了隐藏而不是禁用。** 这些动作里大半是图标按钮，禁用后没有地方承载「为什么点不动」，
> 触屏上连 tooltip 都出不来；而且普通用户在本项目里是纯读者，系列元数据、合集、阅读清单、收藏标记
> 都是站点级共享数据（这几张表都没有 `user_id`），这些动作对他不是「暂时不能做」而是「本来就不归他
> 管」——隐藏才是这个语义。做法也与上一批的整理工作台一致。
>
> 后端权限判定未改动，本批只补前端，管理员看到的一切照旧；普通用户该能做的（阅读进度、书签、
> 系列短评、导出与下载）也一概不动。

#### 修复
- **系列详情页的管理动作只对管理员渲染。** 快捷动作条只留下导出 ComicInfo；书卡菜单去掉写回归档与
  上传封面，保留导出、下载与复制识别码；侧栏的系列关系退成一份可跳转的只读列表，元数据提案与失败
  任务仍然看得到，只是不再给采纳/驳回与重试。
- **审核中心整页归管理员。** 那一屏只有裁决提案这一件事，采纳与驳回都要管理员，于是侧边栏入口、
  首页的待办角标一并只对管理员出现，路由套上 `RequireAdmin`。
- **合集页、阅读清单页保留浏览，收起管理动作。** 新建、改名、删除、固化快照与成员增删都要管理员；
  作品群原本就有的只读语义不受影响。
- **资料库页的批量操作栏按角色裁剪。** 收藏/取消收藏、加入合集、批量编辑、传输只对管理员出现；
  标记已读/未读走每用户的进度端点，普通用户照旧可用。卡片上的刮削、重扫与收藏同样收起，
  已收藏的系列仍以一个静态心形标出来。
- **阅读器里「把当前页设为封面」只对管理员出现。** 书签、进度与下载不受影响。

---

### 🏷️ v1.5.1 — 2026-09-01（普通用户在侧边栏点得进「任务与日志」，进去整屏 403）

> 侧边栏把「任务与日志」摆给所有人，路由也没设防，但那一屏要的四个接口——`/api/system/tasks`、
> `/api/system/logs`、`/api/system/performance`、`/api/system/storage-io`——在后端 `isAdminOnlyPath`
> 里连读都要管理员。普通用户点进去换回四个 403，看到的是一屏加载失败，像系统坏了而不是没权限。
> 紧挨着的「系统设置」两道守卫（隐藏入口 + 路由守卫）都在，「任务与日志」两道都漏了。
>
> 后端的权限判定未改动，本批只补前端的守卫，管理员看到的一切照旧。

#### 修复
- **「任务与日志」的入口只对管理员渲染，路由也套上 `RequireAdmin`。** 隐藏入口挡的是「点得到」，
  路由守卫挡的是直接输 URL 与旧书签；`/logs`、`/organize/tasks` 这两条兼容路由重定向到 `/ops`
  之后同样被挡下。
- **被挡下时说明原因。** 从前是无声跳回首页，用户只会以为链接坏了；现在跳回首页并提示这是管理员页面。
- **应用外壳不再替普通用户探测 `/api/system/capabilities`。** 它只喂给建库/改库表单，而表单本就是
  管理员专属；普通用户每加载一次页面都白吃一个 403。
- **整理工作台上「后端一定会拒」的动作不再摆给普通用户。** 刮削、重扫、重建身份索引、KOReader 对账、
  去重移除与「查看来源任务」都要管理员；健康报告本身仍然人人读得到。

---

### 🏷️ v1.5.1 — 2026-09-01（滑动续期只续了服务端，天天在用的用户仍在第 30 天被踢下线）

> 会话声明的是「30 天有效期 + 滑动续期」，服务端也确实在续：距上次活跃超过 1 小时的请求会把
> `sessions.expires_at` 推到 30 天后。但会话 Cookie 全仓只在登录时下发过一次，浏览器那份的 `Max-Age`
> 因此永远停在登录那一刻。结果是每天都在用的用户，服务端会话好好地活着，浏览器却在第 30 天自己把
> Cookie 扔了——表现为毫无征兆地退回登录页。**「会话过期不再删本机离线数据」那条修复只降低了被踢下线
> 的代价，用户仍要重新登录**；续期对客户端真正生效才是根治。
>
> **升级后不需要重新登录。** 续期用的就是请求里带上来的那个会话令牌，不轮换、不重建会话，服务端的
> 会话记录也照旧有效。现有用户升级后第一次「距上次活跃超过 1 小时」的请求，就会把浏览器里那份 Cookie
> 顶成新的 30 天。只有那些 Cookie 已经过期、已经被退回登录页的用户才需要再登一次。
> 数据库结构、接口契约与 Cookie 的属性均未改动。

#### 修复
- **滑动续期同时下发 Cookie。** 服务端推后 `expires_at` 的那一次请求，响应里带上同一个令牌、同一套属性
  （`Path=/`、`HttpOnly`、`SameSite=Lax`，`Secure` 仍按 `server.cookie_secure` 与可信代理判定）的
  `Set-Cookie`，把浏览器那份的有效期一起推后。客户端与服务端的到期时刻由同一个判断驱动，不再分叉。
- **下发跟着续期的节奏走，每会话每小时至多一个响应头。** 沿用既有的「距上次活跃超过 1 小时才续」阈值，
  没到阈值的请求一如既往不写库、也不下发；不另设「剩余有效期低于某某才下发」的第二个阈值——那只会
  让客户端的到期时刻落后服务端好几天，正是本条修复要消灭的分叉。
- **续期落库失败时不下发。** 服务端没续成却把浏览器的 Cookie 延了 30 天，只会把一次无声失效换成更迷惑的
  失效：用户拿着一个看起来还没过期的 Cookie 撞 401。
- **取页/封面/缩略图这类图片响应照样下发。** 阅读期几乎只有图片请求，把它们排除等于在最该续期的场景里
  继续哑火；这些端点是 `private, max-age=0, must-revalidate`，共享缓存不会替别的用户存下这个 `Set-Cookie`。
  事件流（SSE）在建连的那组响应头里下发；长连接建立后不再经过鉴权中间件，续期靠的是同一浏览器的其他请求。
- **被拒的响应不顺手续期。** 未登录的 401、CSRF 不通过的 403、必须改密时被挡下的请求都在续期之前返回，
  不带 `Set-Cookie`；阅读协议（OPDS / Mihon）走 HTTP Basic，本就不使用会话 Cookie。

---

### 🏷️ v1.5.1 — 2026-09-01（删号删不干净：书签永久堆积，KOReader 设备账号继续指向死用户，同步静默失效）

> 删除一个用户后，`sessions` 与 `user_*` 每用户进度表都由外键 `ON DELETE CASCADE` 清干净了，但两处漏网：
> `reading_bookmarks.user_id` 与 `koreader_accounts.user_id` **都没有指向 `users` 的外键**（后者由迁移
> `ensureColumn` 后加，加不了外键）。前者只是垃圾堆积——`AUTOINCREMENT` 不复用 id，旧书签不会泄漏给新账号；
> 后者却是活的故障：`GetKOReaderAccountUserID` 仍返回死用户 id，进度投影去写 `user_book_progress` 时撞外键
> 失败，而这条失败只写了一行日志，设备侧照样收到 ok。用户以为同步正常，实际一条进度都没落进应用。
>
> **两列都挂不了真外键**：它们的 `0` 是有效哨兵——书签的 0 表示「尚未启用多用户」的历史全局数据，设备账号
> 的 0 表示**未归属**，而 `users` 里不存在 id=0 的行，真外键会当场拒收这些值；书签那列改成可空也不行，
> SQLite 的 `UNIQUE(user_id, book_id, page)` 视 NULL 互不相等，写入书签的 `ON CONFLICT` upsert 会失效。
> 因此改用 `AFTER DELETE ON users` 触发器给出等价的自动级联：新的删除路径同样自动受益，不必重建表，
> `users.id` 由 `AUTOINCREMENT` 生成恒大于 0，也就永远碰不到哨兵行。
>
> **升级注意**：只新增一个触发器，**没有表结构变更、不重建任何表、不需要停机或备份还原**，旧库首次启动
> 即自动补上。**库里已有的孤儿数据**由迁移一次性清理：`user_id` 既不为 0、又在 `users` 里找不到的书签、
> KOReader 设备账号，以及该账号名下的 `koreader_progress` 与 `koreader_sync_events` 全部删除。这些数据的
> 主人已不存在，任何界面都读不到它们。清理每次启动重放，级联触发器在位时不会再产生新孤儿，故此后为无操作。
>
> **被删用户的 KOReader 设备**下次同步会收到 401，需要在设备上重新注册并由管理员指派归属。

#### 修复
- **删号级联到书签与 KOReader 设备账号。** 触发器 `trg_users_ad_purge_owned` 删掉该用户的书签，并删掉他名下的
  KOReader 设备账号连同其 `koreader_progress` / `koreader_sync_events`——与管理员手动删账号（`DeleteKOReaderAccount`）
  口径一致。设备账号选择**删除**而不是解绑成未归属：它是一份能通过鉴权的凭据，删号必须一并吊销；解绑后设备
  仍能登录，其进度还会顺着 `user_id=0` 的回落路径写进全局进度，等于换一种方式继续污染。
- **其他用户与哨兵数据不受影响。** 触发器与一次性清理都显式排除 `user_id = 0`，未归属的设备账号与历史全局
  书签原样保留；在世用户的书签、设备账号与进度不动。
- **进度投影失败不再静默。** KOReader 推送与拉取时若进度匹配到书却写不进去，除日志外还会落一条状态为
  `progress_apply_failed` 的同步事件。账户列表与设备诊断把「状态不在 `ok` / `progress_regressed` 之列」的事件
  当作最新错误展示，这次失败因而在界面上可见。设备侧仍按 kosync 语义收到 ok：规范记录本身已存下，回报错误
  只会让设备无谓重试，救不回这次写入。

---

### 🏷️ v1.5.1 — 2026-09-01（大小写不敏感的文件系统上，库内软链仍会让同一批文件重复入库）

> 上一条「库内软链指向同库真实目录时重复入库」的修复只在**链接目标的写法与真实目录名大小写完全一致**时
> 生效。macOS（APFS）与 Windows（NTFS）默认大小写不敏感，`ln -s "series alpha" "Alpha (link)"` 这种把目标
> 写成另一种大小写的链照样通、指的仍是 `Series Alpha` 那个目录，但去重认不出来：遍历的去重身份取的是
> `filepath.EvalSymlinks` 解析出的**路径字符串**，而它逐段解析软链却保留调用方写的大小写，普通遍历登记的
> 是 `Series Alpha`、软链解析出的是 `series alpha`，两个字符串不相等，去重落空。后果与上一条相同：磁盘上
> 的一个文件变成两本书、两个系列，`book_count` 翻倍，去重、统计与阅读进度各算各的。Windows 上绝对目标里
> 的盘符大小写（`D:\` 与 `d:\`）机理相同，一并盖住。Linux 上常见的大小写敏感文件系统不受此问题影响，
> 那里 `/Data` 与 `/data` 本就是两个不同目录，修复后它们**仍然**各扫各的。数据库结构与线上契约均未改动。
>
> **已经因此重复入库的用户**：新的扫描不会再造出重复，但旧的那一份不会自己消失——链接路径在磁盘上仍然
> 存在，清理判定它没有缺失。要清干净，可以把库内那条软链删掉（或移出库根）之后对该资料库执行一次清理；
> 想留着这条链当组织视图的话，就在界面上直接删掉多出来的那个系列——它的路径是链接那一侧的那个。被删掉的
> 那份上如果记过阅读进度会一并消失，真实目录那份不受影响。

#### 修复
- **遍历的去重身份从「路径字符串」换成「这是不是同一个目录」。** 解析后的路径折叠大小写只用来**分桶**，
  同桶的候选再逐个用 `os.SameFile` 定夺。大小写不敏感的文件系统上，`Series Alpha` 与 `series alpha` 落进
  同一个桶且确是同一个目录，第二条路径被跳过；大小写敏感的文件系统上两者同样落进同一个桶，但 `os.SameFile`
  会说它们不是一个目录，两棵都照走——折叠若用来判等，那里就会漏扫一整棵树。没有引入「探测文件系统是否
  大小写敏感」这类平台判断，文件系统自己怎么回答，去重就怎么算。
- **仍然不用 inode/设备号做键，也没有把去重退化成线性比对。** `syscall.Stat_t` 的 `Dev`/`Ino` 是 Unix-only，
  而 `build.sh` 要交叉编译出 Windows 产物；`os.SameFile` 两边都在，只是进不了 map，所以把它放在桶内使用。
  只有互为别名或只差大小写的路径才会同桶，几万个目录的资料库里桶长仍是 1，`os.Stat` 也只在同桶时才做。
- **上一条修复守住的四种情形不变**：链→链的菱形、a 与 b 互指的环、链→同库真实目录（大小写相同）、以及
  两个资料库各自软链到同一块外置盘的同一个目录时互不相干。两条路径都通时留下来的仍是**真实目录**那条。

---

### 🏷️ v1.5.1 — 2026-09-01（一行超长日志把日志页整个打成 500，管理员在最需要日志时反而看不到日志）

> 元数据源（AniList / MangaDex / Bangumi）遇到非 2xx 时，会把上游响应体**不截断**地写进日志——而
> CDN / WAF 拦截时回的常是一整页 HTML 错误页，几十上百 KB。这样的一行落盘后，日志查看端点用的
> `bufio.Scanner` 会在默认 64KB 上限处中止整趟扫描并报错，端点直接回 500：从这一行落盘起，到日志轮转
> 掉为止，日志页什么都读不出来——包括本来能解释这次失败的那几条。同一行返回给用户的错误串却老老实实
> 走了截断，日志与错误串两套标准不一致，正是问题的来源。数据库结构未改动。

#### 修复
- **日志页不再被单行坏数据打死。** 读取侧改成自己逐行读：超过 16KB 的行截断并在行尾标上
  `…(line truncated by viewer)`，然后继续读后面的行。选降级而不是把上限调大——调大只是把悬崖挪远，
  且救不了已经躺在文件里的行。**日志文件中已有的超长行现在能正常读**：那一行以截断形态呈现（不是静默
  丢弃），它前后的日志全部照常返回，统计口径也照常计入。
- **制造侧不再写出超长日志行。** 五家 Provider（AniList / MangaDex / Bangumi / MyAnimeList /
  Comic Vine）的「上游非 2xx」分支统一走一个出口，由它先截断再脱敏，并把同一份结果交回去拼错误串——
  日志行与用户看到的错误串从此不可能再各用一套标准。配套加了一条扫包目录的护栏测试，禁止新 Provider
  把响应体直接拼进 `slog`。

---

### 🏷️ v1.5.1 — 2026-09-01（多用户下仪表盘的「近 7 天活跃 N 天」显示的是全体用户的活跃度）

> 仪表盘里那个数字紧挨着活动热力图（就印在热力图标题下方），可两者不同源：热力图早已按用户拆分，
> 活跃天数却仍在统计全站的阅读活动。新注册的用户一页没读，面板上却写着「近 7 天活跃 N 天」，而他自己
> 的热力图一格不亮——那个 N 是别人翻书的天数，属于跨用户泄露。同一份统计里的已读书本数本就按用户改写，
> 只有活跃天数没跟上；系列总数、藏书总数、总页数与各资料库体积是库级统计，本就该全站一个数，不受影响。
> 数据库结构未改动，历史数据无需迁移。

#### 修复
- **登录后的「近 7 天活跃天数」只统计当前用户自己的阅读日。** 它现在与正下方的活动热力图读同一张每用户
  活动表、用同一个「一天」的口径（服务器本地日历日），两处的数对得上。未启用多用户的单机部署照旧走全局
  统计，显示不变。

---

### 🏷️ v1.5.1 — 2026-09-01（LLM 刮削给每个状态正确的系列都多产一条「连载中 → unknown」的垃圾提案）

> 三家 LLM provider（Ollama、OpenAI Responses、OpenAI Chat Completions）在交出刮削结果之前，就把模型
> 给的状态过了一道「认不出就落 unknown」的兜底：模型漏了 `status` 字段（本地小模型的常态）、写成别名表
> 里没有的形态（繁体「連載中」、`serialization`）、或按提示词允许的取值老实填了 `unknown`，交出来的都
> 是 `unknown`。提案侧本有一条明确约定——数据源没给状态就留空，由「空提案不入队」丢弃，Bangumi /
> AniList / MAL / Comic Vine 都照办——只有这三家在上游就把空值折没了。后果是每次 LLM 全库刮削，每个
> 已有正确状态的系列都多出一条「连载中 → unknown」的提案，用户要手动逐条驳回；一旦误点应用（或用
> all 模式批量应用），正确状态就被抹成 unknown。数据库结构未改动。
>
> **收件箱里已经堆着的旧提案不会自动消失。** 本次改动只保证不再产生新的，旧的仍需人工清理：差异只有
> status 一条的整条驳回即可（收件箱勾选后可批量驳回，一次至多 100 条）；同一条提案里还夹着有用字段的，
> 改用「只补空缺」模式应用——它跳过当前值非空的字段，已有的正确状态不会被覆盖。已经被写成 unknown 的
> 系列需要在详情页手工改回：服务端没有留下它被覆盖前的值。

#### 修复
- **LLM 交出的状态只报模型确实给了、且我们认得的那些。** 三家 provider 统一走同一个数据源侧出口：没给、
  认不出、以及模型自己说 `unknown`，三种都留空——它们说的都是「数据源不知道」，不是一个能覆盖系列现有
  状态的事实。提案被接受后写进系列的那条路径仍保留「认不出就落 unknown」的兜底：那里用户已经裁决了这个
  字段要写，留空没有意义。
- **状态别名表补上繁体、日文与英文变体。** 「連載中」「已完結」「完結」「休載中」「serialization」
  「releasing」「on hiatus」「discontinued」等现在能被认成对应的状态码，而不是白白留空；大小写、`-`、
  `_` 与多余空白也一并归一。仍然认不出的写法照旧留空，绝不折成 unknown。

---

### 🏷️ v1.5.1 — 2026-09-01（作品群合集能被人工编辑和删除，改动在下一次重建时被静默丢弃）

> 作品群是由系列之间的关系连通推导出来的，不由人工建立。但 `/api/collections` 这份「把系列加进哪个
> 合集」的候选清单不按 `source_type` 过滤，把作品群合集也列了出来；写入端点同样不看 `source_type`。
> 两条后果都是静默的：往作品群里加系列返回 200、成员数 +1，下一次重建按连通分量做成员差集时把它删掉，
> 用户全程没有任何提示；删除作品群返回 200，下一次重建把它原样建回来，但 **id 变了**。第二条更严重
> ——合集 id 是对外暴露的，Mihon 的 `/mihon/collections/{id}/series` 与 OPDS 的 `/opds/collections/{id}`
> 都按它取数，id 漂移会让客户端书库里记下的条目与用户站内收藏的合集链接集体失效。数据库结构未改动。
>
> **库里已有的脏数据不需要迁移。** 被人工加进作品群的系列会在下一次重建时自然收敛掉——那正是这些
> 改动本来就会遭遇的结局，闸门只是把它提前到写入的那一刻并明确告知。被人工删掉的作品群同样已经在
> 之后的某次重建里换着新 id 建回来了，那次 id 漂移已经发生、无从追溯，重建流程会继续按稳定键
> （连通分量的最小系列 id）维持它此后的身份不变。升级后不必做任何清理动作。

#### 修复
- **作品群合集的四个写入端点一律返回 403。** 加系列、移除系列、改名、删除都被挡下，且响应说明它是
  从系列关联推导出来的。要改动作品群的成员，改系列之间的关联即可，重建会跟着走。
- **作品群不再出现在「加入合集」的候选列表里。** `/api/collections` 只返回可人工编辑的合集；合集页
  仍照常列出作品群，那条路径走的是 `/api/collection-views`。

#### 改动
- **作品群在合集页上以只读形式呈现。** 能看能进、成员网格照常展示——「这个系列的相关作品」是有价值的
  浏览视角，隐藏掉等于丢功能；但标题旁多一枚「只读」徽标，删除、改名与逐个成员的移除入口都不再给出。
- **一次性固化的合集不受影响。** 智能书架快照（`smart_snapshot`）与 AI 分组（`ai_grouping`）落地后
  归用户所有，没有任何流程会回头覆盖它们，增删改一如既往。

---

### 🏷️ v1.5.1 — 2026-09-01（接受提案时，刮削源建议**去掉**的标签与署名错误的作者一个都删不掉）

> 收件箱的 diff 面板按「当前值 → 提案值」的替换语义渲染，系列详情页的手工保存也是先清空再重建，
> 唯独接受提案那条写入路径只做 upsert + `INSERT OR IGNORE`，从不清空旧关联。同一个字段，两条写入
> 路径两种语义。系列上已有的标签/作者比刮削源给出的多是所有数据源的常态——Bangumi/AniList 给出的
> 标签集与用户手上那份很少完全一致——于是删除方向的改动全部落空，提案却被关单成「已应用」。
> 后果是三重的：界面报成功而标签一个没少；来源沿革记下的是提案值而不是系列的真实内容，审计链说谎；
> 系列的当前值永远追不上提案值，下一轮刮削同一个源又生成一条一模一样的提案，定时全库刮削会让用户
> 反复看到、反复「应用」同一条。数据库结构与线上契约均未改动。
>
> **已经被错误关单的历史提案：重新刮削一次即可补救，没有自动迁移。** 那些提案已是终态，服务里没有
> 重放入口；但正因为系列没收敛到提案值，同一个数据源再刮一次会照原样重新生成这条提案（收件箱里
> 可能已经堆着一条），这次「应用」就会真正生效。沿革里那些与系列内容对不上的旧记录会在该字段下次
> 被写入时覆盖掉，不作追溯修复。

#### 修复
- **接受提案时 tags 与 authors 整体替换。** 写完之后系列上的这两个集合等于提案值：提案里没有的标签
  被删掉，署名错误的作者被换掉。同一份刮削数据第二次跑不再生成重复提案。
- **来源沿革记的是系列的真实内容。** 那串文本按落库口径去重后写下，与系列上实际存在的标签、作者逐字一致。
- **标签与作者的写入错误不再被吞掉。** upsert、关联、外链的任何一处失败都整体回滚，不会再出现
  「标签没落库、沿革却记下了、提案照样关单成已应用」这种三方说法互相矛盾的结果。

#### 改动
- **锁与「补空」是替换语义下保住用户手工内容的两道口子。** 锁住 tags 或 authors 时整个字段跳过，
  既不写也不清空；「只补空缺」模式下这两个字段只有在系列上本来就没有内容时才参与写入，因此删不掉东西。
  归档内嵌 ComicInfo 的扫描增补不受影响——它按卷累积，本来就不是提案。

---

### 🏷️ v1.5.1 — 2026-09-01（智能书架命中数超过一页时，剩下的成员点进去永远看不到）

> 右栏只发一次不带 limit/offset 的请求，拿到一页就停；计数又用的是「这一页的条数」，
> 响应里的命中总数被丢掉。于是左栏写 42、右栏写 30，另外 12 个既没有翻页控件也没有加载更多，
> 谁都够不着——正是智能书架计数那份 doc 里明令要避免的分家。手工合集一次取完，所以不受影响，
> 两类合集行为不一致更容易被当成数据坏了。数据库结构与线上契约均未改动。

#### 修复
- **智能书架的成员能翻完了。** 右栏首屏仍只取一页，底部给出「加载更多」按 offset 往后追加，
  直到把命中的成员取全。几万个命中的书架也不会一次全灌进页面。
- **右栏计数改报命中总数。** 没加载完时显示「已加载 30 / 共 42 个系列」，加载完显示「42 个系列」，
  两种写法里的总数都与左栏同源。
- **慢网下切换合集不再张冠李戴。** 先点 A 后点 B、A 的响应后到时，整份丢弃而不是覆盖 B 的成员网格；
  此前只有标题受保护，网格会变成 A 的。

#### 改动
- **智能书架的「每页大小」不再兼任端点的截断上限。** 书架配置里的 `page_size` 只当「不指定时用哪个每页
  大小」的默认值；单次响应的体量由端点自己的上限（100）把关，`limit` 不再被 30/50/100 白名单吞掉，
  配置值为 0 的老书架也会退回默认值而不是返回空列表。

---

### 🏷️ v1.5.1 — 2026-09-01（会话过期一次，本机下载好的离线书与还没回传的阅读进度被整份删掉）

> 会话失效有三条路都指向「当前没有用户」，但只有一条是换人。前端把三者混作一谈：状态探测答
> `authenticated:false` 与 401 兜底都按「登出」处置，把 owner 标记、书目索引、缓存字节与待回传的进度队列
> 一并清空。而这两条恰恰是同一个人最常撞上的——30 天到期、管理员重置密码、他在另一台设备上改了密码
>（后端会踢掉该用户的全部会话）——下次打开页面即触发，用自己的账号重新登录后本机数据已经没了。
> 换人隔离本来就由「登进来的是不是同一个人」那条判定兜住，在「没有用户」这一步再清一次只多买到
> 「登出后设备再断网」这一个窄场景。数据库结构与线上契约均未改动。
>
> **已经丢过数据的用户没有恢复路径。** 缓存字节与进度队列都只在浏览器本地（Cache Storage 与
> localStorage），清空是就地删除，服务端从未收到过那些进度。已下载的书需要联网重下；那段没回传的
> 进度只能靠用户自己记得读到哪一页，手动翻回去。

#### 修复
- **会话过期与 401 不再删本机的离线数据。** 这两条只清登录态、回到登录页，离线书目、缓存字节与待回传
  队列原样留着：同一个人重新登录后队列继续回传，下载好的书也还在。
- **换人隔离改由「谁登进来了」单点判定。** 对账入口不再接受「没有用户」这个参数，真正换成另一个用户
  登录时照旧把上一个人的队列、书目索引与缓存字节清干净。
- **登出前先尽力把进度队列回传上去。** 登出这一刻会话仍然有效，是队列最后一次能上行的机会；传不掉的
  （断网时登出）随后照清——留着它，下一个在这台设备上登录的人会被自动当成自己的进度传上去。

#### 改动
- **离线放行的判据说明补齐。** 该判据从来只看本机的 owner 标记与书目索引、不看会话是否有效，会话过期前
  同样放行；过期后放出的仍是这台设备上这个 owner 自己下载的字节，没有多放出任何东西。

---

### 🏷️ v1.5.1 — 2026-09-01（UTC+8 时区里「当前连续」恒为 0，月初凌晨的阅读不算进本月回顾）

> 阅读活动按**服务器本地日历日**记账（这样「今天读了多少」才是用户所在的那一天），但几处读取仍按 UTC
> 日历日取「今天 / 本年」。两套日历在本地日与 UTC 日错开的那几个小时里分裂：UTC+8 的当地 00:00–08:00、
> UTC-5 的当地 19:00–24:00。数据库结构与线上契约均未改动，历史数据无需迁移。

#### 修复
- **连续阅读天数在非 UTC 时区不再恒为 0。** 统计页的「当前连续」按 UTC 日历日判断末次活动是不是今天/昨天，
  本地日领先 UTC 日时算出的间隔是 -24 小时、落后时是 48 小时，两侧都被当成断档；旁边的「最长连续」只比较
  相邻活动日、不涉及今天，所以一直是对的。现在「今天」与写入侧同取本地日历日。
- **月初/元旦凌晨打开回顾不再是空的。** 年度/月度回顾在调用方没指定年份时按 UTC 取默认年，UTC+8 的元旦
  凌晨会退回去年、UTC-5 的除夕深夜会提前跳到明年；月份边界同理，本地已跨月时按月查询捞不到刚写入的行。
- **热力图右端补回今天那一格。** 前端网格按 UTC 今天收尾，UTC+8 当地 00:00–08:00 读的书在图上没有格子可落。
  网格终点改取本地今天，格子内部的日期推进仍用 UTC 坐标，日期与星期几才不会错行。

#### 改动
- **「一天」的口径收进单一入口。** 新增服务器本地日历日的统一来源（今天、近 N 天下界、回顾期年月边界），
  读写两侧都从这里取日期，不再各自调用 `time.Now()` 决定时区口径。

---

### 🏷️ v1.5.1 — 2026-09-01（一边改名一边往库里拷新书，改名那本的阅读进度还是会丢）

> v1.5.0 修掉的是「同一个库的清理与扫描**同一轮到期**」那条路径：清理会被取走、接在扫描之后串行。
> 本次补的是另一条。两条排期由不同事件刷新——扫描由 `Create|Write`、清理由 `Remove|Rename`——因此会
> 分叉：库里只要还有持续写入（最典型的就是一边改名一边往库里拷 `.cbz`），扫描的去抖窗口就不断后移、
> 本轮永远不到期，串行取走的那段控制流根本走不到，而独立的清理派发照常把清理单独放出去，赶在改名重连
> 之前把旧行当成「文件已消失」删掉，阅读进度、书签、合集归属、阅读清单条目随 `ON DELETE CASCADE` 一起没。
> 同源的还有两条：清理撞上一次已派发、仍在跑的扫描（重连是扫描**末尾**才写的），以及同轮扫描失败后
> 清理只被推迟一个窗口、下一轮就单独跑掉。数据库结构与线上契约均未改动。

#### 修复
- **清理的判据从「本轮有没有扫描要跑」换成「这个库有没有未了结的改名重连」。** 三条信号任一成立就继续
  等下去：该库还有排期中、尚未派发的扫描；监听器有已派发未返回的该库扫描；别处（任务面板的「扫描资料库」）
  发起的扫描正在跑。等待期间这条清理留在排期里、时间戳原样不动，下一轮重判。
- **同轮扫描失败时，清理与扫描一起重新排期。** 只把清理放回去，下一个去抖窗口会因为「这个库没有排期中的
  扫描」而判定安全并直接删行；连扫描一起放回，才守得住「清理永远跟在一次成功的扫描之后」。

#### 改动
- **一条清理最多被推迟 2 分钟。** 从第一个删除/重命名事件起算，等过上限就不再干等，改为主动为该库派发一次
  扫描（越过扫描自己的 5 秒去抖）、把清理接在它之后串行。这条不牺牲安全——清理仍在一次**成功**的扫描之后
  ——代价是这次扫描可能读到一个还在拷贝中的半成品归档，下一次扫描会自愈。唯一没有上限的等待是「该库的扫描
  持续失败」：那种情形下删行正是要防的那种丢数据，宁可让幽灵记录一直留着。

---

### 🏷️ v1.5.1 — 2026-09-01（每改一次列表排序，服务端日志就多一条 ERROR）

> 服务端日志里反复出现 `cursor sort "name_asc" does not match request sort "created_asc"`：前端把
> `name_asc` 排序下算出的游标发给了 `created_asc` 的请求，后端拒绝并返回 400，那一次列表请求整个失败。
> 用户几乎察觉不到——前端的世代保护把这个 400 丢掉，随后正确的请求覆盖了结果——但每改一次排序就往日志里
> 写一条 ERROR。根因在前端：取数与「改筛选后重置页码」是两个 effect，React 按注册顺序执行，取数的先跑，
> 它手里的页码还没重置、游标还是上一个排序算出来的，于是发出一次注定被拒的请求，重置才随后生效。后端一侧
> 则是把「客户端手里的游标过期了」当成了异常。数据库结构与线上契约均未改动。

#### 修复
- **游标缓存跟着查询作用域走，作废的游标发不出去。** 缓存下来的「第 N 页起点游标」只在「除页码外的
  整套查询条件」（资料库、每页数量、筛选、关键词、排序）不变时才有效，作用域一变就读不到旧游标，
  改排序那一轮只会发出不带游标的请求。这条判据不依赖任何 effect 谁先谁后，往页面里再插一个 hook 也不会
  把它弄坏。
- **游标过期时列表照常出结果。** `/api/series/search` 收到用不了的游标（排序已换、串坏了、载荷不合法、
  或该排序本就不支持游标）时忽略它，按 `page` 走 offset 分页，并在响应里发回一个属于当前排序的新游标。
  手动改过的地址、分享出去的旧链接、浏览器前进后退都会产生过期游标，这是可预期的正常输入，不该让整页失败。

#### 改动
- **日志级别按「谁出的错」定。** 过期或无效的游标降到 `Debug`，不再往运维日志里刷 ERROR；服务端自身故障
  （SQL 执行失败等）仍然记 `Error` 并返回 500，不会被降级路径一起吞掉。

---

### 🏷️ v1.5.1 — 2026-09-01（存储掉线后点开漫画只有黑屏，看不到任何提示）

> 一块外置盘被拔掉，那块盘上整个资料库（57565 个系列）的书全部读不出来。用户点开任何一本，
> 阅读器**一直停在加载态**——全屏深色底加一个永远转不完的圈，没有任何文字提示；服务端日志里也只有
> HTTP 中间件那一条 `status=500`，运维同样看不出是哪个盘掉了。两侧各有一处断点：前端的
> `readerLoading` 除了 `loading` 还叠了「页清单是否属于当前这本书」，而失败路径上后者永远补不上，
> 于是加载态恒真、两套主题都先判它再判 `loadError`，现成的错误界面一次也渲染不到；后端 `getPagesByBook`
> 把底层的 `err` 整个丢弃，连一行 `slog` 都没有，「文件不存在」还是「归档损坏」的信息在这里断掉。
> 本次不做存储的自动检测与恢复，只让失败说清楚发生了什么。数据库结构未改动。

#### 修复
- **取页失败不再把用户留在黑屏上。** 只要有 `loadError` 就退出加载态，阅读器改为渲染错误面板
  （标题、说明、「重试加载」与「返回系列」两个按钮）。同一处断点也压着「归档里没有页」那条既有提示，
  一并恢复。Comimi 主题原先只有一行未翻译的 `Error:`，改为与默认主题共用同一块面板。

#### 改动
- **读不到书的字节时，失败按四类下发。** 先探测资料库根目录再看单条路径——存储离线时库内每条路径都会
  「看起来不存在」，不先探根就会把整块盘掉线误判成这一本被删了。判定沿用扫描器既有的原则：只有明确的
  `IsNotExist` 才算消失，权限与 IO 错误另算一类。四类各有状态码：**存储离线 503**（依赖的存储暂时
  不可用而非服务器内部错误，附 `Retry-After`）、**单文件缺失 404**、**归档损坏/加密/空档 422**、
  **路径权限或 IO 错误 500**。响应体在原有的 `error` 之外增加 `reason` 与资料库 ID / 名称 / 路径，
  老客户端只读 `error` 仍然可用。取页清单、取页图、整卷下载三条端点共用这套判定；封面不在其列——
  它读的是缓存目录里的缩略图，不随藏书所在的卷一起掉线。
- **服务端为每一次这类失败留一条可诊断日志。** 统一 msg `Book storage read failed`，带
  `op`（哪条端点）、`reason`、`status`、`book_id`、`library_id`、`library_name`、`library_path`、
  `path` 与底层错误，运维一眼能看出是哪个资料库、哪条路径、什么错。
- **界面按分类说人话。** 存储离线会指名道姓：「资料库「dmzj」（G:\dmzj）现在读不到。这块存储可能已被
  拔出、盘符变了或网络路径断开——把它接回来再重试。」文件缺失、归档损坏、权限错误各有各的说法，
  两个语言的词条都已补齐。

---

### 🏷️ v1.5.1 — 2026-09-01（进系列详情后，「返回资源库」第一次点击没反应）

> 从资料库点进一个系列，再点左上角的「返回资源库」，第一次点击**没有任何反应**，要点第二次才回得去；
> 浏览器自带的后退键同样要按两次。根因在资料库卡片：卡片本身就是一个 `<Link>`，而它的 `onClick` 里又做了
> 一次程序化跳转，一次点击于是往历史栈压了**两条**指向同一个详情页的记录。返回按钮走的是「回退一步」，
> 第一步只退掉了重复的那条，落回详情页自己，用户看起来就是「没反应」。同一个根因还让 Ctrl 点击 / 中键
> 点击卡片在开出新标签页的同时，把当前标签页也一起带去了详情页。资料库的筛选、排序与页码不受影响，
> 数据库结构与线上契约均未改动。

#### 修复
- **卡片的普通点击只由 `<Link>` 负责导航。** 卡片的 `onClick` 只在多选模式下拦下默认导航、改成切换选中；
  普通点击不再另走一次 `navigate`。历史栈恢复成一次点击一条，浏览器后退与页面返回都是按一次就动，
  Ctrl / 中键的「在新标签页打开」也回到只开新标签、当前页不动。
- **「返回资源库」直达资料库，不再回退一步。** 按钮的落点改为由系列的 `library_id` 直接算出。按钮写着
  「返回资源库」，而「上一页」未必是资料库——从搜索结果、关系图、合集、继续阅读进到详情页时，回退一步
  会把用户送去别处；历史栈有多深也不是详情页单个组件担保得了的事，这次的重复条目就是例证。改成直达
  之后，从哪个入口进来都落在该系列所属的资料库，深链、新标签页打开、刷新后直接点返回同样成立。
- **返回后资料库的浏览状态仍在。** 筛选、排序、页码由 `useLibraryFilters` 从 URL 与
  `library:{id}:settings` 恢复，直达 `/library/{id}` 会把它们重新写回地址栏，用户回到的还是离开前那一屏。

---

### 🏷️ v1.5.0 — 2026-08-31（修掉 golang.org/x/image 的已披露漏洞）

> 纯依赖升级，无功能变更、无数据库改动，升级无需额外步骤。

#### 依赖
- **`golang.org/x/image` v0.44.0 → v0.45.0**，修掉 GO-2026-6222。这条漏洞在本仓的可达路径上
  （`internal/images` 用它解 webp），`govulncheck` 因此报「你的代码受影响」而不只是「依赖里有」。
  升级后 `govulncheck ./...` 报 0 个漏洞。

---

### 🏷️ v1.5.0 — 2026-08-31（快速扫描首扫的书页数为 0，改回正常档位也补不上）

> 用 `scanner.scan_profile: fast_scan` 做首扫的书，`page_count` 是 0，而且**粘住**：用户把档位改回默认的
> `metadata_scan` 再扫，这些书仍然被跳过，页数与封面永远补不上，只有强制扫描能修。后果是这批书的进度
> 百分比、读完判定与系列已读统计一律算不出来，用户改回正常档位重扫，看起来「扫了但没用」。原因是增量
> 扫描的跳过判据只有「文件没变」这一条：fast 档位不打开归档，它入库的书页数本就是 0、封面为空，而文件的
> mtime 与大小与磁盘完全一致，于是换了档位的下一次普通扫描照样把它当成「无事可做」。修好后跳过要同时
> 满足两条——文件没变，**且**当前档位补不出这本书缺的数据。**已经存着 0 页数的用户**：升级后对该资料库
> 跑一次普通扫描即可，不需要强制扫描，页数与封面会自动补上（前提是档位已改回 `metadata_scan` 之类会打开
> 归档的档位；停在 `fast_scan` 上则维持原样，那个档位本就不读页数）。数据库结构与线上契约均未改动。

#### 修复
- **增量跳过的判据挂上当前档位的能力。** 缺页数就重读、不看档位，会把 `fast_scan` 变成每次全量重读，那个
  档位随之失去意义；因此只有当前这次扫描**补得上**那份缺失数据时才不跳过：会打开归档的档位遇到
  `page_count` 为 0 就重读，会读元数据的档位遇到 `cover_path` 为空就重读，`fast_scan` 两样都补不出，缺着
  也继续跳过。整库扫描与单系列扫描同口径。
- **纳入判据的是页数与封面这两样。** 它们由扫描本身产出，扫描不补就没有第二条路。内容哈希不在其中——
  缺哈希的书由扫描收尾发起的低优先级哈希回填任务负责补，那条路已经通了。
- **数据已齐的书照旧被跳过，增量扫描的速度不变。** 代价只落在真的缺数据的书上：内容为空或读不出页的
  坏档页数恒为 0，会在每次普通扫描里被重读一次——数量有限，换来的是这批书一旦修好就能自愈。

---

### 🏷️ v1.5.0 — 2026-08-31（库内软链指向同库内的真实目录时，同一批文件重复入库）

> 资料库里如果有一条软链指向**同一个库内**的真实目录——按作者、按状态做二次组织时很自然会这么建，例如
> `<库根>/Alpha (link) -> <库根>/Series Alpha`——同一批文件会以两条路径各入库一次：磁盘上的一个文件变成
> 两本书、两个系列，`book_count` 翻倍，去重、统计与阅读进度各算各的。原因是遍历的去重集合只登记递归入口
> （库根，以及每一条被跟进的软链目标），普通子目录从不进去，于是真实目录那一棵被普通遍历走过之后仍不算
> 「走过」，软链再把它走一遍。文档鼓励的那种「软链指向库**外**的外置盘目录」不受影响；两条软链指向同一
> 目录的菱形、以及 a 与 b 互指的环，从前就已经挡住了。修好后这类链被跳过，留下来的是**真实目录**那条路径。
> 数据库结构与线上契约均未改动。**已经重复入库的用户**：新的扫描不会再造出重复，但旧的那一份不会自己
> 消失——链接路径在磁盘上仍然存在，清理判定它没有缺失。要清干净，可以把库内那条软链删掉（或移出库根）
> 之后对该资料库执行一次清理；想留着这条链当组织视图的话，就在界面上直接删掉多出来的那个系列——它的路径
> 是链接那一侧的那个。被删掉的那份上如果记过阅读进度会一并消失，真实目录那份不受影响。

#### 修复
- **遍历把每一个走过的真实目录都登记进去重集合，不再只登记递归入口。** 身份取 `filepath.EvalSymlinks`
  解析出的真实路径，而不是 inode/设备号：后者要 `syscall.Stat_t` 的 `Dev`/`Ino`，Windows 上没有对应物，
  而 `build.sh` 要交叉编译出 Windows 产物。去重集合仍是每趟遍历现建、作用域只到这一趟，因此两个资料库
  各自软链到同一块外置盘的同一个目录时互不相干，谁也不会把对方的内容整个吞掉。
- **软链目录攒到本层走完之后再跟进。** 本层的真实目录到那时已经全部登记在案，指回同一棵树的软链才认得
  出来。这个次序同时定死了两条路径都通时留下来的是真实目录那条，与链接名的字典序无关：留链接那条的话，
  用户哪天删掉这个随手建的组织视图，整个系列在下次扫描时就跟着没了。

---

### 🏷️ v1.5.0 — 2026-08-31（`/` 要按 Shift 才打得出的键盘布局上，搜索快捷键打不开）

> 德语 QWERTZ 的 `Shift+7`、法语 AZERTY 的 `Shift+:` 这类要按 Shift 才能打出 `/` 的布局上，按 `/` 打不开
> 全局搜索，只会开关快捷键帮助面板——这些用户等于完全用不上搜索快捷键。原因是帮助面板除了认 `?` 这个
> 字符，还顺带认「Shift 加 `/`」这个组合，而 `KeyboardEvent.key` 给出的是**字符**、不是物理键：上述布局
> 打出 `/` 时事件正是 `key` 为 `/` 且 `shiftKey` 为真，于是先被面板那一支吃掉，走不到搜索。美式 QWERTY
> 不受影响——那里 `Shift+/` 打出的字符本来就是 `?`。修好后 `/` 在任何布局上都打开搜索，`?` 照旧开关帮助
> 面板。纯前端改动，数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **快捷键帮助面板只认 `?` 这个字符，不掺 shiftKey。** 用户真打出问号时 `key` 就是 `?`，无论他的布局要不要
  按 Shift、按的是哪个物理键，认字符这一条本身已经足够；再叠一个「Shift 加 `/`」的判断只会误伤那些拿
  Shift 打 `/` 的布局。资源库页的 `/` 聚焦搜索本就只认字符，不受这个问题影响。

---

### 🏷️ v1.5.0 — 2026-08-31（英文界面里计数为 1 的地方语法不对）

> 界面语言切到 English 后，凡是「数字 + 名词」的文案，计数为 1 时名词仍是复数：设置页的
> `1 issues need attention`、系列详情页的 `1 books` 与 `1 pages`、仪表盘的 `Active 1 days in the last 7`、
> 元数据审核的 `1 fields`、资料库工具栏的 `1 results in this library`。原因是词条渲染只做变量替换，
> 名词的复数形式被写死在英文词条里，没有任何一处按计数选形态；作者早先给任务气泡手工写成
> `1 task(s) running` 绕开了这个问题，那是从前唯一的应对方式。中文不受影响——中文本来就没有复数变化。
> 修好后英文按计数取正确形态，`task(s)` 这类将就写法一并去掉。词条 key 一条没增没减，两份语言表仍逐条
> 对齐；数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **词条渲染按 `Intl.PluralRules` 选复数形态。** 形态写在词条**值**里、分段标以分类名
  （`one=…|other=…`），不另立 `_one`/`_other` 这类后缀 key：后缀会让两份语言表的 key 集合按语言分叉，
  而中文只有一种形态，跟着补空壳 key 纯属负担。分类由浏览器原生的 `Intl.PluralRules` 按当前语言算出，
  不是「等于 1 取单数」——换成有 few/many 的语言这套写法照样成立。没有分段的词条走原路径原样返回，
  因此中文维持单一形态，其余上千条未涉及复数的词条一行不用改。
- **英文侧先迁移计数确实能取到 1 的那几条。** 设置页待修正项、系列话数与页数、仪表盘已读册数与活跃天数、
  连续阅读天数、元数据审核字段数、资料库结果数，外加改回正常写法的任务气泡。其余含计数占位符的英文词条
  沿用单一形态，机制已经就位，按需逐条迁移即可。

---

### 🏷️ v1.5.0 — 2026-08-31（系列元数据的并发编辑互相静默覆盖）

> 在系列详情页编辑元数据期间，如果服务端的值被别的途径改了——刮削应用了一条提案、另一个浏览器标签页
> 保存了、另一个用户改了——按下保存会把对方的改动整个盖掉，双方都收不到任何提示。保存是「先清空再
> 重建」的全量替换，因此对方新加的标签、作者与链接会被整批抹掉，不只是简介被覆盖。字段锁挡的是刮削器
> 那类自动来源，从不挡用户自己这次保存，所以锁上也拦不住。修好后，编辑期间服务端被改过时保存会被拒，
> 弹窗与用户敲进去的内容原样留着——不会一按保存就没了——并提示有人改过；用户看过提示后再点一次「仍要
> 保存」，即以自己这一份为准覆盖。数据库结构未改动，升级无需额外步骤；接口只是各多了一个可选字段，
> 不带它的调用方行为与从前一致。

#### 修复
- **系列元数据的手工保存带版本回验，冲突时拒绝而不是照写。** 版本取的是「编辑器管辖的那些字段」此刻
  的内容指纹，不是库里新加的一列自增号：改这些内容的写入方不止一处（手工保存、刮削应用提案、批量编辑、
  扫描增补），没有一个共同的写入口能保证「谁写了谁加一」，漏掉任何一处，那条路径下的覆盖就仍然是静默的。
  指纹由读路径算出，任何来源改了内容，用户手里那份版本自然作废。它刻意只覆盖编辑器管辖的字段——收藏、
  阅读进度、卷数页数与封面这些派生统计不算数，否则用户读完一本书就存不下正在写的简介。校验与写入落在
  同一次事务里，两个同时按下的保存不会双双通过检查。
- **保存的基线版本与表单内容同进同出。** 表单这份内容是从哪一版服务端数据长出来的记在一处，编辑期间的
  后台刷新既不重置表单，也不把这个基线换成新版本——换了就等于替用户认领了别人的改动，冲突检测形同虚设。

---

### 🏷️ v1.5.0 — 2026-08-31（中断的任务显示的处理速率被整段停机时长稀释）

> 任务中心里，**中断**状态的任务显示的「处理速率」是个被系统性压低的数：一个跑了 10 分钟、服务停了
> 一夜才重启的扫描任务，会显示成实际速率的约六十分之一。原因是速率按「开始时刻到结束时刻」算，而中断
> 任务的结束时刻是服务**下次启动**时才盖上去的，整段停机时长都被算进了分母、当成了在干活；停机越久，
> 这个数越离谱。其余三种终态（完成、已取消、失败）的结束时刻由任务引擎在任务体返回的那一刻当场盖上，
> 一直是准的；运行中、已暂停、取消中三种活动态量的是「从开始到此刻」，也不受影响。修好后中断任务不显示
> 速率这一项——它究竟停在哪一秒，进程被强杀的那一刻就没有任何地方记下过。计数、百分比、开始与结束时刻
> 照常显示，重试也不受影响。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **中断任务不再下发处理速率。** 服务下次启动时的那一笔批量盖章把 `finished_at` 与 `updated_at` 一起
  写成了重启时刻，库里因此换不出第二个可信的分母：任务停在哪一秒从来没被记录过，而重启可以发生在任意久
  之后。与其显示一个编出来的速率，不如把这一项去掉——这与四种终态都不发「预计剩余时间」同源，理由同样是
  那个数无从谈起。前端相应地在这一项缺席时整格不渲染，而不是回落成 `0/min`：那看着像「一分钟一条都没
  跑」，是另一种谎。

---

### 🏷️ v1.5.0 — 2026-08-31（存储 IO 面板里同一块盘裂成两行，后台排队数与暂停原因落在空行上）

> 「存储 IO 诊断」面板里同一块盘会出现两行：一行有并发数、没有读者也没有排队，另一行读者数、后台排队
> 数与暂停原因都在，却显示 0 并发、0 上限。资料库卡片上标的卷键只对得上那条空行，于是用户想弄清
> 「后台在等谁、为什么停着」时读到的是假数据——明明有作业在排队也显示成 0。触发条件是卷键含大写字母：
> macOS 的外置盘（`/Volumes`）、家目录里的资料库（`/Users`）都属于这一类，全小写的卷键（`/mnt`、
> `/var`）不受影响。限流本身一直是对的，并发上限、阅读优先与暂停都照常生效，只是面板上的数字串了行。
> 修好后面板报出的卷键与资料库卡片上的那个逐字一致。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **观测快照按调用方传进来的卷键归拢。** 卷键在发放存储令牌那一层被按两套口径加工：限流桶键把它转成
  小写，读者数、后台排队数与可暂停作业数这三张表却按原样存，快照于是拿小写的键去查原样键的表，同一块
  盘裂成两行，计数与暂停原因全落在其中一行上。现在归一收在申领令牌的入口一处，桶键与三张表共用同一个
  键；卷键在这一层只是一个不透明身份，大小写这类归一属于推导它的存储策略那一层——只有那里知道所在平台
  的文件系统区不区分大小写，而面板要显示的正是用户配置里的那个写法。

---

### 🏷️ v1.5.0 — 2026-08-31（无限滚动下做批量操作，前面几屏的卡片纹丝不动）

> 资料库切到无限滚动、往下滚过几屏之后，回头勾选前面某一屏里的系列点「标记已读」或「批量编辑」，
> 服务端其实已经改完了，但界面上那几屏的进度条与标签一点没动——只有最靠近底部的那一屏会跟着更新。
> 用户据此以为操作失败，往往会再点一次。触发条件只有一个：无限滚动模式下被操作的系列不在最后一屏；
> 分页模式一次只显示一页，一直是对的，单张卡片上的收藏按钮也不受影响。此前只能手动刷新整个页面
> 才看得到真实状态，修好后不必再刷。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **列表刷新的覆盖范围跟着屏幕上显示的范围走。** 无限滚动把多屏结果累积在同一个列表里，而批量操作、
  刮削、重新扫描之后的静默重取只取了当前页码那一屏，前面几屏因此停在旧数据上。现在无限滚动下的刷新
  会把已累积的每一屏都重取回来，按 id 就地更新，列表的条数与顺序不变；分页模式屏幕上只有当前页，
  仍然只重取当前页并整页替换，好反映新增与删除。刷新走的是 offset 分页，不依赖也不改写向前滚动用的
  游标链，接着往下滚不受影响。

> 用不支持 OPDS-PSE 的传统 OPDS 客户端浏览书目时，条目里那条充当封面/预览的「首页」图片链接取回的
> 是第 2 页；只有一页的归档更直接拿到 404，在书架上显示成破图。触发条件是客户端读取书目 feed 里的
> 第二条获取链接，与藏书内容无关，每一本书都是这样。支持 PSE 的阅读器（KOReader 等）用的是同一条目
> 里的流式链接，翻页与阅读进度一直是对的，不受这个问题影响；整卷下载也一直正常。修好后客户端刷新
> 一次书目即可，藏书不必重扫。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **书目里的「首页」链接按 OPDS 的页码口径下发。** OPDS 页图路由的页码是 OPDS-PSE 规定的 0 基下标
  （`pages/0` 就是第一页），而这条固定链接写成了 1 基的 `pages/1`，于是稳定地偏一页；单页归档没有
  第 2 页，于是 404。两套下标口径此前各写各的：路由与 PSE 流式模板按 0 基，这条链接按 1 基。现在
  链接的生成与请求的换算收在同一处，页号不再由调用点各自拼写。

---

### 🏷️ v1.5.0 — 2026-08-31（任务停下来了，百分比还停在上一帧，已停的任务还在倒数「剩余时间」）

> 任务中心里一条刚跑完的任务，计数与百分比会互相矛盾：`refresh_koreader_matching` 完成时显示
> `2 / 2` 却配着 `50.0%`——那个百分比是收尾前最后一次进度上报留下的。同一处还有第二个症状：
> 失败、中断、已取消的任务旁边照样挂着一个「预计剩余时间」，而它们已经不会再动了；对可重试的
> 中断任务尤其误导，看上去像是还在跑，用户因此不会去点重试。两个症状同源，都是任务收尾那一帧
> 没有重算派生字段。修好后旧任务不必重跑：百分比在任务列表读回时就按计数重算。数据库结构未改，
> 接口字段没有增删（终态不再下发 `eta_seconds`），升级无需额外步骤。

#### 修复
- **任务的百分比与速率跟着当帧计数重算。** 任务表此前有九条写入路径，百分比、速率、ETA 这几个
  派生字段靠每条路径各自记得算，而收尾的那两条（完成 / 已取消、失败）恰好没算，于是终态那一帧
  原样带着上一帧的数字。现在派生字段收口到任务表的唯一写入出口上，写入即重算，九条路径没有一条
  再有机会漏；连带修好的还有终态那一帧的平均速率——它此前同样是收尾前最后一帧的陈值。
- **已经停下的任务不再显示「预计剩余时间」。** 完成、已取消、失败、中断四种终态一律不再算 ETA：
  任务不会再推进，剩余时间无从谈起。停下来的任务要回答的是「做完了多少」（计数与百分比）和
  「花了多久」（详情面板里的开始与结束时刻），这两样都还在。运行中、已暂停、取消中的任务照旧
  显示 ETA，一点没变。

---

### 🏷️ v1.5.0 — 2026-08-31（下载中删掉的书又复活成读不了的 100%，改画质重下则把页数算成两倍）

> 一本已下载过的书，改了画质再点「缓存本书」，下载还在跑时点旁边的「删除」（按钮当时仍可点）：
> 下载跑完后这本书会重新出现在书架上，显示已缓存、100%，但离线打开读不了——删除时清掉的页清单
> 与书籍信息不会再下一遍，阅读器拿不到页清单。同一条「重下同一本书」的路上还有第二个症状：改过
> 画质、格式、滤镜或自动裁边之后再下一次，页数按两套 URL 重复计数，进度条冲到 200%，磁盘占用
> 实打实翻倍，而这多下的一套字节根本读不到——Service Worker 读页图时忽略查询参数，命中的始终是
> 先存下的那一份。改 N 次偏好翻 N 倍。修好后已下载的书不必重下：页数按页路径去重，进度条立刻
> 回到正确值；但此前重复下载多占的那份磁盘要重下一次这本书（或删掉它）才回收得了。数据库结构与
> 线上契约均未改动，升级无需额外步骤。

#### 修复
- **下载收尾前先确认自己还算数。** 下载开始时给这本书的索引条目盖一个本次下载的令牌，每下一件
  之前和收尾时都比对一次；对不上就说明这次下载已被作废——用户删了这本书、换用户清空了索引、或
  另一次下载接管了它。作废时立即停手不再耗流量，并把本次已写下、且当前索引没有认领的字节删掉，
  然后如实回报「这本书不在本机」，界面据此回到「未缓存」。被删掉的书不会再被收尾写回索引。
- **下载期间禁用删除按钮。** 与上一条是两件事：禁用只是让用户点不到，而收尾仍可能与换用户对账等
  别的路径撞上，所以作废检查照做。
- **页图按不带 query 的页路径落盘，一页一份字节。** 页图地址里的画质、格式、滤镜只是同一页的不同
  渲染，彼此可替代——Service Worker 读图正是忽略查询参数命中的。带 query 落盘就让同一页存下多份
  互不可达的字节：计数把它们各算一页，读取只取其中一份。现在请求仍按用户选的画质发出，落盘统一
  归到页路径上，重下覆盖同一批键；已缓存页数也按页路径去重，改过画质的老书立刻显示正确的进度。
  重下时还会清掉这本书名下不属于本次下载的键，把此前重复占用的磁盘还给用户。

---

### 🏷️ v1.5.0 — 2026-08-31（断网后刷新一次，离线书架和下载好的书全被挡在登录页外）

> 在线时把书下载到离线书架，之后断网（飞机、地铁）刷新页面或从 PWA 图标冷启动，打开「离线书架」
> 或某本已下载的书，看到的都是登录页——而登录同样需要网络，用户无路可走，离线阅读事实上不可用。
> 触发条件是断网加一次刷新或冷启动；只有「在线登录后保持标签页不刷新、断网再走站内跳转」这一条
> 窄路读得到离线书。这次放行的隔离保证到这个程度：共享设备上换过账号之后，新用户无论从界面点还是
> 直接输 URL 猜书号，都读不到上一个人下载的书，换人时那些缓存字节也会被删掉；但放行判据全部来自
> 本地（书目索引与归属标记都在 `localStorage` 里），能打开开发者工具改它的人——也就是这台设备的
> 持有者本人——伪造得出来。纯前端挡不住这一类使用者，真要隔离得靠系统账户或浏览器配置分开。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **连不上后端时放行离线书架与本机已下载的书。** 鉴权闸门此前只认「有没有登录用户」，而登录态探测
  （`GET /api/auth/status`）是普通 API，Service Worker 明确不接管它——断网时这个请求直接失败，
  探测被当成未登录，闸门于是把人送去同样需要网络的登录页，连预缓存好的离线书架页都进不去。现在
  探测失败时区分「后端答了错误码」与「根本没走到后端」（`backendUnreachable`），只有后者才进入
  离线态。后端答 4xx/5xx 一律照旧走登录页：它能应答就轮不到离线态，而且此时 Service Worker 的
  阅读回退也救不了场——它只在请求抛错时才回落缓存，错误码是原样透传的。
- **离线放行逐本认，不是一张通行证。** 放行范围只有离线书架与 `/reader/<已下载的书>` 两条路由，
  且要求本机存在归属标记、书号确实在当前用户的书目索引里；书号是可枚举的小整数，认「这台设备上
  有人下过东西」等于把所有书号一起放开。其余页面在离线态下仍回登录页——没有后端也没有内容可看。
  在线时的判定一点没松：未登录、须改密、管理员专属页面的行为全部照旧。
- **换用户时把上一个人的缓存字节也删掉。** 归属对账原先只清书目索引与待同步队列，已下载的书页
  留在 Cache Storage 里；Service Worker 的离线回退不经任何服务端鉴权，这些字节是设备上唯一还能
  泄露内容的东西。现在换人与登出都顺带整份删除离线书缓存。删除是异步的、可能失败，所以它只是清扫：
  隔离由同步清空的书目索引和按索引逐本放行的闸门判据保证。

---

### 🏷️ v1.5.0 — 2026-08-31（跨页选择后转移到外部资源库，报了成功却什么都没转）

> 在第 1 页勾选若干系列、翻到第 2 页再点「传输到外部资源库」，界面弹出绿色提示「所选系列已全部存在
> 于外部资源库」，确认框不打开，传输根本没有发起——而那些系列其实一本都不在外部资源库里。触发条件是
> 管理员开着外部资源库会话，且选中的系列里有不在当前页的（批量选择是跨页保留的，选择栏专门区分
> 「已选择 N 项 · 当前页 M 项」）。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **发起传输前按 id 补查跨页选中的系列。** 外部资源库覆盖表只装当前页的系列——每次拉取都整份替换成
  当前页的结果，系列列表同样只有当前页。于是第 1 页选中的那些系列，「共几本」与「已有几本」双双算成
  0，「需复制」跟着是 0，一路落进「已全部存在」那条分支。现在只把不在覆盖表里的选中项单独问一次
  后端，已在表里的不重问——翻一页不会变成整份选择集的全量重查。
- **状态问不到时不给「已全部存在」的结论。** 「需复制 0」能出自两种情形：所选系列确实都在外部资源库
  里，或者根本没问到它们的状态。现在只有每个选中系列的覆盖都问到了才提示「已全部存在」；有任何一个
  问不到就照常打开确认框交给用户决定，补查请求失败则提示「无法确认所选系列在外部资源库中的状态，
  请重试」。信息不足时绝不静默跳过用户的操作、还报一条成功。

---

### 🏷️ v1.5.0 — 2026-08-31（KOReader 一开始同步，管理界面就整片 500）

> 只要有任意一台 KOReader 设备成功推上第一条阅读进度，整个 KOReader 管理面就永久报错：设置页的
> KOReader 面板、账号列表、连接中心三处同时白屏，对应 `GET /api/system/koreader`、
> `GET /api/system/koreader/accounts`、`GET /api/system/client-connections` 三个接口一律 500。
> 改配置的 `POST /api/system/koreader` 同样报「保存失败」——配置其实已经写进库里，只是它末尾复用
> 读取接口回响应。触发条件是 `koreader_progress` 表里存在任意一行，同步本身自始至终正常工作。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **聚合出来的时间列按文本读回再解析。** SQLite 驱动只在结果列有声明类型（`DATETIME`）时才把库里
  存的时间文本转成 `time.Time`，而账号列表的 `last_used_at` 与统计的 `latest_sync_at` 都是
  `MAX(updated_at)` 包在标量子查询里算出来的——声明类型在表达式列上不存在，驱动原样返回字符串，
  扫进 `sql.NullTime` 直接报 `unsupported Scan`，整个查询连同它服务的三个接口一起失败。
  进度表为空时 `MAX(...)` 返回 NULL，`sql.NullTime` 收得下，所以此前一直看不出问题。现在这两列
  与设备诊断的 `latest_sync_at` 一样，先按 `sql.NullString` 读回，再交给同一个时间解析函数。

---

### 🏷️ v1.5.0 — 2026-08-31（取消后台任务后，任务中心不再把进度显示成满格）

> 取消一个后台任务，任务中心当场把它的进度写成满格：1000 本的资料库扫到第 30 本按下取消，界面显示
> `1000 / 1000`、`100.0%`，看起来像整库都扫完了。触发条件是取消任何一个在任务声明里报了总数的
> 任务——资料库扫描、指纹重建、外部库传输、KOReader 匹配刷新都算。已经落库的历史记录不做回填，
> 升级后新取消的任务才显示真实条目数。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **已取消的任务停在真正处理到的条目数。** 终态写入处对**完成**与**已取消**一视同仁，一律把已处理
  条目数补到总数，而它正是已取消任务唯一的写入处：一本都没读的那 970 本就这样被算成了做完，
  「做完了多少」这个数因此答错。现在补齐只发生在**完成**这一条上，**已取消**与**失败**、**中断**
  一样停在任务停下时真正处理到的那个数，上例显示 `30 / 1000`、`3.0%`。补齐对**完成**仍是必要的
  ——`rebuild_index`、`cleanup_library`、`ai_grouping` 这类任务把总数声明成阶段数却从不推进计数，
  靠它才显示成 1 / 1。

---

### 🏷️ v1.5.0 — 2026-08-31（重启服务后新起的后台任务重新出现在任务中心第一页）

> 重启服务后新发起的任务——扫描、重建索引、清理封面都算——在任务中心里一条都看不到，只有一屏重启
> 前的旧记录：任务确实在跑，界面上却像没起来。触发条件是重启前 `tasks` 表已攒够超过一页（50 条）
> 的记录，一次大库扫描就足够。同源的第二种表现与重启无关：一个长时间不上报进度的大库扫描，会被
> 后来陆续跑完的短任务一条条挤出第一页。数据库结构与线上契约均未改动，升级无需额外步骤；升级后
> 序号从库里已有的最大值接着发，历史记录不受影响。

#### 修复
- **任务序号跨重启单调递增。** 序号是任务中心的主排序键，由内存里的任务引擎发放，而引擎每次启动
  都从 0 重新数；任务表又没有行数上限（内存表那 200 条上限裁不到它，库里的记录只有用户手动清理
  才删）。于是重启后新任务的序号远小于重启前那几千条历史，被一路排到后面，翻不到。现在引擎在装配
  期从库里已用掉的最大序号接上。
- **活动态任务恒排在第一页最前。** 序号只在任务有更新时才递增，一个长时间不上报进度的任务会被后来
  的短任务超过——而它恰恰是用户正等着看的那一个。任务中心的定序因此改为活动态（运行中、已暂停、
  取消中）优先，其后才按最近活动降序；历史任务之间的相对顺序不变。

---

### 🏷️ v1.5.0 — 2026-08-31（扫描 cbr/rar 藏书不再把每本整卷解压一遍）

> 扫描一个 cbr/rar 藏书时，默认档位会为每本变化的书探测一次 `ComicInfo.xml`，而这次探测顺带把整卷
> 图片页全部解压了出来——扫描器紧接着就关掉归档，这份缓存一次都用不上。本该只读几十 KB 的条目头，
> 实际把整个归档从盘上读了出来：一个 24 MB / 60 页的 CBR，未命中的探测耗时 93.4ms、解压并缓存 60 个
> 条目共 24,576,000 字节，修复后是 372.8µs、0 个条目 / 0 字节；命中的探测从 9.9ms、7 个条目 /
> 2,457,639 字节降到 174.8µs、1 个条目 / 39 字节。机械盘与外置盘上 5000 本 CBR 的库，一次扫描因此
> 少读上百 GB，内存侧也不再为每个归档撑起最大 64 MiB 的会话缓存（还要乘以并发 worker 数）。阅读
> 路径的预取与缓存不受影响，顺序读全 60 页仍是 92.6ms → 97.3ms 的同一量级。数据库结构与线上契约
> 均未改动，升级无需额外步骤。

#### 修复
- **元数据探测与阅读取页各走各的扫描。** rar 是前向只读流，两者此前共用同一个前滚函数：它为顺序
  阅读预取途经的图片页，于是只要目的是探测元数据，整卷页字节也照解不误——注释写着要避免的正是这件
  事，代码却在做。现在探测单开一个临时 reader 逐条目头前进，只解压宽松匹配命中的那一个条目，途经
  条目由 `rr.Next()` 零解压跳过；前滚预取只服务 `ReadPage`，顺序阅读的滑动窗口缓存一如既往。
- **探测元数据不再把正在阅读的会话游标打回开头。** 探测此前无条件从头重开归档，读者的下一页因此
  要重新前滚一遍。临时 reader 不碰会话游标，边读边刮元数据不再拖慢翻页。

---

### 🏷️ v1.5.0 — 2026-08-31（带搜索词的资料库链接刷新后不再被弹回第 1 页）

> 在资料库搜索框输入关键词、翻到第 3 页，按 F5 或把地址栏链接分享给别人打开：页面先正确加载第 3
> 页，约 300ms 后自己跳回第 1 页并重发一次请求，看起来像链接坏了。同一处守卫在相反方向也会出错：
> 某个资料库在本机的第一次访问（浏览器禁用或隔离本地存储时则是每次访问），翻到第 3 页后第一次改
> 排序、每页数量或任一筛选，页码不回到第 1 页，且旧排序下算出的游标被带进新查询，那一页显示的是
> 错位、跳过的结果。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **页码与游标的重置只认用户操作，不再靠数水合轮数来避让。** 守卫此前是一个「跳过水合那一轮」的
  开关，可水合到达页面的轮数并不固定：带 `q` 的深链要两轮（先是筛选项，300ms 后才轮到防抖出来的
  关键词），首访且本地无存档时则一轮都不改。挡少了就把深链里的页码冲回第 1 页，挡多了就白吃掉用户
  的第一次操作。现在判据落在状态自己身上——`useLibraryFilters` 只在公开 setter 被调用时记一笔用户
  改动，`useResetPageOnFilterChange` 只认这一笔，水合分几轮到达都与它无关。
- **反向的两条行为保持不变。** 深链 `?page=3` 仍停在第 3 页；从 A 库切到 B 库仍恢复 B 库存档里的
  页码，且切库之后第一次改筛选照样回到第 1 页并清空游标。

---

### 🏷️ v1.5.0 — 2026-08-31（编辑元数据时，后台任务完成不再冲掉没保存的输入）

> 打开系列元数据编辑弹窗，改了简介、加了标签还没点保存，此时任一后台任务完成——扫描、刮削、封面
> 重建都算——编辑框里的内容就被静默换回服务端版本。弹窗还开着，用户不一定立刻发现，回头点保存，
> 存下去的是服务端那份，用户在编辑期间点过的字段锁也一并被抹掉。数据库结构与线上契约均未改动，
> 升级无需额外步骤；已经被冲掉的输入没有留存副本，需要重新填一遍。

#### 修复
- **表单重置同时看「是否在编辑」与「是否换了系列」。** 任务的进度与完成事件会推动 `refreshTrigger`，
  系列上下文随之静默重取，`series`/`tags`/`authors`/`links` 换成新引用（内容可以一模一样），表单
  重置 effect 就跟着跑，把用户正在写的内容盖掉。现在编辑态下只有换了系列才重置：同一系列的后台
  刷新不再动编辑框，非编辑态仍照旧跟随服务端最新值。
- **编辑期间切到别的系列，表单跟着换成新系列的值。** 「表单里这份内容属于哪个系列」记在 ref 里（与
  `useSeriesContext` 判断切换同一套写法），与新到达的 `series.id` 不符即重置——不会因为「正在编辑」
  就把上一个系列的简介与标签留在框里，跟着被保存到新系列头上。

---

### 🏷️ v1.5.0 — 2026-08-31（快速切换系列时，详情页不再挂着上一个系列的内容）

> 在关系图、合集或作品群里连点两个系列，第一个系列的 `/context` 还没回来就跳到了第二个：地址栏
> 是系列 B，简介、书列表、标签与提案却全是系列 A 的——A 的响应晚到一步，把 B 的内容整个盖掉了。
> 页面上还没有任何加载指示，用户不知道自己看的是上一个系列，在这一页上做批量已读，进度就写到了
> A 的书上——这是真的写错数据，且从界面上看不出来。切系列瞬间点保存也会把 A 的 title/summary/tags
> 打到 `PUT /api/series/info/{B 的 id}`。数据库结构与线上契约均未改动，升级无需额外步骤；已经被
> 写错的阅读进度与元数据没有留存副本，需要人工改回。

#### 修复
- **系列上下文按世代号收口，迟到的响应不再写页面。** `/context` 在发起时取一个世代号，写内容、写
  错误、清加载态三条路径都只认最新一次请求（与 `useLibrarySeries`、`useSeriesScraping` 同一套写
  法）。连点系列时 A 的响应到得再晚，也只会被丢弃。
- **切系列时先清空上一个系列的内容，并显示加载态。** 加载态此前由 `!series && books.length === 0`
  推断，而 effect 里读到的是上一轮渲染的闭包值——切系列时它还是上一个系列的非空值，于是加载态直接
  为 false。现在「当前内容属于哪个系列」记在 ref 里，与新的 `seriesId` 不符即判为切换：清空内容、
  显示加载指示；同一系列的刷新仍然静默，不闪加载态。
- **切系列窗口内的保存不再落到新系列 id 上。** 保存以 `series` 非空为前提，而 `series` 在切换瞬间
  已被清空，直到新系列的上下文到达才重新有值，那段「id 是 B、内容是 A」的窗口就此不存在。

---

### 🏷️ v1.5.0 — 2026-08-31（保存的智能视图不再丢掉高级筛选条件）

> 在资料库里设好「未读 + 评分≥8 + 最近 30 天加入」，存成智能视图，之后应用它——这三个条件被静默
> 清空，只剩标签、作者、状态、首字母与排序，结果集比保存时大得多，视图看上去像是坏了。保存按钮的
> 启用条件本来就把高级筛选算在内，于是只设了高级筛选、没设标签作者的那一次，存出来的是一个条件
> 完全为空的视图。后端 `smart_filters` 的六列与 `UpsertSmartFilterRequest` 一直都收这些字段，丢
> 的是前端这一侧。数据库结构与线上契约均未改动，升级无需额外步骤。但此前存下的视图，库里那六列本
> 来就是空的，升级不会把它们凭空补回来：需要重新设好高级筛选，再**用原来的名字存一次**——同名保存
> 会就地覆盖那条视图，不会多出一条。没有重存的旧视图按「这六维都不筛选」应用，与升级前表现一致。

#### 修复
- **保存视图时把六个高级筛选维度一起发给后端。** 阅读状态、评分区间、进度区间、加入天数此前根本
  没有进过请求体，落库的那条视图从一开始就是残缺的——应用时的清空只是这件事的显影。现在
  `saveSmartFilter` 收下当前的 `AdvancedFilters`，六个维度与标签、作者一样是视图的组成部分。
- **读回与应用时把六个维度还原成筛选状态。** 从后端读回的行经 `normalizeRemoteSmartFilter` 补齐
  缺省，应用时经 `smartFilterToSnapshot` 落回 `AdvancedFilters`。缺省一律补 `null` 而不是留空：
  「这一维不筛选」判的是 `!== null`，`undefined` 会被当成设过，凭空多出一个不可见的筛选条件。
- **「一条视图由哪些维度组成」只由一处回答。** 这份字段清单此前在保存、旧数据迁移、应用三处各写
  一份，只有其中一份漏了高级筛选，前端就此与后端的六列漂移。现在三处都问
  `smartFilterRequestBody` 与 `smartFilterToSnapshot`，新增筛选维度时不必再重新想一遍。
- **保存失败后不再在视图列表里留下幽灵条目。** 保存是乐观插入的，请求失败时只弹了提示、没有回滚，
  留下的条目带着客户端 id，后端并不认识它：用户去删只会拿到 404，再被删除那侧的回滚原样放回来，
  只能刷新页面才消得掉。现在保存失败与删除失败一样会把列表还原。

---

### 🏷️ v1.5.0 — 2026-08-31（页数未知的书，阅读进度不再被钉死在第 1 页）

> 用快速扫描首次入库的书页数记作 0。此后在阅读器里读到第 42 页，上报返回 200「Progress updated」，
> 落库的却是第 1 页：下次打开这本书从头开始，「继续阅读」、系列已读统计、读完判定跟着一起错。同一个
> 页码改走离线队列补传反而写得对——同一份输入，两条端点结果不一致。数据库结构与线上契约均未改动，
> 升级无需额外步骤：已被写成第 1 页的进度没有留存副本，需要重新翻到原处上报一次。这些书的页数本身
> 仍要等一次强制扫描才补得上（普通扫描因文件未变而跳过），但在那之前进度已能正常记录。

#### 修复
- **页数未知时不再把进度夹成第 1 页。** 单本进度端点先把页码夹到 `page_count` 上界、再把小于 1 的
  抬回 1。`page_count` 为 0 表示页数未知，不是「这本书只有 0 页」，于是任何页码都先被夹成 0、再被
  抬成 1，永远落在第 1 页。现在上界只在页数已知时才生效，页数未知的书按用户报的页码原样记录；
  页数已知的书照旧夹到上界，小于 1 的页码照旧归一化为 1。
- **「把用户报的页码夹进本书合法范围」只由一处回答。** 这条边界此前由单本进度、批量进度同步、阅读
  书签三条写入路径各写一份字面量，只有单本那份漏了「页数未知」的判断，同一份输入于是在两条端点上
  给出不同结果。现在三处都问 `clampPageToBook`，日后新增写入点不必再重新想一遍。

---

### 🏷️ v1.5.0 — 2026-08-31（缩略图选 AVIF 不再把封面生成拖成小时级）

> 在设置页「媒体」里把缩略图格式选成 AVIF 之后，生成一张 400px 封面要 55 秒，同一张图换成
> WebP 只要 60 毫秒——慢了约 480 倍。千本规模的库首次扫描，封面阶段从分钟级变成小时级：扫描
> 显示早就完成了，封面却整夜一张一张往外冒，封面 worker 全程占满 CPU。阅读页带 `?format=avif`
> 取图、AI 放大把 AVIF 中间态落盘也走同一条编码路径，同样中招。现在同一张封面 115 毫秒编完。
> 数据库结构与线上契约均未改动，升级无需额外步骤：已经生成的 AVIF 封面本身是完好的文件，原样
> 保留，不需要重建；此后新生成的封面体积比编码库的默认档还小两成左右。

#### 修复
- **AVIF 编码不再掉进最慢档。** 编码参数只填了质量，档位（`Speed`）留空。这个字段的 Go 零值是
  `0`，而在 libavif/aom 里 `0` 恰恰是**最慢**的一档，不是「没指定」——编码库对质量判的是
  `<= 0` 才回落默认值，对档位判的却是 `< 0`，于是零值被当成「用户显式要求最慢档」原样送进
  编码器。两个字段对零值的处理不一致，正是踩空的地方。现在档位一律显式给出。
- **档位取 8，是耗时与体积曲线的拐点。** 实测同一张 400px 封面：最慢档 71 秒 / 11250 字节，
  8 档 99 毫秒 / 13149 字节，编码库默认的 10 档 23 毫秒 / 15250 字节。从 10 降到 8，体积省掉
  约两成而耗时仍在百毫秒级；再往下降到 7 及以下，耗时逐档翻倍，体积却只再省个位数百分比。
  缩略图每本书一张、批量生成，速度权重更高，但也不必把 AVIF 本来的体积优势全部让掉。
- **色度取样同样显式给出。** `ChromaSubsampling` 的 Go 零值是 4:4:4，而编码库的默认值是
  4:2:0——又一处「零值不等于未指定」。交付给客户端的封面与页图取 4:2:0，同档位下比 4:4:4 又小
  又快；AI 放大只喂给外部引擎、不外发的中间态取 4:4:4 加满质量，保住细节。
- **「AVIF 该用什么参数」只由一处回答。** 这套参数此前在两个调用点各写一份字面量，两处都漏了
  档位。现在封面/页图与放大中间态各自问 `avifDeliveryOptions` 与 `avifIntermediateOptions`，
  日后新增调用点不必再重新想一遍，也不会再漏掉某个字段。

---

### 🏷️ v1.5.0 — 2026-08-31（清理未引用的封面不再顺手删光页图磁盘缓存）

> 跑一次「清理未引用的封面 / 清理缩略图」任务，整个页图磁盘缓存连同 `<cache.dir>/pages/` 目录
> 一起被删光。`cache.dir` 有默认值且会被强制补齐，所以只要开着页图磁盘缓存，这条路径必然踩中。
> 数据库结构与线上契约均未改动，升级无需额外步骤：被误删的页图缓存没有留存副本，首次重新翻阅
> 时会重新解码转码一遍，之后恢复正常。

#### 修复
- **清理缩略图不再删掉页图磁盘缓存。** 缩略图目录就是 `cache.dir` 本身，页图磁盘缓存是它下面的
  `pages/` 子目录；清理器按「不在 `cover_path` 集合里就删」逐文件判定，页图缓存文件一条也不在
  那个集合里，于是整棵子树被删干净，收尾移除空目录时连 `pages/` 自己也没保住。现在清理遍历跳过
  这些子目录，被引用的封面照旧保留、未被引用的照旧清掉。
- **「cache.dir 下哪些不是缩略图」只由一处回答。** 这条边界此前由重建缩略图与清理缩略图两条
  路径各记一份，后者漏了，误删就此发生。现在缩略图目录、页图缓存目录与两者的包含关系都收进
  `config.ThumbnailDir` / `config.PageCacheDir` / `config.NonThumbnailDirs`，各清理路径只问它；
  日后新增缓存子目录只需登记一次，不会再漏掉某一条。

---

### 🏷️ v1.5.0 — 2026-08-31（手改的系列标题不被扫描打回目录名）

> 系列里只要有一本书带 `ComicInfo.xml`，一次重新解析（强制扫描、该书 mtime/size 变了、该系列
> 新增一卷）就会把用户在详情页手改的标题打回目录名，A-Z 索引的首字母跟着回退；同时把用户为放行
> 刮削而解开的 `title` 锁重新锁死，此后刮削永远写不进标题。数据库结构与线上契约均未改动，升级
> 无需额外步骤：库里已被扫描锁上的 `title` 保持原样，但现在在详情页解锁一次就一直有效，不会再被
> 下一次扫描锁回去；已经被打回目录名的标题没有留存副本，需要重新填一次。

#### 修复
- **手改的系列标题不再被扫描打回目录名。** 扫描把归档内嵌的 `ComicInfo` 增补进系列时，
  `summary`、`publisher`、`status`、`language`、`rating` 都过了一道过锁函数，唯独 `title` 绕开它、
  无条件写成目录名。判据不只是锁：扫描能给 `title` 的唯一值就是目录名，而目录名已经原样存在
  `name` 列里——库里一旦有 `title`，扫描就不带来任何新信息。现在扫描只给还没有 `title` 的系列用
  目录名兜个默认值，已有的标题原样保留；新系列首次入库仍然取目录名。
- **解锁过的 `title` 不会被扫描重新锁上。** 增补分支里有一句无条件的「系列名默认始终锁定」，
  并把重组后的锁写回库中，用户主动解开的锁于是被扫描悄悄撤销；此后写入器里的
  `if !locked["title"]` 永不成立，刮削与 AI 提案都写不进标题。锁定字段是用户在详情页开合的开关，
  归用户所有：扫描只读它来决定自己能写哪些字段，不再写它。目录名与默认锁只是新系列建行时的初值。
- **A-Z 索引的首字母跟着实际标题走。** 增补分支固定按目录名重算 `name_initial`，用户把标题改成
  中文后，系列仍留在按目录名算出的字母下。现在首字母由实际写入的标题推出，与人工编辑、刮削写入
  两条路径口径一致。

---

### 🏷️ v1.5.0 — 2026-08-31（取消掉的任务，气泡不再永久钉在侧边栏）

> 取消任何一个后台任务后，它的气泡会一直转着 spinner 留在左下角：既不自动消失，也不被
> 「清除已完成」清掉，连关闭按钮都不给，用户只能刷新页面才赶得走它。服务重启后转入中断的
> 任务同样如此。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **取消掉的任务，气泡会自己消失，也关得掉、清得掉。** 后端写进任务状态的是 `cancelled`
  （双 l），侧边栏气泡四处判断却都写成 `canceled`（单 l），永远匹配不上：终态延时移除不安排、
  「清除已完成」跳过它、图标落到转圈那一档、关闭按钮不渲染——一个拼写错误同时造成四种后果，
  是因为同一份终态判断在气泡 hook 与组件里各抄了一遍。现在终态与活动态的判定收进
  `utils/taskStatus`，`isTerminalTaskStatus` 与 `isActiveTaskStatus` 互补，气泡与任务中心都只问它。
- **服务重启后中断的任务，气泡同样赶得走。** `CONTEXT.md` 定义的终态有四种，气泡只认三种，
  中断不在内，于是中断的气泡与被取消的一样永久留在侧边栏。现在四种终态齐全；中断另给琥珀色
  警示图标，不与失败共用红叉——它不是失败，任务体没出错，只是没跑完，可重试。
- **进度条右侧显示的是状态文案，不是词条 key。** 总数未知（`total == 0`）时那一格渲染
  `taskBubble.status.<状态>`，而 `cancelled`、`cancelling`、`interrupted` 三个词条两份语言表里
  都没有，用户看到的是词条 key 本身。现在三个词条在 zh-CN 与 en-US 同时补齐，后端任何状态都
  到不了的死词条 `taskBubble.status.canceled` 一并删除。
- **一个任务正在取消时，气泡头部不再显示「所有任务已完成」。** 侧边栏把活动态只算成运行中与
  已暂停，而取消中也属于活动态：任务体还在收尾，计数却已经归到已完成那一侧。现在活动态含
  运行中、已暂停、取消中三种，与任务中心的口径一致。

---

### 🏷️ v1.5.0 — 2026-08-31（弹窗里打字，焦点不再被抢到关闭按钮上）

> 全局搜索弹窗与「新增/编辑资料库」表单里打字，打到一半焦点会跳到右上角的关闭按钮，后续按键
> 全部丢失，空格还会直接把弹窗关掉；扫描进行中即使不打字，焦点也会随进度推送被反复夺走。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **弹窗里打字，焦点不会再被抢到头部的关闭按钮上。** 共享弹窗壳 `ModalShell` 把 `onClose`、
  `closeOnEsc` 放进了 effect 的依赖数组，而调用方普遍传内联箭头（`onClose={() => setOpen(false)}`），
  每次外壳渲染都是新引用，effect 随之卸载重装：卸载时把焦点还给打开弹窗的那个元素，重装时再用
  `requestAnimationFrame` 把焦点抢回弹窗的第一个可聚焦元素——也就是头部的关闭按钮。而搜索词与
  资料库表单值都是外壳的 state，在弹窗里打字必然让外壳重渲染，于是每敲一下就把焦点抢走一次；
  搜索响应返回、SSE 推扫描进度同样触发重渲染，不打字也会被夺走。现在这两个入参改由 ref 持有，
  effect 只依赖 `open`，弹窗从打开到关闭之间只装一次；聚焦逻辑也先判焦点是否已在弹窗内。
- **打开弹窗时，焦点直接落在第一个输入框，而不是关闭按钮。** 头部在正文之前渲染，「第一个可聚焦
  元素」永远是关闭按钮，用户每次都得先点一下输入框才能打字。现在优先取第一个输入类控件，没有
  才退回第一个可聚焦元素——焦点仍然必须进弹窗，读屏软件与键盘导航不受影响。
- **多层弹窗叠加时，一次 Esc 只关掉最上面那层。** 每个挂载中的弹窗都往 `document` 上装自己的
  keydown，谁都没有「我是不是最顶层」的判定，一次 Esc 会把所有层一起关掉，Tab 也会被下层的
  焦点陷阱拽回它自己的面板。现在弹窗打开时登记进一个模态栈，键盘只由栈顶那层响应。
- **多层弹窗叠加再经历一次重渲染，关闭后页面滚动能恢复。** 锁滚动前的 `body.style.overflow`
  由每层各记一份，而 React 重渲染时先跑完所有 cleanup 再跑所有 setup，后装的那层会把已经是
  `hidden` 的值当成原值记下来，此后无论怎么关弹窗，整页滚动都卡在 `hidden`。现在原值由模态栈
  在栈从空变满时记一次，栈空时才还原。
- **弹窗打开时，全局快捷键整体让位。** 应用外壳与资源库页的快捷键只判「事件目标是不是可编辑
  元素」，而弹窗里的焦点常停在按钮上，判不出来：按 `/` 会再叠一个搜索框，按 `g`+`s` 会直接路由
  跳走、把弹窗留在新页面上，在资源库页按 `e` 会在弹窗背后切换批量选择模式。现在两处都先问模态栈
  有没有弹窗打开。

---

### 🏷️ v1.5.0 — 2026-08-31（卷视图里的「全选」不再选中整个系列）

> 系列详情从卷列表点进某一卷后，批量选择的「全选」会选中整个系列的书，用户以为只标了这一卷，
> 实际把整个系列都标成了已读。数据库结构与线上契约均未改动，升级无需额外步骤；已经被误标的
> 阅读进度，在同一处重新框选后「标为未读」即可改回来。

#### 修复
- **在某一卷的视图里点「全选」，只选中这一卷的书。** 卷视图（`?volume=...`）只渲染当前卷的书，
  底部选择栏却渲染在这个分支之外、与整页共用，而「全选」取的是系列全部书的 ID，与当前卷无关；
  接着点「标为已读」，`POST /api/books/bulk-progress` 就带上了整个系列的每一本——写错的是数据
  范围，页面上还看不出来。现在全选的范围等于页面此刻渲染的那批书：在卷视图里就是这一卷。
- **点完「全选」后，按钮文案翻成「取消全选」。** 可选总数按「卷数 + 独立书数 + 全部书数」算，
  独立书被算两遍、卷又额外加一遍，永远大于全选实际选中的数量，于是 `allSelected` 始终为假、
  文案不翻转，用户无从判断自己到底选中了什么。现在可选总数与全选实际选中的那批书同源。

---

### 🏷️ v1.5.0 — 2026-08-31（并发刮削不再把候选写串系列）

> 资料库页对一个系列点了刮削、响应还没回来时又对另一个系列点一次，弹窗里的候选与「当前值」
> 对比列可能属于前一个系列。数据库结构与线上契约均未改动，升级无需额外步骤；已经因此生成的
> 错误提案在提案中心拒绝即可。

#### 修复
- **资料库页并发刮削，弹窗里的候选与对比列不再串到另一个系列上。** 卡片只禁用发起刮削的那一颗
  按钮，别的卡片照常可点，于是 A 的取候选请求在飞时用户仍可以对 B 再点一次。取候选的三个请求
  与弹窗里的重搜此前都没有世代判定，谁最后返回谁就写进弹窗状态：B 先返回把弹窗指向 B，A 后返回
  再把 A 的详情、标签与候选盖上去，用户挑一条应用就给 B 生成了一条由 A 的候选拼出来的提案。
  现在取候选与重搜共用一个世代号，只有最新一次的响应能写弹窗状态、能清加载态、能弹错误提示。

---

### 🏷️ v1.5.0 — 2026-08-31（fast 档位的强制扫描不再清空页数与封面）

> `scanner.scan_profile` 设为 `fast_scan` 时，任何强制扫描都会把整库每一本书的页数清零、封面抹掉，
> 且此后不会自愈。数据库结构与线上契约均未改动，升级无需额外步骤；已经被清空的藏书要切到
> `metadata_scan` 这类打开归档的档位再强制扫描一次才能补回来——fast 档位没有页数与封面的来源。

#### 修复
- **`fast_scan` 档位下的强制重扫、重建搜索索引、重建缩略图，不再把整库的页数清零、封面抹掉。**
  fast 档位不打开归档，页数与封面只能沿用已入库的旧值，而扫描开头那份已入库快照此前只在非强制
  扫描下填充，于是强制扫描里「保留旧页数/封面」那条分支永远拿不到旧值，入库时把 `page_count`
  写 0、`cover_path` 写空。这三条路径都以强制扫描发起，而被清空之后 fast 档位既不打开归档也不排
  封面任务，后续的增量扫描又因 mtime+size 没变而跳过这些书，页数与封面再也回不来。现在这份快照
  无论是否强制扫描都完整填充；强制扫描仍然不吃增量跳过——跳不跳过由遍历里那处独立的守卫决定。

---

### 🏷️ v1.5.0 — 2026-08-31（改名重连不再被同轮的库清理抢跑）

> watch 模式下给一本书改名，阅读进度会丢。数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **监听模式下改名一本书，阅读进度、书签、合集归属与阅读清单条目不再丢失。** 改名会产生
  Rename(旧名) + Create(新名) 两个事件，分别给这个库排期一次清理与一次扫描。此前两者共用同一个
  去抖窗口并在同一次 tick 里并发派发，而清理只做 stat、比要走完整棵树并在末尾才写改名重连的扫描
  快得多，于是稳定抢先把改名前那一行当成「文件已消失」删掉，挂在这本书上的记录随之级联消失——
  改名重连在 watch 模式下等于不存在。现在同一个库的清理接在该库这次扫描之后串行；扫描没跑成时
  不清理，改为重新排期等下一个窗口，宁可让幽灵记录多留一会儿也不冒删错的风险。

---

### 🏷️ v1.5.0 — 2026-08-31（分页排序补齐末位唯一键）

> 十处 `LIMIT/OFFSET` 分页的 `ORDER BY` 在排序键平局时没有区分先后的末位键，行序由查询计划决定。
> 排序键不平局时行序逐字不变；数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **提案中心、AI 分组提案、合集、阅读清单、智能书架，以及 OPDS / Mihon 的系列分页，排序末位
  统一补上唯一键。** 这些分页此前止步于置信度、时间、`sort_order` 或名称——都可能整批相等：
  提案是批量入库的，同一批置信度与时间逐字相同；OPDS / Mihon 按 `COLLATE NOCASE` 比名称，
  大小写不同即算相等。平局行之间谁先谁后此前由查询计划决定，翻页因此可能在页边界重复或漏掉
  条目。补上末位唯一键后，同一批数据的翻页与一次性取数逐位相同。

---

### 🏷️ v1.5.0 — 2026-08-31（资料库翻页不再把同一条目发两遍）

> 按加入时间或更新时间排序翻页时，第 1 页的最后一个条目会再次出现在第 2 页开头。
> 数据库结构与线上契约均未改动，升级无需额外步骤。

#### 修复
- **按「最近更新」「最近加入」排序翻页，不再重复条目、也不再漏掉与翻页边界同一秒的条目。**
  这两个排序的翻页从第 2 页起走键集游标，边界谓词把上一页末行的时间当参数与 `created_at` /
  `updated_at` 比大小。这两列由 `CURRENT_TIMESTAMP` 写入，存的是 `2026-05-01 12:00:00` 这样的
  文本，而驱动会把 `time.Time` 参数写成 `2026-05-01 12:00:00 +0000 UTC`——SQLite 比的是文本，
  同一时刻的两种写法既比不出相等、前者还总是排在后者之前，于是倒序把边界行又发了一遍、
  正序则把与边界同一秒的行整批跳过。现在参数按列的存储格式绑定。按名称、收藏、册数、卷数、
  页数排序的翻页不受此问题影响。

---

### 🏷️ v1.5.0 — 2026-08-31（任务体干活的资格收进一个句柄）

> 后台**任务**的任务体干活所需的三样资格——写这个任务的进度、在可中断点问一句**暂停闸门**、
> 发起一次**磁盘作业**——收进一个交下来的**任务句柄**。除下面这一条外，任务的进度、文案、
> **终态**与并发徽章逐字不变；配置结构、数据库结构与线上契约均未改动，升级无需额外步骤。

#### 改动
- **KOReader 的指纹重建不再把被暂停闸门或存储令牌挡下的书计入「已哈希文件数」。** 那些书一个
  字节都没读。此前该任务与文件身份重建对同一个指标给出不同答案——后者是在读盘出错的检查之后
  才 +1——现在两处口径一致：只数真正被读过的书。慢盘上排队与暂停最多，这个数字与实物的偏差
  也最大，而它是任务面板上用户唯一能看到的吞吐量。

---

### 🏷️ v1.5.0 — 2026-08-31（后台磁盘作业收进单一入口）

> 后台**任务**每动一次磁盘，此前都要自己抄一遍同一套仪式：解析存储策略 → 按工种挑并发上限 →
> 申领**存储令牌** → 过**暂停闸门** → 干活 → 归还令牌 → 把等待与暂停的耗时折进指标。全仓八个
> 取用点各一份，`scanner` 与 `api` 还各自维护了一份取令牌的辅助方法与一份逐字相同的上限公式。
> 本批把这套仪式整个收进新的 `internal/diskwork`：调用方交出「干哪一类活、字节落在哪」和一个
> 闭包，其余全部由模块承担。八个取用点逐个转换完毕，旧接口在同一批里删除；另有一处从来就没抄过
> 这套仪式的读盘（KOReader 的书籍指纹重建）一并收进来，见下。
>
> 配置结构、数据库结构与线上契约均未改动，升级无需额外步骤。

#### 改动
- **每个工种能并发几个，改由工种自己决定，不再取决于发起它的是哪个子系统。** 此前同一个工种在
  两个包里会拿到两个不同的上限——`identity_hash` 在扫描器里是 min(扫描并发, 哈希并发)，在维护
  任务里是 min(哈希并发, 归档打开并发)，而它根本不打开归档；`metadata_scan` 同样分叉。现在按
  「这个工种到底在干什么」裁定：读归档元数据用归档打开并发、算文件指纹用哈希并发、构建封面用
  min(归档打开并发, 封面并发)、写缓存用封面并发。
  - **影响面只有手工把某个库的四项并发配成不同正值的部署。** 四种存储档位的内置值一个都不受
    影响：默认档与 SSD 档四项全为零（本就不限流、不排队），外置硬盘档与网络存储档四项全为一，
    新旧公式在它们上面结果完全相同。
- **按下暂停后，后台不再继续往盘上写缩略图。** 此前从上一次暂停检查到缩略图落盘之间隔着整个
  图片转码，暂停在这一段是不生效的——用户按下暂停，盘上仍会继续多出文件。现在写文件之前必过
  一次闸门。
- **存储策略的解析源统一成「字节落在哪」。** 此前有五处按**资料库根**解析——扫描器的归档打开与
  两处指纹取这次扫描的库根，维护子域的两处指纹回填取那本书所属资料库的路径——现在一律按真正被
  读写的那个文件的路径解析。
  存储策略走最长前缀匹配，因此只有**配了比库根更深的策略条目**时，这几处生效的档位才会变；
  只按库根配策略的部署不受影响。缓存目录的写入另有一条规则：卷键仍由缓存目录本身决定，而 IO
  策略在它与藏书同在一块盘上时服从藏书那一侧——它通常落不进任何资料库的前缀，却和藏书抢同一
  块盘。
- **慢盘上，KOReader 的书籍指纹重建此后为阅读让路。** 这个任务逐本读完整个文件算指纹，此前
  全程不取令牌——外置硬盘档与网络存储档下，用户一边翻页一边跑指纹重建，盘被两边一起抢。现在
  它和其余磁盘作业同形：过闸门、取令牌，因而遵守「为阅读让路」与「只在空闲时干重活」两项设置，
  等待与暂停的耗时也进了任务面板。匹配刷新任务的第一阶段就是这次重建，一并受益。
  默认档与 SSD 档不限流，不受影响。
- **慢盘上「等令牌等了很久」现在九处都会记日志。** 此前只有封面构建与缓存写入两处会留下记录，
  扫描、指纹与 ComicInfo 回写卡在令牌上是无声的——而它们才是慢盘上真正会堵住的那几处。日志带
  工种、路径、存储档位、卷键与等待毫秒；等了很久最终被取消的那次申领也会留下记录。缩略图落盘
  自己那条慢日志不变，它另有一半在计写入耗时。

#### 架构
- 新建 `internal/diskwork`，位于 `config`、`storageio`、`taskcontrol` 三个叶子包之上，由
  `scanner`、`api` 与 `koreader` 共用。对外只有一个入口：交出一份**磁盘作业**声明和一个闭包。
- **三条契约由结构而不是调用方的记性保证**：闸门必然先于令牌，被暂停的作业不会攥着令牌把别人
  堵死；令牌的归还不依赖闭包走哪条出口，闭包 panic 时同样已归还；能并发几个只由工种决定。
  相应地，两处为「提前归还 + 兜底归还」手写的幂等布尔、一处为「取到令牌之后再看一眼取消」
  手写的 `select` 一并消失。
- **等待与暂停的耗时是返回值而不是副作用**，模块不持有也不上报任何指标；取用点上只剩一行
  「把它折进我的指标」，此前逐处手写的耗时折算与 `nil` 守卫沉进两个包各自的那一个方法里。
- **令牌的持有区间钉死在真正的 IO 上。** 归档打开那一处此前握着令牌多跑了一段纯 CPU 的路径
  解析、复合哈希与排序号提取，ComicInfo 回写那一处多跑了一段聚合与序列化——在慢盘上这些都在
  虚占归档打开的并发额度。
- **两份一模一样的指纹回填循环合成一处。** 低优先级的全量哈希回填此前在 `api` 里重抄了一遍
  KOReader 的**指纹**重建（数缺口 → 按游标切批 → 读一遍文件 → 回填 `file_hash`）；本批抹平了
  两者最后一处差异——取不取令牌——之后，它收成一个薄适配器：批大小、批间停顿与匹配模式作为
  选项交给 `koreader`，`api` 只留自己的任务指标与节奏常量。坏书那条警告日志的文案随之统一成
  一条（「Failed to fingerprint book」）。
- 前台的阅读取页**不是**磁盘作业，仍直接向调度器申领：它不过暂停闸门、上下文刻意去掉取消、
  耗时进请求诊断而非任务指标，收进来只会换得一个「这几件事都反着来」的开关。
- 删除两个包各自的取令牌辅助方法（含 api 那份里没有任何调用点的封面构建死分支），以及那条从
  扫描器内部伸手进未导出方法、靠固定时长 `sleep` 去测 `storageio` 自身排队语义的用例——该包
  已有一条等价用例。扫描器包内不再有任何用例触碰取令牌的内部。
- **新增覆盖**：模块的契约用例压在唯一的新接缝（构造点）上，注入新建的调度器与桩配置函数，
  无数据库、无文件树、无扫描器，覆盖四个工种的上限、短路、闸门先于令牌、取消、归还的必然性
  （闭包返回错误与闭包 panic 两条路径）与 `Stats` 如实返回。另补上三组此前**零覆盖**的失败
  出口：归档打开失败与页目录读取失败、封面那一页读不出、指纹算不出来时的分页游标推进——它们
  正是本次删掉手写归还闭包之后唯一可能漏掉令牌或卡死游标的地方。KOReader 的书籍指纹重建另有
  三条：哈希令牌被占满时它不该动手、前台正在读这块盘时它该让路、每本读盘的实况要落到任务面板的
  存储 IO 那一行。

---

### 🏷️ v1.5.0 — 2026-08-31（元数据提案独立成子域）

> 元数据提案的入队、裁决与读取从 `internal/api` 整体搬进新的 `internal/proposal`。
> 绝大部分是内部改动，用户可见的只有下面两条。无数据库改动，升级无需额外步骤。

#### 改动
- **批量处理时「已被别人抢先」的条目不再报成失败。** 批量应用与批量拒绝的响应各新增一个
  conflict 桶，收「这条提案已经不是待裁决」这一种结局——最常见的情形就是另一个标签页、
  另一个人或上一次操作已经处理过它们。此前它们混在 failed 里，于是一堆并没有坏掉的东西
  会让整条汇总提示变红。现在 failed 只收真故障（查询报错、写入失败、提案查不到），
  提示的严重度也只看 failed。两个语种的批量结果文案各加了一项「已被处理 N 项」。
  - 单条应用与单条拒绝的 409 语义不变。
- **打开有多条待裁决提案的系列时不再随提案条数发起额外查询。** 此前系列详情页在提案列表上
  逐条取字段行（N 条提案 = N 次查询），收件箱那条路径早就是批量取。两处现在同一条通路。

#### 架构
- 新建 `internal/proposal`，承载提案子域的全部规则：入队去重（对待裁决提案与最近的拒绝记录
  逐字比对）、按**锁定字段**过滤、应用与拒绝，以及三条读路径（按系列列出、
  收件箱分页、待裁决计数）。元数据写入器一并迁入，那条把事务句柄递出去的 hook 参数随之消失。
- **每一种结局都是结果值而不是哨兵错误。** 入队的五种（已入队 / 复用既有提案 / 无差异 /
  差异全被锁 / 此前已拒绝）、应用的六种、拒绝的三种各由一个分类表达，`error` 只留给故障。
  四个哨兵错误与 api 侧四个 handler 里的 `errors.Is` 一并删除——此前「良性结局不退化成
  500」全靠「每个哨兵都必须被识别到」这条纪律撑着，漏一个，用户看到的就是「服务器错误」
  而不是「先去解锁字段」。被抢先无论是前置检查发现的还是事务内 CAS 撞上的，都是同一个分类。
- 「锁定状态按**当前**值算」从视图映射里的一个 `||` 迁进模块，成为字段上的终值。它是一条
  裁决规则而不是渲染细节：它防的是「先入队、后加锁」的字段没有锁徽章、用户点了应用却被
  静默丢弃。
- 模块只声明自己要用的查询，字段行只暴露批量版——那条 N+1 因此在包内写不出来。
- **`internal/api` 与提案表之间立了一条源码扫描守卫**（`TestAPIDoesNotQueryProposalTables`）。
  符号名不写死，从 sqlc 生成物里按 SQL 反推，新加一条打提案表的查询会自动纳入；不开白名单。
  它存在是因为编译器在这里拦不住：api 仍握着全量的 `Store`（收窄经评估不做，见 v1.4.0），
  「顺手查一下提案表」永远编译得过。
- **故障路径的报错更诚实了**（成功响应逐字未变）。此前单条应用遇到数据库故障会报
  404「提案不存在」或「系列不存在」——一次数据库抖动被说成「这条东西没了」，用户会去找一件
  其实还在的东西。现在只有真的查不到才回 404，其余故障回 500。相应地：
  - 收件箱三条查询原先各配一句 500 文案，统一为 `Failed to list metadata reviews`。
  - 单条应用的两句文案（`Failed to load review fields`、`Series not found`）并入
    `Failed to apply metadata review` 与 `Metadata review not found`。

---

### 🏷️ v1.4.1 — 2026-07-30（工具链与依赖升级）

> 纯维护版本，无功能变更、无数据库改动。v1.4.0 的产物是这批升级**之前**的代码。
> 从 v1.4.0 升级无需任何额外步骤（v1.4.0 本身的两条升级须知见下一段）。

#### CI
- **七个 GitHub Action 全部升到原生 node24。** GitHub 已弃用 Actions runner 上的 node20，
  此前有**四个** action 还在 node20（GitHub 的弃用告警只点了其中两个，另外两个是逐个查
  各自 `action.yml` 的 `runs.using` 才发现的），靠 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`
  被强制降级运行——CI 能过，但等 node20 真从 runner 上移除时会集体失效。
  - `golangci-lint-action` v7.0.1 → v9.3.0 · `upload-artifact` v5.0.0 → v7.0.1
    · `download-artifact` v6.0.0 → v8.0.1 · `action-gh-release` v2.6.2 → v3.0.2
    · `setup-node` v6.5.0 → v7.0.0 · `setup-go` v6.5.0 → v7.0.0
  - 逐条核对过跨大版本的破坏性变更对本仓用法的影响。其中 `download-artifact` v8 把
    「下载哈希不匹配」从警告改为**报错**——对发布流水线是净收益（我们本来就产出
    `SHA256SUMS.txt` 供自建者校验）。
  - `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` **保留不删**：所有 action 现已原生 node24，
    它因此变成安全网而非必需项——将来某次依赖升级若又引入 node20 的 action，
    它能让其继续跑，而不是等 runner 移除那天 CI 突然全红。已补注释写清这一点。

#### 依赖
- **Go 依赖 7 项**：`modernc.org/sqlite` 1.51.0 → 1.55.0（内嵌 SQLite 版本随之变化，
  因此单独验过迁移、FTS5 gram 索引、部分唯一索引与 RAR 夹具）、`golang.org/x/crypto`
  0.39.0 → 0.54.0（bcrypt 在鉴权路径上）、`x/image` 0.43.0 → 0.44.0、
  `go-chi/chi` 5.3.0 → 5.3.1、`rardecode` 2.2.3 → 2.3.0、`gen2brain/avif` 0.4.4 → 0.6.0。
- **前端依赖 21 项**，含四个大版本：`eslint` 9 → 10、`@vitejs/plugin-react` 5 → 6、
  `vite` 7 → 8、`@types/node` 25 → 26，以及 `@yui540/comimi` 0.5 → 0.18（阅读器的
  Comimi 主题）。前端高危漏洞从 **7 个降到 2 个**。
  - **TypeScript 暂留在 6**：`typescript-eslint` 目前没有任何版本支持 TS 7
    （最新 8.65.0 的 peer 仍是 `typescript <6.1.0`），强行升级会让 `npm ci` 直接
    ERESOLVE 失败。等上游支持后再单独升。
  - 剩下的 2 个高危漏洞是同一条——react-router 的 RSC 模式 CSRF 绕过
    ([GHSA-qwww-vcr4-c8h2](https://github.com/advisories/GHSA-qwww-vcr4-c8h2))。
    **对本仓不可达**（我们用的是最朴素的 `BrowserRouter`，没有 RSC 与 server actions），
    且**没有修复版可升**（通告范围 7.12.0–8.2.0，而 npm 上最新就是 7.18.2；
    `npm audit` 建议的「修复」是降到 7.11.0）。
- ⚠️ **待人工确认**：`comimi` 0.5 → 0.18 的运行时行为（手势、默认设置、菜单）无法用类型
  检查覆盖，建议在阅读器的 Comimi 主题上过一眼。它是**可选**主题，默认主题不受影响。

#### 修复
- eslint 10 默认启用了 `preserve-caught-error` 与 `no-useless-assignment`，报出 4 处真问题：
  登录 / 初始化 / 改密三处 `throw new Error(...)` 把原始的 axios 错误整个丢掉了
  （状态码与响应体这些排查要用的信息全没了），改为挂到 `cause` 上；
  另有一处读不到的变量初值。

---

### 🏷️ v1.4.0 — 2026-07-30（全量代码审计的逐批修复）

> 对整个代码库做了一次系统性体检并逐批修复，共 62 个提交。每条修复都配了回归测试，
> 高危项做了**反向验证**（临时回退修复、确认用例变红）；部分方案在对抗式复核后被判为
> 「不做」并在下面写明理由。
>
> **升级须知（两条，务必先读）：**
>
> 1. **不可回滚到 v1.4.0 之前的二进制。** `metadata_review_fields.status` 这一列已被删除
>    ——它是死数据（唯一写入点硬编码 `'pending'`，全仓无 UPDATE，且读路径只查 pending
>    审核，写进别的值也永远读不出来）。旧版二进制的 SELECT/INSERT 显式带这一列，
>    启动后会报 `no such column`。升级前请备份数据库。
> 2. **首次启动后 franchise 合集的链接会失效一次。** 这类系统合集改用稳定自然键后，
>    存量的无键行会被换成带键新行（id 变化）。如果你在 Mihon/OPDS 客户端里按 id 收藏过
>    franchise 合集、或在站内存了合集链接，需要重新添加一次；之后就不会再变了。
>    **手工创建的合集完全不受影响。**
>
> **对外行为变更：**
>
> - 元数据审核：对已处于终态的记录重复 apply/reject 由 200 变为 **409**。
> - 多用户站点新注册的 KOReader 设备在被管理员指派之前，其同步进度不计入任何用户
>   （此前会被静默记到 id 最小的管理员名下）。
> - 普通用户不再看到资料库增删改与系统设置的入口（后端一直是管理员专属，前端此前照样
>   渲染，点了只会吃 403）。
> - 批量应用元数据的响应新增 `partial` 桶。
> - 设置页「保存本分区」不再连带提交其他分区未保存的草稿；反过来保存后也不再抹掉它们。
>
> 下面按主题列出本次的全部改动。

## 更早的版本

- [2026-08](docs/changelog/2026-08.md) — 1 条
- [2026-07](docs/changelog/2026-07.md) — 95 条
- [2026-06](docs/changelog/2026-06.md) — 3 条
- [2026-05](docs/changelog/2026-05.md) — 58 条
- [2026-04](docs/changelog/2026-04.md) — 42 条
