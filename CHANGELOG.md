# Changelog

本文件记录项目各版本的功能新增、改动与修复。

---

### 🚧 未发布（后台磁盘作业收进单一入口）

> 后台**任务**每动一次磁盘，此前都要自己抄一遍同一套仪式：解析存储策略 → 按工种挑并发上限 →
> 申领**存储令牌** → 过**暂停闸门** → 干活 → 归还令牌 → 把等待与暂停的耗时折进指标。全仓八个
> 取用点各一份，`scanner` 与 `api` 还各自维护了一份取令牌的辅助方法与一份逐字相同的上限公式。
> 本批把这套仪式整个收进新的 `internal/diskwork`：调用方交出「干哪一类活、字节落在哪」和一个
> 闭包，其余全部由模块承担。八个取用点逐个转换完毕，旧接口在同一批里删除。
>
> 配置结构、数据库结构与线上契约均未改动，升级无需额外步骤。

#### 改动
- **每个工种能并发几个，改由工种自己决定，不再取决于发起它的是哪个子系统。** 此前同一个工种在
  两个包里会拿到两个不同的上限——`identity_hash` 在扫描器里是 min(扫描并发, 哈希并发)，在维护
  任务里是 min(哈希并发, 归档打开并发)，而它根本不打开归档；`metadata_scan` 同样分叉。现在按
  「这个工种到底在干什么」裁定：读归档元数据用归档打开并发、算文件指纹用哈希并发、构建封面用
  min(归档打开并发, 封面并发)、写缓存用封面并发。
  - **影响面只有手工把某个库的四项并发配成不同正值的部署。** 四种存储档位的内置值一个都不受
    影响：默认档与 SSD 档四项全为零（本就不限流、不排队），外置硬盘档与网络存储档四项全为一，
    新旧公式在它们上面结果完全相同。
- **按下暂停后，后台不再继续往盘上写缩略图。** 此前从上一次暂停检查到缩略图落盘之间隔着整个
  图片转码，暂停在这一段是不生效的——用户按下暂停，盘上仍会继续多出文件。现在写文件之前必过
  一次闸门。
- **存储策略的解析源统一成「字节落在哪」。** 此前有五处按**资料库根**解析——扫描器的归档打开与
  两处指纹取这次扫描的库根，维护子域的两处指纹回填取那本书所属资料库的路径——现在一律按真正被
  读写的那个文件的路径解析。
  存储策略走最长前缀匹配，因此只有**配了比库根更深的策略条目**时，这几处生效的档位才会变；
  只按库根配策略的部署不受影响。缓存目录的写入另有一条规则：卷键仍由缓存目录本身决定，而 IO
  策略在它与藏书同在一块盘上时服从藏书那一侧——它通常落不进任何资料库的前缀，却和藏书抢同一
  块盘。
- **慢盘上「等令牌等了很久」现在八处都会记日志。** 此前只有封面构建与缓存写入两处会留下记录，
  扫描、指纹与 ComicInfo 回写卡在令牌上是无声的——而它们才是慢盘上真正会堵住的那几处。日志带
  工种、路径、存储档位、卷键与等待毫秒；等了很久最终被取消的那次申领也会留下记录。缩略图落盘
  自己那条慢日志不变，它另有一半在计写入耗时。

#### 架构
- 新建 `internal/diskwork`，位于 `config`、`storageio`、`taskcontrol` 三个叶子包之上，由
  `scanner` 与 `api` 共用。对外只有一个入口：交出一份**磁盘作业**声明和一个闭包。
- **三条契约由结构而不是调用方的记性保证**：闸门必然先于令牌，被暂停的作业不会攥着令牌把别人
  堵死；令牌的归还不依赖闭包走哪条出口，闭包 panic 时同样已归还；能并发几个只由工种决定。
  相应地，两处为「提前归还 + 兜底归还」手写的幂等布尔、一处为「取到令牌之后再看一眼取消」
  手写的 `select` 一并消失。
- **等待与暂停的耗时是返回值而不是副作用**，模块不持有也不上报任何指标；取用点上只剩一行
  「把它折进我的指标」，此前逐处手写的耗时折算与 `nil` 守卫沉进两个包各自的那一个方法里。
- **令牌的持有区间钉死在真正的 IO 上。** 归档打开那一处此前握着令牌多跑了一段纯 CPU 的路径
  解析、复合哈希与排序号提取，ComicInfo 回写那一处多跑了一段聚合与序列化——在慢盘上这些都在
  虚占归档打开的并发额度。
- 前台的阅读取页**不是**磁盘作业，仍直接向调度器申领：它不过暂停闸门、上下文刻意去掉取消、
  耗时进请求诊断而非任务指标，收进来只会换得一个「这几件事都反着来」的开关。
- 删除两个包各自的取令牌辅助方法（含 api 那份里没有任何调用点的封面构建死分支），以及那条从
  扫描器内部伸手进未导出方法、靠固定时长 `sleep` 去测 `storageio` 自身排队语义的用例——该包
  已有一条等价用例。扫描器包内不再有任何用例触碰取令牌的内部。
- **新增覆盖**：模块的契约用例压在唯一的新接缝（构造点）上，注入新建的调度器与桩配置函数，
  无数据库、无文件树、无扫描器，覆盖四个工种的上限、短路、闸门先于令牌、取消、归还的必然性
  （闭包返回错误与闭包 panic 两条路径）与 `Stats` 如实返回。另补上三组此前**零覆盖**的失败
  出口：归档打开失败与页目录读取失败、封面那一页读不出、指纹算不出来时的分页游标推进——它们
  正是本次删掉手写归还闭包之后唯一可能漏掉令牌或卡死游标的地方。

---

### 🚧 未发布（元数据提案独立成子域）

> 元数据提案的入队、裁决与读取从 `internal/api` 整体搬进新的 `internal/proposal`。
> 绝大部分是内部改动，用户可见的只有下面两条。无数据库改动，升级无需额外步骤。

#### 改动
- **批量处理时「已被别人抢先」的条目不再报成失败。** 批量应用与批量拒绝的响应各新增一个
  conflict 桶，收「这条提案已经不是待裁决」这一种结局——最常见的情形就是另一个标签页、
  另一个人或上一次操作已经处理过它们。此前它们混在 failed 里，于是一堆并没有坏掉的东西
  会让整条汇总提示变红。现在 failed 只收真故障（查询报错、写入失败、提案查不到），
  提示的严重度也只看 failed。两个语种的批量结果文案各加了一项「已被处理 N 项」。
  - 单条应用与单条拒绝的 409 语义不变。
- **打开有多条待裁决提案的系列时不再随提案条数发起额外查询。** 此前系列详情页在提案列表上
  逐条取字段行（N 条提案 = N 次查询），收件箱那条路径早就是批量取。两处现在同一条通路。

#### 架构
- 新建 `internal/proposal`，承载提案子域的全部规则：入队去重（对待裁决提案与最近的拒绝记录
  逐字比对）、按**锁定字段**过滤、应用与拒绝，以及三条读路径（按系列列出、
  收件箱分页、待裁决计数）。元数据写入器一并迁入，那条把事务句柄递出去的 hook 参数随之消失。
- **每一种结局都是结果值而不是哨兵错误。** 入队的五种（已入队 / 复用既有提案 / 无差异 /
  差异全被锁 / 此前已拒绝）、应用的六种、拒绝的三种各由一个分类表达，`error` 只留给故障。
  四个哨兵错误与 api 侧四个 handler 里的 `errors.Is` 一并删除——此前「良性结局不退化成
  500」全靠「每个哨兵都必须被识别到」这条纪律撑着，漏一个，用户看到的就是「服务器错误」
  而不是「先去解锁字段」。被抢先无论是前置检查发现的还是事务内 CAS 撞上的，都是同一个分类。
- 「锁定状态按**当前**值算」从视图映射里的一个 `||` 迁进模块，成为字段上的终值。它是一条
  裁决规则而不是渲染细节：它防的是「先入队、后加锁」的字段没有锁徽章、用户点了应用却被
  静默丢弃。
- 模块只声明自己要用的查询，字段行只暴露批量版——那条 N+1 因此在包内写不出来。
- **`internal/api` 与提案表之间立了一条源码扫描守卫**（`TestAPIDoesNotQueryProposalTables`）。
  符号名不写死，从 sqlc 生成物里按 SQL 反推，新加一条打提案表的查询会自动纳入；不开白名单。
  它存在是因为编译器在这里拦不住：api 仍握着全量的 `Store`（收窄经评估不做，见 v1.4.0），
  「顺手查一下提案表」永远编译得过。
- **故障路径的报错更诚实了**（成功响应逐字未变）。此前单条应用遇到数据库故障会报
  404「提案不存在」或「系列不存在」——一次数据库抖动被说成「这条东西没了」，用户会去找一件
  其实还在的东西。现在只有真的查不到才回 404，其余故障回 500。相应地：
  - 收件箱三条查询原先各配一句 500 文案，统一为 `Failed to list metadata reviews`。
  - 单条应用的两句文案（`Failed to load review fields`、`Series not found`）并入
    `Failed to apply metadata review` 与 `Metadata review not found`。

---

### 🏷️ v1.4.1 — 2026-07-30（工具链与依赖升级）

> 纯维护版本，无功能变更、无数据库改动。v1.4.0 的产物是这批升级**之前**的代码。
> 从 v1.4.0 升级无需任何额外步骤（v1.4.0 本身的两条升级须知见下一段）。

#### CI
- **七个 GitHub Action 全部升到原生 node24。** GitHub 已弃用 Actions runner 上的 node20，
  此前有**四个** action 还在 node20（GitHub 的弃用告警只点了其中两个，另外两个是逐个查
  各自 `action.yml` 的 `runs.using` 才发现的），靠 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`
  被强制降级运行——CI 能过，但等 node20 真从 runner 上移除时会集体失效。
  - `golangci-lint-action` v7.0.1 → v9.3.0 · `upload-artifact` v5.0.0 → v7.0.1
    · `download-artifact` v6.0.0 → v8.0.1 · `action-gh-release` v2.6.2 → v3.0.2
    · `setup-node` v6.5.0 → v7.0.0 · `setup-go` v6.5.0 → v7.0.0
  - 逐条核对过跨大版本的破坏性变更对本仓用法的影响。其中 `download-artifact` v8 把
    「下载哈希不匹配」从警告改为**报错**——对发布流水线是净收益（我们本来就产出
    `SHA256SUMS.txt` 供自建者校验）。
  - `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` **保留不删**：所有 action 现已原生 node24，
    它因此变成安全网而非必需项——将来某次依赖升级若又引入 node20 的 action，
    它能让其继续跑，而不是等 runner 移除那天 CI 突然全红。已补注释写清这一点。

#### 依赖
- **Go 依赖 7 项**：`modernc.org/sqlite` 1.51.0 → 1.55.0（内嵌 SQLite 版本随之变化，
  因此单独验过迁移、FTS5 gram 索引、部分唯一索引与 RAR 夹具）、`golang.org/x/crypto`
  0.39.0 → 0.54.0（bcrypt 在鉴权路径上）、`x/image` 0.43.0 → 0.44.0、
  `go-chi/chi` 5.3.0 → 5.3.1、`rardecode` 2.2.3 → 2.3.0、`gen2brain/avif` 0.4.4 → 0.6.0。
- **前端依赖 21 项**，含四个大版本：`eslint` 9 → 10、`@vitejs/plugin-react` 5 → 6、
  `vite` 7 → 8、`@types/node` 25 → 26，以及 `@yui540/comimi` 0.5 → 0.18（阅读器的
  Comimi 主题）。前端高危漏洞从 **7 个降到 2 个**。
  - **TypeScript 暂留在 6**：`typescript-eslint` 目前没有任何版本支持 TS 7
    （最新 8.65.0 的 peer 仍是 `typescript <6.1.0`），强行升级会让 `npm ci` 直接
    ERESOLVE 失败。等上游支持后再单独升。
  - 剩下的 2 个高危漏洞是同一条——react-router 的 RSC 模式 CSRF 绕过
    ([GHSA-qwww-vcr4-c8h2](https://github.com/advisories/GHSA-qwww-vcr4-c8h2))。
    **对本仓不可达**（我们用的是最朴素的 `BrowserRouter`，没有 RSC 与 server actions），
    且**没有修复版可升**（通告范围 7.12.0–8.2.0，而 npm 上最新就是 7.18.2；
    `npm audit` 建议的「修复」是降到 7.11.0）。
- ⚠️ **待人工确认**：`comimi` 0.5 → 0.18 的运行时行为（手势、默认设置、菜单）无法用类型
  检查覆盖，建议在阅读器的 Comimi 主题上过一眼。它是**可选**主题，默认主题不受影响。

#### 修复
- eslint 10 默认启用了 `preserve-caught-error` 与 `no-useless-assignment`，报出 4 处真问题：
  登录 / 初始化 / 改密三处 `throw new Error(...)` 把原始的 axios 错误整个丢掉了
  （状态码与响应体这些排查要用的信息全没了），改为挂到 `cause` 上；
  另有一处读不到的变量初值。

---

### 🏷️ v1.4.0 — 2026-07-30（全量代码审计的逐批修复）

> 对整个代码库做了一次系统性体检并逐批修复，共 62 个提交。每条修复都配了回归测试，
> 高危项做了**反向验证**（临时回退修复、确认用例变红）；部分方案在对抗式复核后被判为
> 「不做」并在下面写明理由。
>
> **升级须知（两条，务必先读）：**
>
> 1. **不可回滚到 v1.4.0 之前的二进制。** `metadata_review_fields.status` 这一列已被删除
>    ——它是死数据（唯一写入点硬编码 `'pending'`，全仓无 UPDATE，且读路径只查 pending
>    审核，写进别的值也永远读不出来）。旧版二进制的 SELECT/INSERT 显式带这一列，
>    启动后会报 `no such column`。升级前请备份数据库。
> 2. **首次启动后 franchise 合集的链接会失效一次。** 这类系统合集改用稳定自然键后，
>    存量的无键行会被换成带键新行（id 变化）。如果你在 Mihon/OPDS 客户端里按 id 收藏过
>    franchise 合集、或在站内存了合集链接，需要重新添加一次；之后就不会再变了。
>    **手工创建的合集完全不受影响。**
>
> **对外行为变更：**
>
> - 元数据审核：对已处于终态的记录重复 apply/reject 由 200 变为 **409**。
> - 多用户站点新注册的 KOReader 设备在被管理员指派之前，其同步进度不计入任何用户
>   （此前会被静默记到 id 最小的管理员名下）。
> - 普通用户不再看到资料库增删改与系统设置的入口（后端一直是管理员专属，前端此前照样
>   渲染，点了只会吃 403）。
> - 批量应用元数据的响应新增 `partial` 桶。
> - 设置页「保存本分区」不再连带提交其他分区未保存的草稿；反过来保存后也不再抹掉它们。
>
> 下面按主题列出本次的全部改动。

## 更早的版本

- [2026-08](docs/changelog/2026-08.md) — 1 条
- [2026-07](docs/changelog/2026-07.md) — 95 条
- [2026-06](docs/changelog/2026-06.md) — 3 条
- [2026-05](docs/changelog/2026-05.md) — 58 条
- [2026-04](docs/changelog/2026-04.md) — 42 条
